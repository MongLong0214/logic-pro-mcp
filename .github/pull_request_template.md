## Summary

-

## Related issue

-

## Verification

What did you actually run, and what did it say? Commands and results, not checkboxes. You are not
expected to have run the whole repository's checks — CONTRIBUTING.md, *Who does what*, says which
are yours and which are CI's.

```text

```

Not verified (and why):

-

## Logic evidence

Every fact this change states about Logic is either cited to Apple's own bytes, or declared to
state no fact about Logic. `docs/canon/README.md` gives the reference format, the exact opt-out
wording, and when the opt-out is refused. `Scripts/check-canon-citations.py` checks this offline
and the `pr-policy` check runs it against this description.

Paste each reference with the value it resolves to; a reference on its own is not a citation.

```text

```

<!--
If this change states nothing about Logic, do not paste anything above: write the opt-out sentence
docs/canon/README.md gives, in your own visible prose below, with the reason. It is deliberately
not typed here -- a sentence the template writes for you is a sentence nobody meant, and the first
version of this file shipped it pre-filled, so every untouched template passed. The opt-out is
refused outright for a change that edits a Logic-facing path, whatever the description says.
-->

## Risks and limitations

- User-visible impact:
- What this change does not do:
- Known limitation:
