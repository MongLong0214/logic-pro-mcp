# T1: #302 R1 — sealed independent-expected type + rejection-only verify guard (dark)

**PRD Ref**: PRD-issue-302 > R1 acceptance · **ADR Ref**: ADR-014 (Decision B, strict)
**Priority**: P1 · **Size**: M · **Status**: Todo · **Depends On**: None · **Executor**: opus sub-agent

> ⛔ **DO NOT START implementation until CEO Gate 3 PASS.** No scope-file edit, no TDD step, no evidence generation before the CEO records Gate 3 PASS on this exact ticket. Any scope-file modification before Gate 3 PASS VOIDS the lane: stop the implementer, restore both scope files byte-identical to base, discard the pre-approval evidence, and re-satisfy the §0 clean-scope precondition. (This ticket previously had a premature implementer breach the gate — hence the hard precondition below.)

## 0. Base, preconditions, evidence binding, scope
- **Base SHA (exact):** `ab799fa3bef40a1338eed9544742489239b21215`, branch `feat/adr014-302-r1-independence-core`.
- **Preconditions** (`abort+escalate` if any false): (1) `test "$(git rev-parse HEAD)" = ab799fa3bef40a1338eed9544742489239b21215`. (2) base `verifyRegion` returns `.exactMatch` for a note-identical O→E fixture. (3) base test `sealedIndependentProofGrantsExactMatch` exists. (4) **CLEAN SCOPE (step-gate enforcement):** `git diff --quiet HEAD -- Sources/LogicProMCP/MIDIReadback/MIDINoteIndependentVerification.swift Tests/LogicProMCPTests/MIDIIndependentVerificationTests.swift` exits 0 AND `git diff --quiet --cached HEAD -- Sources/LogicProMCP/MIDIReadback/MIDINoteIndependentVerification.swift Tests/LogicProMCPTests/MIDIIndependentVerificationTests.swift` exits 0 (both the worktree and the index, real paths — no placeholder) — both scope files byte-identical to base, nothing staged. If dirty → **a prior/rogue implementer already ran; ABORT**, restore to base, discard its evidence, do NOT proceed. (5) **CEO Gate 3 PASS for THIS EXACT revision (mechanical, not prose):** on PASS the CEO writes the approval receipt `~/.openclaw/workspace/reviews/302-r1-evidence/GATE3-APPROVAL.json` = `{"base":"ab799fa3bef40a1338eed9544742489239b21215","ticket_sha256":"<sha256 of T1-strict-verify-guard.md at approval>","approval_ts":"<ISO-8601>","source_blob":"5b0b95686032a70321991e4b0db9df76c41cdc2e","test_blob":"<HEAD:Tests/.../MIDIIndependentVerificationTests.swift blob>"}`. Before ANY TDD step the implementer MUST mechanically verify ALL of: `test "$(shasum -a 256 docs/tickets/issue-302-r1-independence-guard/T1-strict-verify-guard.md | cut -d' ' -f1)" = <receipt.ticket_sha256>` (current ticket byte-identical to the approved revision — blocks a stale/pre-edit approval); `test "$(git rev-parse HEAD)" = <receipt.base>`; `test "$(git rev-parse HEAD:Sources/LogicProMCP/MIDIReadback/MIDINoteIndependentVerification.swift)" = <receipt.source_blob>`; `test "$(git rev-parse HEAD:Tests/LogicProMCPTests/MIDIIndependentVerificationTests.swift)" = <receipt.test_blob>`. Any mismatch → the approval is for a different revision/state → ABORT (no TDD, no evidence). The evidence run dir is `run-<receipt.approval_ts>` (§7).
- **Baseline manifest (content SHA-256, NOT `git status` — porcelain misses edits inside the untracked ticket dir and further edits to the already-`M` PRD):** at start, from repo root, hash the WHOLE worktree (not just docs/Sources/Tests — must catch `Package.swift`, `CHANGELOG.md`, `Scripts/`, config, anything), pruning only VCS/build artifacts and the transient `Package.resolved`; paths from `find .` HAVE a `./` prefix so excludes carry it: `find . -type f -not -path './.git/*' -not -path './.build/*' -not -path './.swiftpm/*' -not -path './Package.resolved' ! -path './Sources/LogicProMCP/MIDIReadback/MIDINoteIndependentVerification.swift' ! -path './Tests/LogicProMCPTests/MIDIIndependentVerificationTests.swift' -print0 | sort -z | xargs -0 shasum -a 256 > /tmp/302-baseline.sha`. Sanity-assert the two scope files are ABSENT from the manifest (`! grep -q 'MIDINoteIndependentVerification.swift\|MIDIIndependentVerificationTests.swift' /tmp/302-baseline.sha`) — else the exclude failed → abort. Scope-delta check (run at start of S1 AND end of S11, after `git checkout -- Package.resolved`): recompute the identical `find … > /tmp/302-current.sha` and `diff /tmp/302-baseline.sha /tmp/302-current.sha` MUST be empty (every non-scope doc/source/test byte-identical). Any non-empty diff → abort. The ONLY files allowed to change are the two scope files.
- **Evidence binding (no dev commits):** each artifact records `base=ab799fa…`, `scopeDiffSHA=$(git diff HEAD -- Sources/LogicProMCP/MIDIReadback/MIDINoteIndependentVerification.swift Tests/LogicProMCPTests/MIDIIndependentVerificationTests.swift | shasum -a 256 | cut -d' ' -f1)` (**`git diff HEAD` — HEAD-relative so any staged edit is included**), `step`. A RED and its GREEN MUST differ in `scopeDiffSHA`.
- **Scope files (ONLY):** `Sources/LogicProMCP/MIDIReadback/MIDINoteIndependentVerification.swift`, `Tests/LogicProMCPTests/MIDIIndependentVerificationTests.swift`. Forbidden (→abort): editing any other file (incl. `MIDINoteCanonicalizer.swift`, `Package.swift`), any `git add`/staging or commit/push during S1–S11 (keeps working-tree `git diff` and `git diff HEAD` identical), leaving `Package.resolved` dirty.

## 1. Frozen symbols + base-suite fate
Final: `RegionMatchVerdict{ mismatch(added:removed:), incompleteCannotVerify(reason:) }` (no other case). `IndependentExpectedProof{ unproven; #if QUALIFICATION_FAULT_SEAM testFixture(CallerTrustedFixture) }`. `CallerTrustedFixture` (seam-only): keeps base field access (`root`/`rootID` internal, `region`/`expectedNotes`/`expectedPPQ`/`contentBinding` fileprivate); the non-forgeability guard is the `fileprivate init`; ADD a second seam-only `fileprivate init(root:rootID:region:notes:ppq:overrideDigest: String)` that sets `contentBinding = overrideDigest` (bypassing co-compute) — used ONLY by `makeCorruptedTestFixture`. Makers (seam-only): `makeTestFixture(root:rootID:region:notes:ppq:)` (normal init), `makeCorruptedTestFixture(root:rootID:region:notes:ppq:digest:)` (override init). `IndependentExpectedRoot{ authoredIntent, controlledExport }`.
Rename map (S4, behavior-unchanged): `proven→testFixture`, `SealedIndependentExpected→CallerTrustedFixture`, `IndependentExpectedSeam.mint→makeTestFixture`.
Base-suite @Test fate: DELETE `sealedIndependentProofGrantsExactMatch` (S3). RENAME + STRENGTHEN (S4/S5/S7, MODIFY in place — NOT re-add): `unprovenExpectedNeverMatches→unprovenExpectedRejected`, `foreignNotesYieldMismatchNotMatch→foreignNotesYieldMismatch`, `incompleteObservedNeverMatches→incompleteObservedRejected`. KEEP + STRENGTHEN: `samePipelineRootRejected`, `wrongRegionBindingRejected`. ADD-new: `oToECopyDoesNotMatch`(S1), `noExactMatchCaseExists`(S2), `contentBindingTamperRejected`+`normalMakerCannotMismatch`(S6), `observedPPQNonPositiveRejected`+`fixturePPQNonPositiveRejected`+`ppqNormalizationOverflowRejected`(S7ppq), `provenanceRootsDistinctAndNonObserved`(S8). External check (not a @Test): `negativeCompileNonReconstruction`(S9).

## 2. AC ↔ @Test (closed) + assertion form
**[AC numbers match PRD-issue-302 criteria 1–7 EXACTLY.]** AC-1 (release non-constructibility)→`negativeCompileNonReconstruction`(§5/S9)+release census(§3); AC-2 (no positive-match case)→`noExactMatchCaseExists` + structural (every rejection test also asserts one of the two verdict cases); AC-3a→`unprovenExpectedRejected`; AC-3b→`samePipelineRootRejected`; AC-3c→`wrongRegionBindingRejected`; AC-3d→`incompleteObservedRejected`; AC-3e→`contentBindingTamperRejected`(+`normalMakerCannotMismatch`); **AC-3f = PRD's single "non-positive or overflowing PPQ" criterion, refined into atomic sub-criteria:** AC-3f.1→`observedPPQNonPositiveRejected`; AC-3f.2→`fixturePPQNonPositiveRejected`; AC-3f.3→`ppqNormalizationOverflowRejected`; AC-4→`oToECopyDoesNotMatch`; AC-5→`foreignNotesYieldMismatch`; AC-6 (provenance taxonomy)→`provenanceRootsDistinctAndNonObserved`; AC-7 (no production path)→0 public API + 0 production caller + release census (§3) + LIVE_NA (§5).
Rejection tests: `guard case .incompleteCannotVerify(let reason) = result else { Issue.record(...); return }; #expect(reason.contains("<exact base token>"))`. `oToECopyDoesNotMatch`: `guard case .incompleteCannotVerify` (no `.exactMatch` ref). `foreignNotesYieldMismatch`: `guard case .mismatch(let added, let removed)` + `#expect(added == [<the observed-only note>])` + `#expect(removed == [<the expected-only note>])` (not just the case). Base tokens (verbatim from base `verifyRegion`): "observed readback is not complete"; "expected must carry a sealed independent provenance proof"; "expected root shares the observed conversion pipeline"; "expected proof is not bound to the observed region"; "expected proof content binding is invalid"; "PPQ must be positive"; "PPQ normalization overflow".

## 3. Commands (exact, executable) + expected
ALL commands run under **bash** (`bash -c '…'`, NOT zsh — `${PIPESTATUS}` is empty in zsh and silently masks failures). Wrapper: `set -o pipefail; <cmd>; ec=$?` — with `pipefail`, `$?` after a `… | tail` pipe equals the FAILING element (swift), not tail. Record `ec`.
- Focused: `swift test --no-parallel --filter MIDIIndependentVerificationTests 2>&1 | tail -40`  (RED: `ec!=0`; GREEN: `ec==0`).
- Build-tests (for S9 compile probes): `swift build --build-tests 2>&1 | tail -25`.
- Full: `swift test --no-parallel 2>&1 | tail -40` (`ec==0`).
- Release + census (exact shell; guard the build BEFORE hashing so a stale binary can't false-PASS): `set -o pipefail; swift build -c release 2>&1 | tail -3; ec=$?; test "$ec" -eq 0 || exit "$ec"; relSHA=$(shasum -a 256 .build/release/LogicProMCP | cut -d' ' -f1); for S in CallerTrustedFixture makeCorruptedTestFixture makeTestFixture; do n=$(strings .build/release/LogicProMCP | grep -c "$S" || true); [ "$n" = 0 ] || { echo "CENSUS-FAIL $S=$n"; exit 1; }; done; git checkout -- Package.resolved`. **Census token set = seam-ONLY symbols** (compiled out in release): `CallerTrustedFixture`, `makeCorruptedTestFixture`, `makeTestFixture`. Deliberately EXCLUDED: `IndependentExpected*` (the KEPT `IndependentExpectedProof`/`IndependentExpectedRoot` legitimately survive in release → would false-fail), `QUALIFICATION_FAULT_SEAM` (a compile flag, never a binary string), `testFixture` (too short/ambiguous a substring). Census is necessary-not-sufficient; S9 is the authoritative non-reconstruction proof.
- No-positive-path (both scope files, after S3): `test -z "$(grep -nE 'exactMatch|grants a match' Sources/LogicProMCP/MIDIReadback/MIDINoteIndependentVerification.swift Tests/LogicProMCPTests/MIDIIndependentVerificationTests.swift | grep -v 'noExactMatchCaseExists')"`.
- 0 public API — **base-relative diff (NOT `git diff --`, which is EMPTY on a clean committed candidate → false-PASS at S12):** `test -z "$(git diff ab799fa3bef40a1338eed9544742489239b21215 -- Sources/LogicProMCP/MIDIReadback/MIDINoteIndependentVerification.swift | grep -E '^\+[[:space:]]*public ')"`. Correct at S3 (working-tree vs base) AND at S12 (candidate checkout vs base, since `candidateSHA^`==base); S12 may equivalently use `git diff "${candidateSHA}^" "$candidateSHA" -- Sources/LogicProMCP/MIDIReadback/MIDINoteIndependentVerification.swift`.
- 0 production callers — **scope-wide, MULTILINE census** (plain `grep` is line-based and misses a `verifyRegion(\n  observed:` call, so use `rg -U` which crosses newlines; does NOT exclude the verification file; verifies exactly 1 declaration + 0 calls): `total=$(rg -U -o --no-filename 'verifyRegion\s*\(\s*observed:' Sources -g '*.swift' | wc -l | tr -d ' '); decls=$(rg -U -o --no-filename 'func\s+verifyRegion\s*\(\s*observed:' Sources -g '*.swift' | wc -l | tr -d ' '); test "$decls" = 1 && test "$total" = "$decls"` — `decls`==1 (the sole declaration) AND `total`==`decls` (zero additional invocation sites anywhere in `Sources`, verification file included, across line breaks). Any inequality → abort.

## 4. Atomic steps (compilable behavioral mutations only; no comment-out that breaks compile; no out-of-scope edits). Each: change → focused → observe STATED RED → minimal GREEN → focused → PASS. Abort on false-RED. Artifact `S{n}-{tag}` (cmd, ec, base, scopeDiffSHA, step, excerpt).
S1–S3 pre-rename (base API `mint`/`.proven`); rename at S4; S5+ final names.
- **S1** (AC-4 behavioral RED): add `oToECopyDoesNotMatch` → focused RED (`ec!=0`, returns `.exactMatch`). `S1-red`.
- **S2** (AC-2 compile RED, pre-Sources): add `noExactMatchCaseExists` (exhaustive switch, no default, cases `.mismatch`/`.incompleteCannotVerify`) → focused compile RED. `S2-compile-red`. (S3 gated on this.)
- **S3** (GREEN S1+S2, one Sources edit): delete `.exactMatch` case + delete `sealedIndependentProofGrantsExactMatch`; `verifyRegion`: equal notes → `.incompleteCannotVerify(reason:"R1 grants no positive match; independent positive verification is R2 live-ingestion")`, else `.mismatch(added:removed:)`. **Decision-B comment cleanup (part of this edit):** also delete the test-file `// --- POSITIVE CONTROL …` section comment and the header-comment sentences that describe the positive-grant control (they document the now-deleted test), and rewrite `verifyRegion`'s doc-comment line "Grants `.exactMatch` only when…" (now false) to "R1 grants NO positive match — rejection-only; the expected is a seam-only fixture, not a proof." Focused GREEN. `S3-green`. Run no-positive + public/caller checks → empty. `S3-grep0`.
- **S4** (rename per §1 map + rename the 3 base tests; **+ Decision-B vocabulary cleanup**; behavior-unchanged) → focused GREEN. `S4-green`. Vocabulary: rename test helper `e0Proven`→`e0TestFixture` (+ its doc line); in source AND test doc-comments/identifiers replace "genuine proof"/"proven proof"/"non-forgeable proof"/"positive grant control" with "seam-only test fixture", and REMOVE "feature flag off" from the top source comment (the gating is the compile seam `#if QUALIFICATION_FAULT_SEAM`, not a runtime flag). **CRITICAL: do NOT alter the `verifyRegion` guard reason STRINGS** (e.g. "…sealed independent provenance proof", "expected proof content binding is invalid") — those are the §2 asserted tokens and must stay verbatim; only comments and identifiers change. Decision-B: the expected is a FIXTURE, not a proof; R1 grants no positive match. **ALSO fix these two trust-boundary comments (correct wording, in-scope):** (L1) the top source header must NOT say `verifyRegion can never grant a positive match in release` (that implies a grant is possible under the seam) — reword so it states **R1 grants NO positive match in ANY configuration (including the debug seam); a positive grant is future R2 (live-ingestion) work; the seam exercises only the rejection guards.** (L4) the `CallerTrustedFixture` doc must NOT say "no … factory" unqualified (the debug-seam makers ARE the construction path) — reword to **no RELEASE/PRODUCT-accessible construction (no public/internal init, no `Codable`/`RawRepresentable`/memberwise); the ONLY construction is the debug-seam makers via the `fileprivate` init, entirely absent from release; a decoded or label-only expected can never present one.**
- **S5** (AC-5, MODIFY the renamed `foreignNotesYieldMismatch`): strengthen to assert `.mismatch` added==[observed-only note] & removed==[expected-only note]. GREEN under S3. Mutation-proof (in-scope, compilable): in `verifyRegion`'s diff, swap `added`/`removed` in the returned `.mismatch` → focused RED (contents wrong) → restore → GREEN. `S5-mut-red`,`S5-green`.
- **S6** (AC-3e): add the `overrideDigest` seam init + `makeCorruptedTestFixture` + `contentBindingTamperRejected` (token "content binding") + `normalMakerCannotMismatch` (a `makeTestFixture` fixture's `independentPayload.contentBinding == midiRegionNoteDigest(payload.notes, ppq: payload.ppq)`). Base content-binding guard exists → GREEN. Mutation-proof (compilable): change that guard's `else` return from `.incompleteCannotVerify(...)` to `.mismatch(added:[],removed:[])` → `contentBindingTamperRejected` (asserts incomplete+token) RED → restore. `S6-red`,`S6-green`.
- **S7** (AC-3a-d, MODIFY renamed/kept tests, strengthen to reason-tokens; compilable result-changing mutation = change that guard's `else` return verdict, NOT comment-out): `unprovenExpectedRejected` (mutate the `guard let independent = expected.independentPayload else { return .incompleteCannotVerify(...) }` → `else { return .mismatch(added:[],removed:[]) }`); `samePipelineRootRejected`; `wrongRegionBindingRejected`; `incompleteObservedRejected` — for each, flip its guard's else-return verdict → test RED → restore. Tokens per §2. `S7-<name>-red/green`.
- **S7ppq** (AC-3f.1/.2/.3 — the three atomic sub-criteria of PRD 3f "non-positive or overflowing PPQ"; distinct tokens; mutate the IN-SCOPE call-site else-branch): `observedPPQNonPositiveRejected` (observed.ppq=0, token "PPQ must be positive"); `fixturePPQNonPositiveRejected` (fixture.ppq=0, token "PPQ must be positive"); `ppqNormalizationOverflowRejected` (fixture ppq=1, observed ppq=480, note `startTicks=Int64.max/240`, token "PPQ normalization overflow"). All GREEN under base. Mutations (compilable, in `verifyRegion`): for f/g — change the `guard observed.ppq>0, independent.ppq>0 else { return .incompleteCannotVerify(reason:"PPQ must be positive") }` else-return to `.mismatch(added:[],removed:[])` → f/g RED → restore. For h — change the `guard let normalized = normalizePPQ(...) else { return .incompleteCannotVerify(reason:"PPQ normalization overflow") }` else-return to `.mismatch(added:[],removed:[])` → h RED → restore (do NOT touch `normalizePPQ`). `S7ppq-*-red/green`.
- **S8** (AC-6): `provenanceRootsDistinctAndNonObserved` (exhaustive switch over `IndependentExpectedRoot` no-default + `#expect` `authoredIntent.rawValue != controlledExport.rawValue` && neither == `MIDIRegionNoteSnapshot.eventListConversionID`). Mutation-proof (compilable, not duplicate): set `authoredIntent`'s rawValue to `"eventListAX.readback.v1"` (== observed pipeline id; compiles, unique among the enum) → test RED (collides with observed) → restore. `S8-mut-red`,`S8-green`.
- **S9** (AC-1 negative-compile, in-scope test file — NOT external): one at a time, insert each offending line as the SOLE body of a temporary `throws` function placed immediately after the suite's opening brace (`@Suite struct MIDIIndependentVerificationTests {`, base line 37) — `@Test func __ac6Probe() throws { <offending line> }` (`throws` so probe (b)'s `try` is legal and only the Decodable absence is tested; inside the suite, NEVER at file top-level, which would emit a spurious "expressions are not allowed at the top level" error instead of the access diagnostic), run `swift build --build-tests 2>&1 | tail -25`, capture the EXACT diagnostic, then delete the temp function before the next probe. Probe args use REAL fixtures/labels (base init is `fileprivate init(root:rootID:region:notes:ppq:)`, so labels match → the diagnostic is the access failure, not a label/scope mismatch): (a) `_ = CallerTrustedFixture(root: .authoredIntent, rootID: "x", region: e0RegionA, notes: [], ppq: 480)` → `'CallerTrustedFixture' initializer is inaccessible due to 'fileprivate' protection level`; (b) `_ = try JSONDecoder().decode(CallerTrustedFixture.self, from: Data())` → `instance method 'decode(_:from:)' requires that 'CallerTrustedFixture' conform to 'Decodable'`; (c) `_ = CallerTrustedFixture(rawValue: "x")` → inaccessible-init / `argument 'rawValue' … no exact matches` (type is not `RawRepresentable`). Plus **`S9-grep` (exact command + expected):** `test -z "$(grep -nE ': *(Codable|Decodable|Encodable|RawRepresentable)|public init|internal init|init\(rawValue' Sources/LogicProMCP/MIDIReadback/MIDINoteIndependentVerification.swift)"` (0 matches → no Codable/RawRepresentable conformance and no public/internal/rawValue initializer) AND `grep -q 'fileprivate init' Sources/LogicProMCP/MIDIReadback/MIDINoteIndependentVerification.swift` (at least the fileprivate seam init(s) present) — else abort. Record each S9-negcompile diagnostic verbatim. `S9-negcompile-a/b/c` + `S9-grep`.
- **S10** (full): full command `ec==0`. `S10-full`.
- **S11** (release+census): release-census block → 0 each, record `relSHA`; **then RESET the transient lockfile as the terminal step: `git checkout -- Package.resolved`. This reset is PERMITTED and REQUIRED — `Package.resolved` is a build-generated lockfile, NOT a scope file; the "no git write" constraint covers only the two SCOPE files, staging, and commits, and explicitly EXEMPTS this lockfile reset. S11 evidence MUST record `Package.resolved` CLEAN at the terminal (`git status --porcelain Package.resolved` empty) — a dirty record is a §7 abort.** `S11-census`.
- **S12** (candidate commit for the final gate): after CTO review of S1–S11 evidence, CTO FIRST updates `STATUS.md` to the correct transition — **Gate 3: PASS · Gate 4 (TDD impl): complete (evidence IDs S1–S11) · CEO exact-head final: PENDING** — so the committed STATUS is NOT the stale "Gate 3 In Review / Gate 4 Blocked". THEN CTO creates the candidate commit (the six approved files: ADR, PRD, T1, STATUS + the two code files) and records `candidateSHA`. CTO then verifies the candidate tree: (i) **parent is the exact base** — `test "$(git rev-parse ${candidateSHA}^)" = ab799fa3bef40a1338eed9544742489239b21215`; (ii) **changed-path EXACT-SET match (not `grep -vFx`, which passes when an approved file is MISSING — must catch both extras AND omissions):** `diff <(git diff-tree --no-commit-id --name-only -r "$candidateSHA" | sort) <(printf '%s\n' 'docs/adr/ADR-014-independent-midi-event-readback.md' 'docs/prd/PRD-issue-302-independent-midi-event-readback.md' 'docs/tickets/issue-302-r1-independence-guard/T1-strict-verify-guard.md' 'docs/tickets/issue-302-r1-independence-guard/STATUS.md' 'Sources/LogicProMCP/MIDIReadback/MIDINoteIndependentVerification.swift' 'Tests/LogicProMCPTests/MIDIIndependentVerificationTests.swift' | sort)` MUST be empty — the changed set EQUALS the six approved paths exactly (any extra `>` or missing `<` line → abort). Wrong parent → abort. Then, IN ORDER at `candidateSHA` (a real commit, not the dirty worktree): focused → full → release+census (record `relSHA`) → no-positive/public/caller checks (**the public-API check here MUST be commit-relative — `git diff "${candidateSHA}^" "$candidateSHA" -- Sources/LogicProMCP/MIDIReadback/MIDINoteIndependentVerification.swift` — because a plain `git diff --` on the clean checked-out candidate is empty and would false-PASS**) → CTO review → grok BOOMER-6 → PR CI → CEO exact-head final. **Stale-evidence invalidation (hard rule):** if `candidateSHA` changes at all (any amend/re-commit/rebase), EVERY item in that chain (focused/full/release/relSHA/checks/CTO/BOOMER-6/PR-CI) is INVALIDATED and MUST be re-run in the same order at the new SHA. A CEO exact-head final PASS is valid ONLY for the exact `candidateSHA` whose entire chain was re-run at that SHA. No merge until CEO exact-head PASS at the current `candidateSHA`.

## 5. AC-1 = S9 + release census; AC-7 = §3 public/caller + census + this LIVE_NA. 6. LIVE gate = NA
Runtime-independent (no Logic/AX/CoreMIDI); 0 public API; 0 production caller. LIVE_NA (satisfies AC-7's no-production-path) approved iff review record shows: (i) §3 public-API + caller checks empty, (ii) release census 0 with `relSHA`, (iii) explicit CTO/CEO `LIVE_NA` ack. R2 carries the live gate.

## 7. Abort / escalation + evidence
Abort+escalate if: a §0 precondition false (incl. dirty scope or missing Gate-3 PASS); an expected-RED runs `ec==0`; a mutation needs an out-of-scope file; wrong test fails; scope-delta shows a non-scope file; census≠0; unrelated full-suite failure; base SHA moves. **Evidence run isolation (step-gate):** evidence is written ONLY to a UNIQUE run dir keyed to the approval receipt — `RUN=~/.openclaw/workspace/reviews/302-r1-evidence/run-<receipt.approval_ts>`. Before creating it the implementer MUST assert it does NOT already exist (`test ! -e "$RUN"` — a pre-existing dir means a prior run under this same approval; ABORT) then `mkdir "$RUN"`. Each artifact `"$RUN"/S{n}-{tag}.txt` records: command, `ec`, `base`, `scopeDiffSHA` (+ `relSHA`/`candidateSHA` where relevant), step, stdout/stderr excerpt. Evidence from any prior or pre-approval run (e.g. the quarantined `stale-preapproval-20260723/`) is VOID and MUST NOT be reused; each Gate-3-approved execution gets its own fresh, previously-nonexistent run dir.

## 8. Review checklist (by artifact ID)
- [ ] §0 preconditions; baseline manifest; scope-delta = only the two files (+reset Package.resolved)
- [ ] `S1-red`+`S2-compile-red` (distinct scopeDiffSHA) before `S3-green`; `S3-grep0` (no positive path, 0 public, 0 caller)
- [ ] compilable behavioral mutation-RED for every guard: `S5-mut-red`,`S6-red`,`S7-*-red`,`S7ppq-*-red`,`S8-mut-red` (each RED/GREEN differs in scopeDiffSHA; no out-of-scope edits)
- [ ] `S9-negcompile-a/b/c` (exact diagnostics) + `S9-grep`
- [ ] `S10-full` `ec==0` · `S11-census` 0 each + `relSHA` · Package.resolved reset
- [ ] §2 AC numbers match PRD criteria 1–7 exactly; Decision-B vocab cleaned in S3/S4 (fixture-not-proof; no "positive grant control"/"feature flag off"/`e0Proven`; guard reason strings UNCHANGED)
- [ ] `S12`: STATUS.md set to Gate3 PASS/Gate4 complete/final PENDING BEFORE commit; parent==base; changed-path EXACT-SET == the six approved files; candidateSHA created; final focused/full/release re-run at candidateSHA (public-API check commit-relative) for CTO review + grok BOOMER-6 + CEO exact-head; no merge before CEO PASS
