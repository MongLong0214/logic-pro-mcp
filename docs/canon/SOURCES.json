{
  "schema": 1,
  "note": "The Logic assets this repository treats as source of truth, and what each can answer. A source is listed here only after something measured what it contains; the counts are from Scripts/logic_canon.py build against the Logic in MANIFEST.json. Every count here is printed by `Scripts/logic_canon.py census`; do not retype one.",
  "sources": {
    "quickhelp": {
      "path": "Contents/Resources/<locale>.lproj/QuickHelp.plist",
      "answers": "the identity of a CONTROL, as a key that is the same in every locale",
      "fields": [
        "Title",
        "Text",
        "Anchor",
        "composed"
      ],
      "keys": 9835,
      "locale_names": 10,
      "distinct_files": 7,
      "distinct_files_note": "en, it, pt and zh_TW ship the same bytes (sha256 3ed7aa22...), so it, pt and zh_TW are untranslated English. A claim of agreement 'across ten locales' counts three agreements that are true by construction.",
      "reaches_live_axhelp": "measured: a composition of Title and Text is the SUFFIX of a live AXHelp value on 86 of 114 distinct ko-KR readings",
      "limits": [
        "3766 of 9835 ko keys share their composition with another key, so a string identifies a SET of keys and not always one",
        "the ja and zh_CN join rule is unverified and their punctuation argues against it -- see LOCALE_JOIN_CONFIDENCE in Scripts/logic_canon.py",
        "93 of 9835 keys have ever been confirmed live across every census this repository holds"
      ],
      "corpus_files": 20,
      "corpus_files_note": "QuickHelp.plist and QuickHelpDefault.plist in each of ten .lproj. The key and distinct-file counts below describe QuickHelp.plist only; QuickHelpDefault.plist is distinct in all ten locales and is counted in `entries`.",
      "entries": 390820
    },
    "strings": {
      "path": "**/<locale>.lproj/*.strings",
      "answers": "the text of a LABEL, which is weaker than a control identity because labels are reused",
      "fields": [
        "value"
      ],
      "files": 2538,
      "entries": 605190,
      "locale_names": 10,
      "reaches_live_axhelp": "measured: 27 of the 28 live ko-KR AXHelp values QuickHelp cannot explain resolve here, all of them in Logic.framework and none in any other framework",
      "limits": [
        "English is structurally absent from 97 of 135 tables that back a live match: en text lives inside Base.lproj nibs and only translated locales get a .strings overlay. A resolver keyed on <locale>.lproj/<name>.strings silently loses en.",
        "across the nine non-English locales, 135 measured tables showed zero key-set drift",
        "a value may be present in the table and NOT used by the interface: the live UI shows untranslated 'German' and 'MIDI Region' although ko translations for both keys exist",
        "The AXHelp resolver reads three Logic.framework tables, not all 2,538 files, so 'none in any other framework' is true of what it looked at. Checked separately against every ko .lproj table in the bundle and it still held, but the code could not have found otherwise."
      ]
    },
    "madsp": {
      "path": "Contents/Frameworks/MADSP.framework/Versions/A/Resources/<plug-in>.plist",
      "answers": "a stock plug-in's parameter table: parameterID, name, control type",
      "fields": [
        "name",
        "preferredControlType"
      ],
      "files": 128,
      "entries": 14940,
      "locale_names": 1,
      "limits": [
        "not localised: the parameter names are fixed in the plist and the ten Localizable.strings beside them hold 23 error and side-chain strings with no parameter names in them",
        "parameterID is an ordinal private to each plug-in, not a global identifier, and 57 of the 124 tables that emit one have gaps in it. An earlier revision said 55 of 127, a figure nothing in the tree derives; `Scripts/logic_canon.py census` prints what does.",
        "nothing measured connects a parameterID to an element in the live accessibility tree"
      ]
    },
    "nib": {
      "path": "**/*.nib (NSIBUserDefinedRuntimeAttributesConnector)",
      "answers": "which SCREEN FILE holds a control, by the qhid runtime attribute",
      "fields": [
        "nibs"
      ],
      "files": 1169,
      "entries": 3313,
      "locale_names": 1,
      "limits": [
        "619 distinct qhid values reach 587 QuickHelp keys, which is 6.0% of 9835; 32 point at keys QuickHelp does not have",
        "coverage is concentrated in Preferences and Song Settings (PFS 93%, SET 89%) and is zero for TRA, ARR, SPE, SEQ, LLP, CSS, MIX and INS -- the main window",
        "zero coverage of the 6056 numeric-triple and other keys: plug-in editors are not built from nibs",
        "only 1 of the 86 live QuickHelp-matched controls carried a qhid, so this is a static asset link and not a live control identifier"
      ]
    },
    "nibstrings": {
      "path": "**/Base.lproj/*.nib",
      "answers": "the ENGLISH text of a LABEL, at the same (unit, key) its translations use",
      "fields": [
        "value"
      ],
      "files": 162,
      "entries": 6270,
      "locale_names": 1,
      "why": "Apple uses base internationalization: the English is compiled into `Base.lproj/<table>.nib` and only the translated locales get a `<locale>.lproj/<table>.strings` overlay. Measured: of the 162 Base.lproj nibs that carry labels, ZERO have an `en.lproj/<table>.strings` beside them, so `strings` had no English for any of them and an absence claim over English was answered by a corpus that had never held it.",
      "how": "Read from the archive's CLASSES, not guessed from what the strings look like: Interface Builder emits the English as an `NSLocalizableString` object and the `.strings` key for it as the NEXT object, an `NSString`. 6,270 pairs from 162 nibs with zero objects that did not follow the rule; a shape the reader cannot pair stops the build rather than becoming an absence.",
      "joins_strings": "measured: against each of the nine translated locales, 6,188 of the overlay's 6,216 keys are also in the nib and NOT ONE key is nib-only; 146 of 159 tables are key-identical. So the unit here is the overlay's own address and English lands in the same table as its translations. `TRANSLATION_NAMESPACE` in Scripts/logic_canon.py is what makes the join happen when deriving which English Apple translates.",
      "limits": [
        "The 28 keys the overlay has and the nib does not are Interface Builder's numeric placeholders (`255`, `100`, `30.0`, `1`) and two literal `<PLACEHOLDER STRING: DO NOT LOCALIZE>` -- text held for a field the running code fills in, not a label Apple shows.",
        "Three of the 162 nibs -- MAGraphPanel, NoteRepeater and PlugInManager, all in MAToolKitHighLevel.framework -- have no `.strings` overlay in any locale. Their unit names a table that does not exist on disk, which is the address the overlay WOULD use; the English is real and the screen is not translated.",
        "Base.lproj ONLY. The other 1,007 nibs -- 60 per locale in the old fully-localized style, including `en.lproj`, and 407 in no `.lproj` at all, 312 of them MAPlugInGUI plug-in screens -- hold zero `NSLocalizableString` objects, so this extractor yields nothing from them. `niblabels` reads them, by the (class, key) sites AppKit draws as text. Issue #902.",
        "Presence is not use, the same as for `strings`: a label compiled into a nib may belong to a screen the running code never shows."
      ]
    },
    "niblabels": {
      "path": "**/*.nib outside Base.lproj",
      "answers": "the text of a LABEL in a nib Apple does not base-internationalise: an old-style nib translated as a whole in each `.lproj`, or a nib in no `.lproj` that is one copy for every language",
      "fields": [
        "value"
      ],
      "files": 1007,
      "entries": 8992,
      "locale_names": 11,
      "why": "Measured over the live reading `docs/observations/2026-09-25-977-de-DE-plugin-editor-is-not-a-blocking-modal.json`: a German Logic's Compressor editor shows nine AXButton titles -- `-12 dB`, `Meter` and the seven circuit names `Vintage\u2028Opto` through `Platinum\u2028Digital` -- that were absent from every one of the 24 corpora before this source and are present in `niblabels/-`. They are cells of `MAPlugInGUI.framework/.../GCompressorViewController.nib`, which sits in no `.lproj`. So an AX reading of a plug-in screen can now be cited to Apple's bytes. It closes 0 of the 85 policy literals classified `nowhere`: none of them is an exact value here.",
      "how": "A string is read only at a (class, key) whose meaning AppKit defines as displayed text: NSTextFieldCell.NSContents and .NSPlaceholderString, NSButtonCell.NSContents and .NSAlternateContents, NSMenuItem.NSTitle, NSTableHeaderCell.NSContents, NSTableColumn.NSHeaderToolTip, NSWindowTemplate.NSWindowTitle, NSTabViewItem.NSLabel, NSSegmentItem.NSSegmentItemLabel, a tool-tip NSIBHelpConnector's NSMarker, an NSNibAXAttributeConnector's AXDescription or AXHelp value, and the `accessibilityLabel` runtime attribute. An NSClassSwapper is read as its NSOriginalClassName. `NIB_LABEL_SITES` in Scripts/logic_canon.py holds the list with each site's meaning.",
      "joins_translations": "The unit is the nib with its `.lproj` taken out and the key is the object's path from the archive root -- which key of which parent, and which occurrence -- so a translated copy meets its `en.lproj` twin. The object INDEX cannot do this: 20 of the 60 old-style nibs have a different object sequence in at least one locale. Measured: 623 addresses carry a translated-locale row; 613 carry all ten locales, 7 English addresses miss one locale and 3 translated rows have no English twin. Of the 620 English addresses, 467 are translated and 153 ship the English in every locale. `translated.en.u32` grows from 68,417 to 68,548.",
      "excluded": {
        "identifiers_and_other_non_label_sites": "402,921 strings at 300 (class, key) sites that are not in the list are dropped, among them NSNibOutletConnector.NSLabel 17,313, NSTableColumn.NSIdentifier 1,266, NSPopUpButtonCell.NSContents 506 (the selected item's title, read at its NSMenuItem), NSTabViewItem.NSIdentifier 330, NSMenu.NSTitle 259 and NSTokenAttachmentCell.NSContents 102 (format tokens such as `$LN`)",
        "runtime_attributes_other_than_accessibilityLabel": "42,305 values, among them `text` 862 and `labelString` 816. They are properties of `MA*` classes this repository has no definition of, so the class cannot say what they mean",
        "ib_default": "810 strings equal to the title Interface Builder gives a new object at that site (`Item 1`, `Button`, `Text Cell`, `Window` on a window template)",
        "translation_of_an_excluded_english_string": "2,493 translated rows whose `en.lproj` twin was excluded. `Item 2` is `Objekt 2` in de.lproj and nothing in the German says it is a default",
        "no_letter": "333 strings with no letter in them (`0`, `-12`, `1234`): a readout's placeholder",
        "do_not_localize": "43 strings carrying Apple's own DO NOT LOCALIZE marker"
      },
      "limits": [
        "Only 68 of the 312 MAPlugInGUI nibs in no `.lproj` carry a label at these sites; with the one old-style GarageBand channel EQ they give 1,116 rows. The other plug-in screens set their text through `text` and `labelString` runtime attributes, which are not read. So `THRESHOLD`, which a Korean Logic showed as a plug-in's text field (docs/observations/2026-09-04-native-view-sliders-are-anonymous-and-names-live-on-text-fields.json), is still absent from every corpus although a nib holds it as a `text` attribute. `OUTPUT GAIN` from the same reading is in no corpus and in no string of a non-base nib.",
        "The Interface Builder default list is the object library's own titles and nothing more. Placeholders a designer typed -- `Header Text`, `LowNote Text`, `Text here` -- are in the corpus, because nothing in the class separates them from a label.",
        "A nib in no `.lproj` is locale `-`. That it is shown untranslated on every locale is measured for the Compressor on de-DE only.",
        "Presence is not use, the same as for `strings`: `Loop` is a segment label in MAContentTaggingUI's STORMMainWindow.nib in zh_CN, which makes `docs/locale/ui-labels.json` read two labels' zh-CN coverage as `derived` although nothing has shown that window is reachable."
      ]
    }
  },
  "not_canonical": {
    "help_book": "Contents/Info.plist declares CFBundleHelpBookFolder=logic.help and that folder is on no disk here. Anchor resolves only over the network, at support.apple.com/guide/logicpro/<anchor>/mac, so an Anchor-to-screen mapping is not a local canonical fact.",
    "content_databases": "ContentDatabaseV01.db, index.db-fts and PackageContents.dbz are a content catalogue, a search index and an install manifest. Their label columns are empty (ZLABEL 0 of 533) or absent, and they carry no interface strings.",
    "keymap_momd": "MAKeymap.framework's Keymap_CD.momd is the Sampler's zone and group model, not key commands. The name invites the opposite reading."
  }
}
