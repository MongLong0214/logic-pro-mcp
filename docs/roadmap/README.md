# Roadmap — source of truth

**This file's table records which issues are open and what each is waiting on. It does not set
order.** Scheduling for the ADR programme is [#308](https://github.com/MongLong0214/logic-pro-mcp/issues/308)
(START HERE), and each owning issue's body is that work's implementation contract (#308 §1). Where
this file and an issue disagree, the issue is right — the last section of this file says the same.

The structure it replaces — `roadmap-v10-r*-CONTROLLING-*.md` in `~/.hermes/controlling/`, with its
promotion receipt and append-only ledger — is **non-authoritative as of 2026-08-24**. Those files
are left in place deliberately. Deleting them would take the reason for this change with them:
the controlling copy was last modified 2026-08-10, its ledger could not be found, and the in-repo
mirror opened by disclaiming itself, so **both copies were stale and neither could be corrected
without the other**. That is the failure this file exists to end.

`roadmap-2026-08-10.md` was retained beside this file, superseded, until 2026-09-05. It is gone
now — a second roadmap in the tree is the thing this file exists to end, and the reason it
existed is written above rather than left in a file nobody reads. Git has it if it is wanted.

## The update rule

**When an issue opens or closes, the same PR updates the table below.** A roadmap that is updated
"when someone remembers" is stale within two weeks — measured, that is exactly how its predecessor
got here.

That rule is a convention, and a convention is a claim. **The check now exists**:
`Scripts/roadmap-table-matches-github.py` runs in CI and fails when this table and GitHub disagree —
either a row that was never flipped, or an open issue nobody added. It refuses (exit 2) rather than
reporting clean when it cannot tell: a `gh issue list` result that hit its own `--limit` looks
identical to a complete one, and written against `--limit 200` this check would have compared
against the newest 200 of 239 issues and passed. `Scripts/test-roadmap-table-guard.sh` drives every
one of those paths and asserts the exit codes, so the guard has been watched fail.

One exception, and it is narrow: **a pull request may mark closed the issues it says it closes.**
Without it a closing PR has no truthful row to write — open while it is open, stale the instant it
merges — and #684 hit exactly that, turning `main` red on its own merge commit. The exemption
applies only to issues named in that PR's `Closes #N`, only in the ahead direction, and not at all
on a push to `main`; a row claiming an open issue is closed still fails, and the closing PR must
still list the issue rather than delete its row.

It does not check the prose. The "waiting on" column, the order, and the state block below are still
claims dated by the "measured" line, and only issue numbers and open/closed are mechanised.

## State — measured 2026-09-07 at `a70df168`

```
v3.15.0 published, 207 commits since · ADR-019 landed · de-DE joined the locale campaign (#876) · 14 open issues · 6 of 7 LPMCP-PRD-001 debts open, now tracked by #815/#816/#373 rather than asserted by CI and written down nowhere
```

| ADR | issue | state | what it is waiting on |
|---|---|---|---|
| ADR-001 | #284 | OPEN | **CORRECTION (#956): the removal described next was reversed.** The owner reopened #284 on 2026-09-14 and that decision supersedes the retirement; #926 (`5519079e`) restored the runner, transport, contracts/promotion and CLI on 2026-09-19, and restoration alone did not wire independent enforcement or publish evidence (#284 §2). #373 owns implementation, #290 the atlas evidence adapter and #816 publication. The 2026-09-13 reading, kept as history: The subject was removed on 2026-09-13. The qualification CLI (`--qualify`, `--verify-promotion`), the attestation/waiver types, the `trusted-verifier` product, the managed fixtures and the release-workflow gates that consumed them are all gone; `release.yml` carries no qualification step and `Package.resolved` dropped the sixteen transitive dependencies the verifier pulled in. The row that stood here described a critical path — #373 produces a bundle, #816 publishes it, #815 makes the gates unconditional — through machinery that no longer exists. A future release gate is new work with its own contracts, not a continuation of this. |
| ADR-002 | #285 | closed | |
| ADR-003 | #286 | closed | |
| ADR-004 | #287 | closed | |
| ADR-005 | #288 | closed | |
| ADR-006 | #289 | closed | shipped #674/#675, measured and closed |
| ADR-007 | #290 | OPEN | closed 2026-08-30 with all seven criteria met and measured (criterion 1 was narrowed to Desktop by the owner — Creator left product scope on 2026-07-17, so the line predated the decision), and **reopened 2026-09-21 by the owner** for one remaining release-integration obligation, not for an atlas rewrite: the qualification subsystem was removed and restored, and at `d067e919` `AtlasQualification.caseFor` exists but its documented invocations do not arm the diff and its emitted case carries `evidenceFiles: []`, so "release qualification automatically includes the atlas diff" is not currently unconditional. The shipped production selectors stand |
| ADR-008 | #291 | OPEN | **Selected 2026-09-22 (#308 §3, #291 r5):** R0 resource bootstrap, R1 typed graph, R2 physical and existing-bus outputs, R3 sends/input/removal, as the routing owner for the session-repair epics #965–#971; R4 sidechain-source mutation remains later scope. Earlier findings: node identity was decided and shipped in PR #740, and the destination read that blocked every edge is fixed. **Node identity works** — reading `logic://tracks` issues `trk_` references and nodes go 0 → 3. **The destination read was never unimplemented**: the model has carried `input` and `output` since it was written, the readers were wired, and `outputSlotHelpKeyword` / `inputSlotHelpKeyword` carried `variants: []`, so `output slot` and `input slot` were the only strings either could match. With the ko-KR renderings measured 2026-09-04 the same project goes `with_input` 0 → 1 and `with_output` 0 → 3, and both `#291` harnesses run clean (13/13 and 9/9) — they could not run at all before, for three reasons of their own filed as #767. Ruled out on the way rather than assumed: the depth-4 search radius (4 of 12 layout items reach an output slot, and depth 8 finds no more) and Korean normalisation (the live help is NFC). What remains open is the rest of the graph — sends name no destination in either language, which is a property of the surface, and the edge half beyond slots. **The WRITE side is now measured and it is a wall**: `AXPress` on the output and send slots of the inspector strip returns **0 — success — and changes nothing** (description, value, child counts, and the application's menu titles identical before and after, every run), so a caller trusting the return code believes the edge was opened. The negative is not the instrument's: the same signature in the same run moves the mute button `off` -> `on` -> `off`. The Group and Audio Effect slots behave identically, so the wall is not specific to routing. Record `2026-09-09-the-routing-slots-answer-a-press-with-success-and-do-nothing`, surface `inspector.channel_strip`. NOT measured: the MIXER's own routing controls (a different surface), any action other than `AXPress`, and whether an assigned send names its destination — no send exists in this fixture and the probe cannot create one **MEASURED 2026-09-11 — the slots are NOT a wall, and the write side is reachable.** The 2026-09-09 record said these slots 'answer a press with success and do nothing'; it watched the SLOT'S OWN VALUE, which an `AXPress` never changes, because the press OPENS A MENU. Watching for `AXMenu` instead, with a track-header Mute moving `0->1->0` as the run's positive control: Output slot `0 -> 11` menus, Send slot `0 -> 13`, Group slot `0 -> 1`, Audio Effect slot `0 -> 172` (the plug-in insertion menu). And the choices are NAMEABLE, which `popupNotAddressableByName` had made doubtful: 273 distinct titles among 274 enabled Output-slot items, versus the Controls-view case that produced that refusal, where three enabled items all read `-12 dB`. The full write then ran end to end — `Stereo Output` -> `Output 3-4` selected BY TITLE, slot reads back the new destination, Logic's own undo stack names it `Change Output in Channel Strip` — and the reverify script undoes it and confirms the restore by re-reading rather than by trusting osascript. **A first attempt looked non-reproducible and that was my test value, not the surface**: routing to `No Output` leaves the strip with no menu to open, measured as `[11,11,11]` open cycles before, `[0,0,0]` after selecting `No Output`, `[11,11,11]` after selecting `Output 3-4`, with no recovery at 30s. Superseded record + probe + mutating reverify land with this. NOT closed: `Stereo Output` appears twice in the Output menu so an implementation must refuse or disambiguate; only the Output slot was actuated, Send and Group were read only; en-US only, so titles must go through `AXLocalePolicy`; and none of this is wired into the product yet |
| ADR-009 | #292 | OPEN | **Controls view cannot reach this ADR's own acceptance criterion, and that is now measured rather than pending.** The criterion is 3 stock plug-ins or 10 parameters; `set_param_verified` supports one, Threshold, through the Controls-view CHECKBOX path. Both routes to widen it are walls: sliders (`sliderNotActuable`, 2026-09-02 — AXValue set and AXIncrement both report success and the slider does not move) and popups (2026-09-04 — `AXShowMenu` opens the menu, but `Circuit Type`/`Distortion`/`Mode` expose only the current choice plus one untitled disabled item, and `Auto Gain` exposes three enabled items ALL titled `-12 dB`, so no selection can be requested by name; polled to settle, not read once). Reading is unaffected — every popup reports its label and current value. What this needs is a decision on what the ADR promises, not more effort against the same surface. **The one route that could still have made the criterion reachable was tried on 2026-09-10 and failed**: no slider in the Compressor's native editor declares `AXTitleUIElement` (0 of 22; DMD 0 of 7; positive control present), so the native view offers no non-positional pairing either. With Controls-view sliders and popups already walls, **no surface remains on which 3 plug-ins or 10 parameters is reachable**. The decision narrows from three options to two — amend the criterion to the control shapes that support verified write, or close as platform-limited and keep the read side — and both leave the same code shipping **MEASURED 2026-09-14 — the acceptance criterion IS reached, and the sentence above is about the wrong surface.** Everything said above is true of the Controls view, and the criterion was never restricted to it: ADR-009 asks for 3 stock plug-ins **or 10 parameters** with independent write/readback evidence, and ADR-013 shipped `set_eq_band_verified`, which writes a Channel EQ band control and reads it back before answering. Driven live against one Channel EQ: **18 of 24 named band parameters reach State A**, and the six that refuse are exactly the two Cut bands on all three of their parameters. On 2026-09-13 those 18 were deliberately NOT promoted — a one-way State A is not what this catalog's own gate means by verified, which requires a measured transition and its reverse. The reciprocal sweep ran on 2026-09-14: **18 of 18 reciprocal, 18 of 18 idempotent**, 72 writes all State A with the readback equal to the request, and the idempotent rewrite walking 0 steps and verifying anyway. The 18 are `.verified` in `StockPluginCatalog` now, each carrying its own `observed_transition=<from>-><to>` pair in both directions. One gate defect was found on the way and fixed: `hasVerifiedParameterWriteObservation` named ONE operation, `set_param_verified`, so it refused evidence that named the operation ADR-013 shipped — the accepted set is derived from `OperationRegistry` now (plug-in op, `readbackRequired`, carries `value` and `unit`, which is what excludes `insert_verified`). Record `2026-09-14-eighteen-channel-eq-parameters-move-both-ways-and-stay-put`. NOT established: WHY the Cut bands refuse; the extremes of each declared range (ADR-009 asks for min/max separately and this sweep drove mid-range pairs only); a second Channel EQ instance, another locale, or another machine; and whether a value survives a window close or a project reload. The other three acceptance criteria — independent readback on every public parameter, wrong track/slot/plugin causing no write, closed/open window starts — are NOT re-measured here. **The other three acceptance criteria were then measured too, 2026-09-14, and two of them hold outright.** Criterion 2 (independent readback on every public parameter): 0 of the 29 catalog parameters claiming `verified` or `observed` lack a readback method, read off `logic://stock-plugins` on the release binary. Criterion 4 (closed and open window starts): a closed start opens the editor, writes and closes it; an open start writes and leaves the window as it found it — both State A, and **Logic's own window list agrees**, not only the response's lifecycle fields. Criterion 3 (a wrong track/slot/plugin causes no write) holds for every WRONG selector — insert with no Channel EQ, a ref resolving to nothing, another track's ref, and a project path that is not the open project all refuse pre-write with a typed error and `write_attempted: false`, the parameter is unchanged, and the wrong-insert refusal repeated three times left the surface usable each time. **It does not hold for a bare index, because a bare index is not refused at all**: `set_eq_band_verified` accepts `track: 0` with no `target_ref` and writes, while `insert_verified` refuses the same shape with `index_binding_corroboration_required`. That asymmetry is REPORTED here, not fixed — it is a public contract difference between two operations in one tool. Record `2026-09-14-the-other-three-adr-009-criteria-measured-against-the-eq-path`. Also NOT built, and deliberately not claimed: the ADR's 'Public surface' section — `list_verified_capabilities`, `get_param_verified`, `apply_snapshot_verified`, `diff_snapshot`, `logic://stock-plugins/{plugin}/verified-capabilities` — none of which the acceptance criteria name. **The bare-index gap was then chased to its reason, and it is a decision rather than an oversight.** `OperationRegistry` pins `set_eq_band_verified` and `set_param_verified` at `.legacyIndexAllowed` deliberately: `IndexBindingCensusTests` census (c) names this one a 'reversible named-EQ parameter write', the tier's rule is that every member 'earns its place only by being reversible — a wrong-target write here is recoverable by the user', and the set is the ADR-002 done-bar's issue-linked waiver under **#401 (CLOSED)**. `insert_verified` is `.corroborated` because an insert is irreversible-class. So criterion 3 holds for every write the registry treats as unrecoverable, and the gap is confined to writes a user can undo. What remains is a tension in ADR-009's OWN WORDING — 'wrong track/slot/plugin causes no write' does not distinguish reversible from irreversible, while the registry does — and resolving it is an ADR edit, not a code change. Flipping the tier would be a breaking contract change this repository has already weighed and decided against. |
| ADR-010 | #293 | closed | **Closed 2026-09-21 as not planned: a transfer to #302, not completed MIDI reading (#308 §6, #293 title).** Earlier findings: the collector reads correctly and a release build cannot enter it. Re-measured 2026-09-04 against a region written by `record_sequence`: both notes came back with the right pitch, velocity, channel and position, so the reading half is settled. The closed half is sharper than "the public provider" — `collect` takes a `RegistryResolvedIdentityProof`, and the only construction of that type sits inside `#if QUALIFICATION_FAULT_SEAM`, which `Package.swift` scopes to debug. The shipped binary exports no mint, so nothing can build the argument; nothing registers the provider either. `--probe-event-list` walks the same read path from the hashed artifact for observation only. It could not START outside English until #712 fixed the `"Event"` tab literal; `Status` is localised too (`노트`), and no literal comparison on it exists today **MEASURED 2026-09-14 — the seam is not the blocker, and the row above names the wrong thing.** The seam facts are right: `RegistryResolvedIdentityProof`'s init is fileprivate, there is no `Decodable`, and the only mint is under `#if FAULT_TEST_SEAM`, which `Package.swift` scopes to debug. What the rows do not say is WHY no production mint was written. `ResolvedRegionIdentity` is `(name, ordinal, startTick)`, and chasing each field to a live surface: name and ordinal both come out of the region enumeration that already ships. **`startTick` does not exist anywhere production can read it.** The arrange's region help names BARS (`parseRegionBars` returns `(startBar, endBar)`). The transport was driven live rather than read off `currentPlayheadBar`'s comment — which claims `"Bar.Beat.Division.Tick"` — and Logic answered differently: `goto_position` asked for `4.1.1.1`, observed `4.1`, `observed_position_components: ["bar", "beat"]`, State B `readback_unavailable`. **Two components, not four.** Tick resolution DOES exist, in the Event List's Position column (`1 1 4 203`, matched to within 0.8 tick on 2026-09-13) — but that is an EVENT's position, a different quantity from a region's start. So the work here is not plumbing a mint past a seam; it is deciding what the tick field is for — keep it and find a region-start reading, or replace it with something the surface reports and name it honestly. Record `2026-09-14-the-seam-is-not-the-blocker-a-region-start-tick-is-not-readable`. NOT measured: whether Logic's transport display has a mode that shows ticks and whether the product could select it coordinate-free — that is an open route, not a wall; and the OBSERVED side (`ObservedRegionIdentityProof.proven`) was not chased field by field. **THEN MEASURED FURTHER, SAME DAY — the open route was walked and it is open.** The reading above said tick resolution does not exist on the transport and named the display mode as an unmeasured route rather than a wall. It is a route: the control bar's display-mode popup offers `비트` / Beats, selectable BY MENU-ITEM TITLE with no coordinate, and in that mode the Playhead Position group holds **four** named sliders — `마디`, `비트`, `디비전`, `틱` — with the group's own AXValueDescription reading `4 마디 1 비트 1 디비전 1 틱 `. **The product was reading two of the four and discarding the rest**: `extractTransportState` matched only `barSliderLabel` and `beatSliderLabel`, under a comment asserting "the Control Bar exposes bar and beat independently" — true of the default mode and false of the surface. Taught the other two labels, `goto_position` answers **State A** observing all four (`6.2.3.120` requested, `6.2.3.120` observed), and restoring the original mode returns it to two components and State B, measured in both directions. Record `2026-09-14-the-transport-shows-ticks-when-the-display-mode-does`. What this does NOT close: it is the PLAYHEAD's position, not a REGION's start — reading a region start this way needs the playhead moved to it and that move verified, which was not done. Korean only; the division and tick label sets carry measured `디비전`/`틱` and English canonicals read off no English Logic, with no ja-JP or de-DE variant. And the product does not SELECT the mode — it reports whatever the mode exposes and says so in `observed_position_components`. And the region half was TRIED, 2026-09-14, and stopped on a different thing: `탐색 > 이동 > 선택 범위 시작점` (Navigate > Go To > Selection Start) exists as a NAMED menu item and clicking it succeeds, but there is **no public operation that selects a region**. `region.select_last` is an internal channel arm with no `OperationRegistry` row, and the only region-shaped public operation is `project.get_regions`, which reads. So with nothing selected the menu item parks the playhead at `1.1.1.1` and the reading says nothing about any region — measured on three tracks, all three answering `1.1.1.1`. The region-start route is therefore UNPROVEN, not refuted, and what it is waiting on is a public region selection. **Closed 2026-09-21 by the owner during the SSOT reconciliation, as an execution issue superseded by #302, which explicitly owns every remaining implementation and qualification requirement. Closing the duplicate queue waives none of them, and it is not a claim that MIDI readback shipped.** |
| ADR-011 | #299 | OPEN | behind #292, and the 22-sliders-and-1-name census is now explained rather than restated. Native editor, measured 2026-08-30: 22 sliders (11 settable) on one Compressor instance and 20 (10 settable) on another, naming exactly one parameter (`Threshold`). Read attribute by attribute 2026-09-04, the other 21 are not anonymous by accident — every slider is described `슬라이더` and carries only a number, while the parameter NAMES are the value of separate `AXTextField`s (`THRESHOLD`, `MIX`, `OUTPUT GAIN`) and several settings live in button titles (`ON`, `0 dB`, `AUTO`). So every value and every name is readable and nothing in the elements says which belongs to which; pairing them means pairing by AX order or position, which this repository refuses everywhere. **That is a wall for verified writes, not a gap in the census.** Writes HAVE been attempted on the Controls view and both non-checkbox roles are walls too: AXSlider value and increment each reported success on 2026-09-02 and moved nothing, and the popups open but expose only the current choice or identically-titled options. The checkbox path is the one that works. **The last untried pairing route is now measured and it is also a wall.** `AXTitleUIElement` — the accessibility API's own control-to-label association, and the one mechanism that would have made pairing non-positional — is declared by **0 of the Compressor's 22 sliders** (2026-09-10, native editor opened coordinate-free through the mixer slot's own named `open` button). Drum Machine Designer: 0 of 7. A positive control in the same run shows one element in the tree DOES declare the attribute, so the zeros are about the sliders rather than a blind probe. The wall statement is therefore no longer 'this repository refuses positional pairing' but 'the platform's association mechanism is absent here'. NOT swept: `AXLinkedUIElement` and `AXServesAsTitleForUIElements`, two plug-ins only, one host, one locale **PARTIAL 2026-09-14 — one of the two unswept attributes questions is answered for a Logic plug-in window, and NOT for the Compressor.** This row records `AXLinkedUIElement` and `AXServesAsTitleForUIElements` as never asked. Asked now, against the open Channel EQ editor by walking every descendant and reading its attribute NAME list (a failed read and an undeclared attribute are different answers, and only the name list separates them): **0 of 70 elements declare either one**, sliders included. Consistent with the 2026-09-10 `AXTitleUIElement` zeros — the platform's control-to-label association mechanisms appear simply absent from Logic's plug-in views. **The Compressor itself was NOT reached**, and four routes refused, each for a reason worth keeping: the #306 harness answers `ambiguous` because Absolute Zero holds TWO Compressors (inserts 0 and 2) and will not choose; the four tracks with exactly one Compressor are all named `Studio Grand`, so a strip lookup by track name is ambiguous too; `set_param_verified` against the Compressor's own `plugin_insert_ref` refuses `plugin_window_plugin_mismatch` while the Channel EQ editor is open; and that editor's close button is not a direct child of its window. Every one of those is a component refusing an ambiguous target, which is the wanted behaviour. Record `2026-09-14-logics-plugin-window-declares-neither-ax-association-attribute`. The half that matters most is still open: the Compressor's parameter NAMES live in text fields, and whether any of those declares `AXServesAsTitleForUIElements` is exactly what was not measured. **MEASURED 2026-09-14 — ADR-018 did NOT ship nothing, and the 'four modules, zero external references' line counts FILENAMES.** Two of its four names declare no symbol of that name: `HostParameterGate.swift` declares `enum HostWriteRejection` and `HostParameterPlane.swift` declares `enum PluginParameterProvider`, so grepping for either filename finds zero everywhere and reads as deadness. The directory holds **five** files, not four, and the missing fifth is the one that matters: **`ControlsViewBooleanParameterWriter` is on the live write path** — `locate`, `prepareView` and `pressAndVerify` each have an external caller in `AccessibilityChannel+VerifiedPlugins.swift`, and its `LocatorFailure` strings are what the `sliderNotActuable` / `popupNotAddressableByName` refusals quote. That is the Controls-view checkbox writer this roadmap elsewhere calls "the path that works", and it is what lets `set_param_verified` reach Compressor threshold at all. Counted by symbol: four of the five have zero external references, the fifth ships. Record `2026-09-14-adr-018-did-ship-one-module-and-the-row-counts-filenames`. A reference count is not a behaviour measurement — it says the code is reached, not that it works. |
| ADR-012 | #300 | closed | promoted and closed 2026-08-28; all seven acceptance criteria measured, four artifacts found and fixed on the way (#693-#697) |
| ADR-013 | #301 | closed | closed again 2026-09-21 08:27Z, hours after the same day's reopen, while its body still lists unchecked scope rows -- read the row as GitHub's state and the rows as the remainder, not as work this table claims is finished. **That remainder is #301's own.** Its body names E1 (the exact-instance eight-band state reader) and E2 (finishing the existing named scalar writer against E1's before/after reads) as scope rows of THIS ADR, and its own acceptance list says the final receipt must map every row to implementation **or** to #955's explicitly retained remainder -- which is the deferred batch #725 did not discharge: snapshot apply, compensation and verified inverse, and the Automatic-EQ recommendation (#955 B4, stated there as *not* a dependency of E1/E2). #308 owns scheduling and no scope at all. So the closed state and the unchecked rows disagree, and that disagreement is what this row reports; transferring E1/E2 to #955 would resolve it by moving the work rather than by finishing or reopening it. Shipped 2026-08-31 — `plugins.set_eq_band_verified` registers all 24 named band parameters and reaches State A live. The recorded wall was not where it was said to be: Channel EQ's native view exposes 8 named band enables and 26 `AXSlider`s, and the opacity claim came from a census that instantiates the AU *outside* Logic, which ADR-018's Important Boundary says cannot speak for the live instance. The write is a walk, not a set — the sliders expose only `AXIncrement`/`AXDecrement`, and a direct `AXUIElementSetAttributeValue` returns success while landing a different value. Original wording: Measured 2026-08-30, the recorded wall is not where it was said to be: Channel EQ's native view exposes 8 named band enables and 26 `AXSlider`s, including all 24 named band parameters; every slider reports `settable=yes` and carries `AXValueDescription` in Hz/dB. **No write was attempted, and AX settability lies** — what is established is that the controls are addressable and readable. The opacity claim came from a census that instantiates the AU *outside* Logic, which ADR-018's Important Boundary says cannot speak for the live instance. The ADR's C4 Decision was revised on the issue the same day **Reopened 2026-09-21 by the owner: a real single-parameter feature shipped, the whole ADR did not.** The remaining scope is truthful same-instance EQ state readback and the bounded single-operation contract, with `logic_plugins.set_eq_band_verified` preserved; the earlier C4 setup/codec/port/doctor instructions are superseded and must not be implemented. #308 owns scheduling. |
| ADR-014 | #302 | OPEN | R1 shipped. The 2026-09-04 re-measurement of the live Event List was CORRECT and correctly transcribed — `AXColumns` present, status 0, **6** columns named `L`, `M`, `위치`, `이름`, `트랙`, `길이` — but what it was taken to be ABOUT was wrong. Those six are `regionLevelHeaderColumns` verbatim, and `AXLocalePolicy` labels three of them `Region-level` in their own rationale: the reading was taken while the Event List showed REGIONS, a state the collector already classifies as `paneAtRegionLevel`, "a recoverable navigation failure, not a column-layout drift". The note level this ADR reads has **eight** columns (`L, M, 위치, 상태, 채널, 번호, 값, 길이/정보`, measured on a Korean Logic 12.3), and identity for them is ALREADY bound through `AXLocalePolicy` label sets — `readHeaders` refuses when sort buttons do not cover every header child, matches each title against a label set rather than an English literal, and throws `headerMismatch` naming the canonical column and what Logic rendered. Indices are used only after the order has been proven at every index. A column-identity ticket was drafted on the misread and WITHDRAWN before any code (`docs/tickets/issue-302-r2-column-identity/`). R2 is the POSITIVE GRANT R1 deferred — `verifyRegion` answering a match rather than only rejecting — and its blocker is the release-build seam shared with ADR-010 and ADR-015, not columns **MEASURED 2026-09-14 — the seam is not the blocker, and the row above names the wrong thing.** The seam facts are right: `RegistryResolvedIdentityProof`'s init is fileprivate, there is no `Decodable`, and the only mint is under `#if FAULT_TEST_SEAM`, which `Package.swift` scopes to debug. What the rows do not say is WHY no production mint was written. `ResolvedRegionIdentity` is `(name, ordinal, startTick)`, and chasing each field to a live surface: name and ordinal both come out of the region enumeration that already ships. **`startTick` does not exist anywhere production can read it.** The arrange's region help names BARS (`parseRegionBars` returns `(startBar, endBar)`). The transport was driven live rather than read off `currentPlayheadBar`'s comment — which claims `"Bar.Beat.Division.Tick"` — and Logic answered differently: `goto_position` asked for `4.1.1.1`, observed `4.1`, `observed_position_components: ["bar", "beat"]`, State B `readback_unavailable`. **Two components, not four.** Tick resolution DOES exist, in the Event List's Position column (`1 1 4 203`, matched to within 0.8 tick on 2026-09-13) — but that is an EVENT's position, a different quantity from a region's start. So the work here is not plumbing a mint past a seam; it is deciding what the tick field is for — keep it and find a region-start reading, or replace it with something the surface reports and name it honestly. Record `2026-09-14-the-seam-is-not-the-blocker-a-region-start-tick-is-not-readable`. NOT measured: whether Logic's transport display has a mode that shows ticks and whether the product could select it coordinate-free — that is an open route, not a wall; and the OBSERVED side (`ObservedRegionIdentityProof.proven`) was not chased field by field. **THEN MEASURED FURTHER, SAME DAY — the open route was walked and it is open.** The reading above said tick resolution does not exist on the transport and named the display mode as an unmeasured route rather than a wall. It is a route: the control bar's display-mode popup offers `비트` / Beats, selectable BY MENU-ITEM TITLE with no coordinate, and in that mode the Playhead Position group holds **four** named sliders — `마디`, `비트`, `디비전`, `틱` — with the group's own AXValueDescription reading `4 마디 1 비트 1 디비전 1 틱 `. **The product was reading two of the four and discarding the rest**: `extractTransportState` matched only `barSliderLabel` and `beatSliderLabel`, under a comment asserting "the Control Bar exposes bar and beat independently" — true of the default mode and false of the surface. Taught the other two labels, `goto_position` answers **State A** observing all four (`6.2.3.120` requested, `6.2.3.120` observed), and restoring the original mode returns it to two components and State B, measured in both directions. Record `2026-09-14-the-transport-shows-ticks-when-the-display-mode-does`. What this does NOT close: it is the PLAYHEAD's position, not a REGION's start — reading a region start this way needs the playhead moved to it and that move verified, which was not done. Korean only; the division and tick label sets carry measured `디비전`/`틱` and English canonicals read off no English Logic, with no ja-JP or de-DE variant. And the product does not SELECT the mode — it reports whatever the mode exposes and says so in `observed_position_components`. And the region half was TRIED, 2026-09-14, and stopped on a different thing: `탐색 > 이동 > 선택 범위 시작점` (Navigate > Go To > Selection Start) exists as a NAMED menu item and clicking it succeeds, but there is **no public operation that selects a region**. `region.select_last` is an internal channel arm with no `OperationRegistry` row, and the only region-shaped public operation is `project.get_regions`, which reads. So with nothing selected the menu item parks the playhead at `1.1.1.1` and the reading says nothing about any region — measured on three tracks, all three answering `1.1.1.1`. The region-start route is therefore UNPROVEN, not refuted, and what it is waiting on is a public region selection. |
| ADR-015 | #303 | OPEN | behind #293. The stated blocker was CHECKED rather than taken on trust and it holds: `IndependentExpectedSeam` sits inside `#if QUALIFICATION_FAULT_SEAM` (lines 59-121), `Package.swift` scopes that define to `.debug`, and the shipped release binary carries **zero** seam symbols — so `independentPayload` is always nil there and `verifyRegion` can only answer `incompleteCannotVerify`. A positive note match is structurally impossible in what ships. The three verification modules exist; the gap is the live ingestion boundary in front of them, which is R2 **MEASURED 2026-09-14 — the seam is not the blocker, and the row above names the wrong thing.** The seam facts are right: `RegistryResolvedIdentityProof`'s init is fileprivate, there is no `Decodable`, and the only mint is under `#if FAULT_TEST_SEAM`, which `Package.swift` scopes to debug. What the rows do not say is WHY no production mint was written. `ResolvedRegionIdentity` is `(name, ordinal, startTick)`, and chasing each field to a live surface: name and ordinal both come out of the region enumeration that already ships. **`startTick` does not exist anywhere production can read it.** The arrange's region help names BARS (`parseRegionBars` returns `(startBar, endBar)`). The transport was driven live rather than read off `currentPlayheadBar`'s comment — which claims `"Bar.Beat.Division.Tick"` — and Logic answered differently: `goto_position` asked for `4.1.1.1`, observed `4.1`, `observed_position_components: ["bar", "beat"]`, State B `readback_unavailable`. **Two components, not four.** Tick resolution DOES exist, in the Event List's Position column (`1 1 4 203`, matched to within 0.8 tick on 2026-09-13) — but that is an EVENT's position, a different quantity from a region's start. So the work here is not plumbing a mint past a seam; it is deciding what the tick field is for — keep it and find a region-start reading, or replace it with something the surface reports and name it honestly. Record `2026-09-14-the-seam-is-not-the-blocker-a-region-start-tick-is-not-readable`. NOT measured: whether Logic's transport display has a mode that shows ticks and whether the product could select it coordinate-free — that is an open route, not a wall; and the OBSERVED side (`ObservedRegionIdentityProof.proven`) was not chased field by field. **THEN MEASURED FURTHER, SAME DAY — the open route was walked and it is open.** The reading above said tick resolution does not exist on the transport and named the display mode as an unmeasured route rather than a wall. It is a route: the control bar's display-mode popup offers `비트` / Beats, selectable BY MENU-ITEM TITLE with no coordinate, and in that mode the Playhead Position group holds **four** named sliders — `마디`, `비트`, `디비전`, `틱` — with the group's own AXValueDescription reading `4 마디 1 비트 1 디비전 1 틱 `. **The product was reading two of the four and discarding the rest**: `extractTransportState` matched only `barSliderLabel` and `beatSliderLabel`, under a comment asserting "the Control Bar exposes bar and beat independently" — true of the default mode and false of the surface. Taught the other two labels, `goto_position` answers **State A** observing all four (`6.2.3.120` requested, `6.2.3.120` observed), and restoring the original mode returns it to two components and State B, measured in both directions. Record `2026-09-14-the-transport-shows-ticks-when-the-display-mode-does`. What this does NOT close: it is the PLAYHEAD's position, not a REGION's start — reading a region start this way needs the playhead moved to it and that move verified, which was not done. Korean only; the division and tick label sets carry measured `디비전`/`틱` and English canonicals read off no English Logic, with no ja-JP or de-DE variant. And the product does not SELECT the mode — it reports whatever the mode exposes and says so in `observed_position_components`. And the region half was TRIED, 2026-09-14, and stopped on a different thing: `탐색 > 이동 > 선택 범위 시작점` (Navigate > Go To > Selection Start) exists as a NAMED menu item and clicking it succeeds, but there is **no public operation that selects a region**. `region.select_last` is an internal channel arm with no `OperationRegistry` row, and the only region-shaped public operation is `project.get_regions`, which reads. So with nothing selected the menu item parks the playhead at `1.1.1.1` and the reading says nothing about any region — measured on three tracks, all three answering `1.1.1.1`. The region-start route is therefore UNPROVEN, not refuted, and what it is waiting on is a public region selection. |
| ADR-016 | #304 | OPEN | the tempo map is read, and written by converging on the target rather than assuming a write lands. Bounded in three places, each measured rather than assumed: an attempt budget of 64, a wall-clock deadline, and a rollback that converges back to the initial tempo under the same deadline. Two CI-only failures came from that third clock being unpinned while a test declared which bound it measured — the deadline override now reaches the rollback. `supportedTempoRange` is integer BPM 5...999. Still open: sub-bar tempo events and Smart Tempo's analysis modes are untouched **MEASURED 2026-09-14 — 'sub-bar tempo events are untouched' is no longer the state.** The placement, the readback and the restore each have a live reading. The playhead was driven to `5.3.2.60` by the product's own `goto_position`, answering **State A** with all four components (which needed today's transport fix — the reader was taking two of the four the Playhead Position group exposes). The List Editors' Tempo pane carries a named button, `새로운 템포 이벤트 생성`, located by AXDescription under an exactly-one-match rule; pressing it took the table from **one row to two**, and the new row's first four cells read **5, 3, 2, 60** — exactly the four components requested. `logic_edit.undo` took it back to one row, confirmed by RE-READING the table rather than by trusting the undo's own answer. And the two surfaces are independent: with the control bar restored to `비트 및 프로젝트` the transport reports two components while the Tempo list still reports four, so the list's resolution does not depend on the display mode. Record `2026-09-14-a-tempo-event-lands-on-a-sub-bar-position-and-the-list-reads-it`. **This is reachability, NOT a feature**: no public operation creates a tempo event, the button was pressed by a probe, and `TempoMapSnapshot` is still referenced only inside `Sources/LogicProMCP/SmartTempo/` — nothing in the product builds one from this table. Also NOT measured: a tempo event carrying a DIFFERENT bpm (the new row inherited 120), what a second event does to positions after it, whether a created event's bpm can be written and verified, and whether any delete path exists besides undo. Korean only — `새로운 템포 이벤트 생성` has no label set yet. Smart Tempo's analysis modes remain untouched. Two more things came out of that run and one of them is not a finding yet. **`logic_edit.undo` answered State C `element_not_found` six times and then State A once the arrange window was raised — and reading that as a WINDOW precondition was WRONG. Retracted.** Re-measured with no modal present, undo works with the PLUG-IN window frontmost (State B, tempo 118 -> 120). The six failures were the stuck `경고` alert; raising the arrange window merely coincided with clearing it. What stands: while that alert was up, a created tempo event survived the pane's own `삭제` button (AX code 0, no effect), `편집 > 삭제`, and a real Delete keypress with the row AX-selected and Logic frontmost: **no delete path other than undo has been found**, so a created tempo event must not be assumed reversible. And a `set_tempo` answering **State A with `observed: 144` while neither tempo row moved** was seen ONCE and could NOT be reproduced — the second run answered State C `unsupported_state` under different conditions. A false State A is the worst class this repository has, so it is recorded as an unreproduced sighting needing a controlled re-measurement and is NOT asserted as a defect. The project was left as found, single row at `1 1 1 1` @ 120, confirmed by re-reading. **THE UNREPRODUCED STATE A REPRODUCED UNDER CONTROL, AND IT WAS A FALSE STATE A.** Same call, one variable — how many tempo events the project holds. With ONE, `set_tempo 126` answers State A and the Tempo list moves to 126. With TWO, `set_tempo 144` answered **State A, `success: true`, `verified: true`, `observed: 144`** while **both rows stayed at 120**, and a single-button modal `경고` was on screen reading *여러 개의 템포 이벤트가 발견되었습니다! 템포를 더 편집하려면 템포 목록 편집기를 사용하십시오.* — Logic refused the edit and said so. Cause: every success branch verified by reading the SAME tempo field it had just typed into, and a field holding the typed text cannot tell a refusal from a success. FIXED: the operation reads Logic's modal state before and after the write and refuses with State C `ax_write_failed` when a blocking modal appeared that was NOT there before — the before-reading is what stops someone else's stuck dialog being blamed on this write, and both halves are pinned by tests that were mutation-checked (disabling the gate turns the refusal test red with exactly the defect's signature). Live-verified in both directions. That alert also explains the rest of the session's failures on this surface: it wedged `undo` (State C `element_not_found`, six attempts), the audio-import panel (no panel in 78 s of polling) and three separate tempo-row delete paths, all of which recovered the moment its `확인` was clicked. Record `2026-09-14-set-tempo-reported-a-verified-write-logic-had-refused-with-a-dialog`. NOT swept: any OTHER operation whose readback is of the control it wrote has the same exposure, and this is one site rather than a census. The exposure SHAPE was then counted rather than left as a sentence: **21 of the 40 `encodeStateA` sites under `Sources/LogicProMCP/Channels` sit in the 10 of 13 files that never read Logic's modal state**, and `mixer.set_volume` declares `verify_source: "ax_slider"` — it reads back the fader it wrote, the same shape that made this defect. That is a count of where the shape COULD occur, NOT a list of defects: the refusal here is specific to a control-bar tempo edit on a project with more than one tempo event, and no other operation was driven to see whether Logic ever answers it with a dialog. A blind review then took the FIRST version of that fix apart, and each objection held. It refused when the classified modal KIND differed before and after the write, and: `ModalReconciliation.classify` answers `.none` for an **unreadable** read — every signal it takes defaults to false — so **the original false State A survived every failed after-read**; a kind change (`strayMenu` -> `informationalAlert`) is not evidence this write caused anything; and one informational alert replaced by another compares equal and slips through. It also found the geometry-free direct AX write returning State B with no modal reading at all, and Escape being pressed before any scan — Escape can clear the very alert that is the evidence. **The claim is narrowed to one the function can support: State A requires a COMPLETE modal read that is clean.** A blocker present at all, whoever raised it, means the field cannot be read as the tempo; an incomplete read means we do not know; both refuse. The before-reading survives only as the annotation `blocker_present_before_write` and is not load-bearing. Both halves are mutation-checked: disabling the completeness check turns the unreadable-scan test red with `State A verified:true`, disabling the presence check turns the alert test red the same way. The deliberate cost: a stuck dialog now makes every tempo write refuse until it is cleared, and the annotation says which problem to fix. A second review round then confirmed all three of those findings CLOSED and found one more, which was also right: **State C `ax_write_failed` asserts the write itself did not succeed, and that is not what was observed** — what was observed is that the write was attempted and the outcome cannot be read. It answers `readback_lost_after_write` now; State C is kept because fail-closed matters more here than the failed/unverified distinction (State B asserts `success: true`, the more dangerous overclaim), and the error code carries the distinction. The same round caught `blocker_present_before_write` being computed as `refusalLabel != nil`, which is ALSO true when the before-scan merely failed to read — it reported a sighting nobody made; it is `complete && kind != .none` now, beside a `blocker_scan_before_write` of `complete`/`unreadable`. And it found that **none of the three tests protected the pre-Escape refusal**: removing it left all three green. A fourth test now clears the alert from inside the fixture's Escape, so only an observation taken before it can see anything, and both new fixes are mutation-checked. NOT measured, and named by the reviewer: a modal published AFTER the single post-write scan, or one that disappears during it. A third round closed the two lesser findings and left one standing plus one new site. The standing one: the refusal HINT argued the outcome was "unknown rather than known to have failed" while riding a **State C** envelope whose own contract says the write did not land — the hint no longer argues with the state it rides on. The new site: **the geometry-free direct write still returned State B, and State B asserts `success: true`** — the same overclaim as the original defect, one branch over; it refuses on the same terms now, and restoring the claim turns its new test red with exactly `state: B, success: true`. Noted and not resolved: the contract's three states fit none of this cleanly — State A needs confirmation, B asserts the write landed, C says it did not — so an attempted write whose outcome cannot be read is reported State C, fail-closed, and the reviewer's objection that State C is stronger than the evidence stands on the record. A fourth round closed R3-01 and found the direct-write branch **discarding both AX results** — `setAttribute` reports whether the write was accepted and `performAction` whether the confirm was, and ignoring both meant answering State B `success: true` after AX had said no. That failure IS establishable, so it reports `ax_write_failed` and `safe_to_retry: true`, distinct from the blocked-readback refusal where nothing can be established. The same round caught the routing assertion (`count == 1` stayed green for a different singleton such as `[.mcu]`; it asserts `== [.accessibility]` now) and a comment left stale by the previous round. Both new checks mutation-verified. **Still open, both timing and both named rather than measured:** a blocker seen before the write that vanishes before the post-write scan, and a modal published after that single scan. |
| ADR-017 | #305 | OPEN | NOT STARTED rather than blocked, and measured 2026-09-04 to be the same shape as ADR-015: the modules exist and nothing can reach them. `FlexPitchModel` / `FlexPitchEditGate` / `FlexPitchRefValidity` are pure logic with **one** reference outside their own directory — the feature flag's own declaration — and the flag's only reader is a test asserting it is off. So no path produces a `FlexPitchSnapshot` from a live region, and the note-identity-stability measurement this ADR needs cannot be made through the product. The correction stands: Flex Pitch is in the Audio Track Editor, so the AU-parameter-view wall was inherited, not observed. What is still unmeasured is Logic's side — an AX walk of that editor over a Flex-analysed region |
| ADR-018 | #306 | OPEN | the write that 2026-08-30 said had not been attempted was attempted 2026-09-04, and Tier B does not survive it: Controls-view popups open but expose only the current choice, and `Auto Gain` exposes three enabled items all titled `-12 dB`, so no selection is addressable by name — see ADR-009. Addressable stays weaker than viable, and for popups it is now measured NOT viable; the checkbox path is the one that works. Separately, and shared with ADR-015 and ADR-017: this ADR's four modules (`PluginCapabilityManifest`, `HostParameterGate`, `HostParameterPlane`, `ControlsViewParameterEnumerator`) each count **zero** references outside their own directory, and `adr018HostParams`'s only reader is a test asserting it is off. The third-party plug-ins this ADR is named after remain unmeasured **MEASURED 2026-09-14 — ADR-018 did NOT ship nothing, and the 'four modules, zero external references' line counts FILENAMES.** Two of its four names declare no symbol of that name: `HostParameterGate.swift` declares `enum HostWriteRejection` and `HostParameterPlane.swift` declares `enum PluginParameterProvider`, so grepping for either filename finds zero everywhere and reads as deadness. The directory holds **five** files, not four, and the missing fifth is the one that matters: **`ControlsViewBooleanParameterWriter` is on the live write path** — `locate`, `prepareView` and `pressAndVerify` each have an external caller in `AccessibilityChannel+VerifiedPlugins.swift`, and its `LocatorFailure` strings are what the `sliderNotActuable` / `popupNotAddressableByName` refusals quote. That is the Controls-view checkbox writer this roadmap elsewhere calls "the path that works", and it is what lets `set_param_verified` reach Compressor threshold at all. Counted by symbol: four of the five have zero external references, the fifth ships. Record `2026-09-14-adr-018-did-ship-one-module-and-the-row-counts-filenames`. A reference count is not a behaviour measurement — it says the code is reached, not that it works. |

Also open, outside the ADR set:

| issue | state | what it is waiting on |
|---|---|---|
| #308 | OPEN | **The ADR programme's scheduling entry (START HERE).** It selects work and points to one owning issue body per implementation contract, and it stays open permanently and outside release and completion milestones (#308 §1, #956 §4). History: closed 2026-09-05 as superseded by the phases below, on the reasoning that a second copy of the order would drift; reopened 2026-09-21 by the owner, which moved order out of this file rather than into both. The phases below are now dated history (see *Order — the phases*). |
| #369 | closed | shipped #737 (`466dc20f`) — the drive works against a localized Logic. Seven live rounds found six independent defects, each fatal alone: English-only matching, the panel read ~1 s before it could exist, `AXURL` read as a String, the destination searching the wrong AX roles, navigation confirmed by a *change* when the panel reopens on the last destination, and a progress dialog titled `Logic<U+00A0>Pro`. Terminal State B is the design: three tracks named "Studio Grand" came out as `Studio Grand.aif`/`_1`/`_2`, names Logic assigns after the export |
| #373 | OPEN | **CORRECTION 2026-09-20: this row opened with "THE SUBJECT OF EVERYTHING BELOW WAS REMOVED ON 2026-09-13", and that stopped being true on 2026-09-19.** #926 (`5519079e`) restored the qualification subsystem: `Sources/LogicProMCP/Qualification/` holds `ProductionReadinessContracts.swift`, `PromotionGate.swift`, `QualificationRunner.swift`, `SemanticOracles.swift` and the rest, and `--qualify`/`--verify-promotion` are back in `MainEntrypoint.swift:66`. It also fixed the defect that made the old evidence unverifiable — a mutating operation's probe is required to have been REFUSED fail-closed while its record was filed under a shape rule demanding `operationIsError == false`, which is not satisfiable, so the verifier rejected every mutating pass while the runner reported it passed; a case now carries the SHA-256 of its record and the verifier checks that binding. WHAT IS STILL MISSING IS THE GATE, not the subject: the seven release steps all carried `if: vars.ADR001_QUALIFICATION_ENFORCED == 'true'` and that variable is set nowhere, deleting the `if:` makes every tag require a `QUALIFICATION_EVIDENCE_URL` secret that does not exist (release OFF, not gate ON), and the old pin is 7,522 lines behind and would reject all 21 Phase-A passes. Wiring it is its own change with a run URL as its acceptance. The Phase-A measurements below are kept and are still facts about the binary they were taken against. Historical record follows. Phase A IS ALREADY DONE and was invisible. Driven live 2026-09-07 against the release binary, **21 of 23** read-only operations report `passed` with `semanticReadback` — not the 15 this row carried, and not the zero the contract evaluator reports. The two that do not are both already understood: `tracks.list_library` is the failure pinned in `knownLiveGateFailures`, and `tracks.scan_plugin_presets` needs a plug-in window. All 90 mutating operations report `notQualified`/`typedDeferral` BY DESIGN — the runner probes them with parameters that must be refused and asserts `writeAttempted == false`, so a qualification run cannot modify a project, and adding a `.passed` branch would be moving the grader. So the remaining work for the read-only surface is NOT producing coverage; it is carrying coverage that exists into the release evidence bundle and into a form the static evaluator can read — that evaluator HAD no live `.passed` input at all and `waivers.json` holds zero entries, so its 113/113 measured its own blindness rather than the product's. The channel now exists: `ProductionReadinessContractEvaluator.evaluate` takes `liveCreditedOperationIDs`, produced only by `PromotionGate.liveCreditedOperationIDs(in:)` applying the release gate's own pass predicate to a real attestation, and defaulting to empty so a bare tree answers as it did before. So the 113/113 is still what this tree reports, but it is now a statement about missing evidence rather than about a missing input. NOT established: that the 21 live passes survive being carried through a bundle — no attestation has been fed to this parameter yet. NOT established: whether a different fixture moves any of the 21, and whether the transport's verdict is release-grade — a bundle also needs the raw transcript, the manifest entry and the signature **MEASURED 2026-09-14 — the subject is gone from the code, and now it is counted rather than asserted.** Grepping `Sources/` for each named type: `QualificationAttestation` 0 files, `ProductionReadinessContractEvaluator` 0, `PromotionGate` 0, `QualificationTransport` 0, `liveCreditedOperationIDs` 0. `QualificationCase` appears **once in the whole tree and it is a doc comment** — the type does not exist. That comment lived in `SelectorAtlas/AtlasQualification.swift`, describing a case it would emit into an attestation and a case manifest; it is corrected in this pass to say the run it was built to feed was removed with ADR-001, and that what survives is the pure decision plus its `--probe-atlas-diff` driver. So this issue's parent (#284) closed as retired and its machinery is measurably absent: what it needs before coverage is a SUBJECT, and the 21-of-23 live read-only passes recorded above remain true about the product's read surface either way. |
| #448 | closed | **Closed 2026-09-09 as not planned; a non-completion transfer, not delivery (#308 §3).** The shipped sort stays. The remainder moved: population and hierarchy to #965, naming to #968, arbitrary order and stacks to #969, colour to #970, plan and execution to #966/#971. The row as written before that transfer: `tracks.sort_verified` ships: it drives `Track > Sort Tracks by`, binds the verdict to the menu leaf actually pressed, and checks the effect against a complete caller-supplied order — so a sort by the wrong criterion cannot pass, and an already-sorted project stays State B because a no-op cannot prove the command ran. It refuses a collapsed stack (a scope it cannot enumerate) and a duplicate-name project (an identity it cannot key). Driven live 2026-09-03: 4 of 4 State A. Two things measured and NOT fixed here — Logic 12.3's `트랙 이름` leaf performs no operation at all while `악기 이름` and `생성일` work, all seven reading `AXEnabled = true`; and the settle witness is #757. Still open: anchor-based reorder is drag-only, and colour still needs a definition of verified for a write nothing reads back  MEASURED 2026-09-08: the colour half is not a design question. `AXUIElementCopyAttributeNames` over every element reachable from the main window — **91 distinct attribute names, 0 containing colour, 0 failed name calls, 0 empty answers — and 20 child lists the sweep could NOT read**, so twenty subtrees were never entered. The Mixer, revealed by pressing its own named button, added zero names. A track header declares nineteen attribute names in total and none could carry a colour. So the question moves from what `verified` should mean to whether any observable consequence exists — and none was found anywhere the sweep reached. The record is `partial`, not a wall: this is a census of attribute NAMES, a value could still be carried inside `AXValue` or spelled into `AXDescription`, and twenty subtrees refused to be read. Remaining routes are a different surface (project file, palette window) or an explicit refusal. Limits stated in the record: names not values, palette and dialogs unswept, depth 16 ||
| #678 | closed | the drift guard and this file's update rule shipped in #684 |
| #735 | closed | legacy `MIDIPacketList` traversal walked past a value copy and killed the server (SIGBUS), reachable by anyone sending to a published port. `MIDIPacket.data` imports as a fixed 256-byte tuple, so a value copy captures one packet's storage however many the list holds; both sites now walk the ORIGINAL pointer with `unsafeSequence()` and reject rather than truncate an oversized packet. `MIDIFeedback.parse` took the list BY VALUE, so no change inside its body could have fixed it — the signature takes a pointer now. Independently confirmed by #683's reporter: 5 of 5 crashes in isolation at 40 packets per list with the defect, 0 of 8 with the fix, and 2 of 3 in-server deaths on `LogicProMCP-MIDI-In`. The live-gate rule that could not express this proof is #754; the inbound destination nothing reads is #755 |
| #736 | closed | ROOT CAUSE FOUND 2026-09-08 and it is ours: **MCU feedback is decoded as if UMP words were a MIDI 1.0 byte stream.** Traced live with `MCU_TRACE=1` against a Logic whose Mackie surface is bound — 161 packets in, **8 events out**, all channel pressure and all misvalued. The port is created `._1_0` so CoreMIDI delivers UMP words, and the callback hands their little-endian memory image to `parseBytes`; a word sits in memory as `[data2, data1, status, 0x20|group]`, reversed. A four-byte packet puts the status at index 2 with one byte after it, so `case 0xE0:`'s two-data-byte guard DROPS every fader position, every CC and every note-on while one-data-byte channel pressure squeaks through. All 11 fader echoes discarded — that is `echo_timeout_500ms`, a decode failure rather than a missing message. It also explains `connected:true`: `handle` marks the connection on any event, so eight misvalued packets declare a fully broken decode alive. The test covering the path builds its fixture the way the code READS rather than the way CoreMIDI WRITES and says so in a comment, so it passes on input Logic never sends. TX is unaffected (legacy `MIDIPacketList` + `MIDIReceived`). Ticket: `docs/tickets/issue-736-mcu-ump-decode/`. Duplicate-port half measured absent on v3.16.0, single start and concurrent second instance. NOT established: that fixing the decode restores echo verification — zero packets followed our own fader write, and `MCUProtocol` answers no Host Connection Query. FIXED: the callback converts UMP words to MIDI 1.0 bytes before parsing, and the three fixtures that encoded the defect now build real UMP words. Live at the fix: 111 frames received, 0 starting with a data byte, first bytes 0x90/0xb0/0xd0/0xe0…0xe8. A review then found the converter carried the SAME class of defect one level up: it advanced one word past every message type it did not read, but Flex Data and UMP Stream are FOUR words each (`CoreMIDI/MIDIMessages.h`), so a continuation word whose top nibble reads `0x2` was decoded as a header and FABRICATED an event. It now strides by the UMP size table, skips a message whole, refuses a truncated tail, and counts WORDS rather than messages. Malformed input was being repaired rather than refused — a data byte with bit 7 set was masked into a valid-looking note-on, and the test PINNED that repair; both are reversed. The unconverted-word sink was never supplied by `FeedbackSink()`, so it reached nothing while its comment promised a health field; it is logged instead, and publishing it is a separate change. Still NOT established: that fixing the decode restores echo verification |
| #747 | closed | `project.save_as` was dead on a Korean Logic. Measured 2026-09-03: the menu item is `별도 저장…` not `다른 이름으로 저장…`; the panel is titled `저장` with radios `패키지`/`폴더`, all three compared against English literals; and Command-Shift-G opens no Go-to-Folder sheet on this build, while typing `/` opens it at once. Proven by a live save that landed the file on disk |
| #748 | closed | the title was wrong. Re-measured 2026-09-03: `logic://tracks` DOES read live, and `refresh_cache` closes the cold-cache window on the first call — the report caught the gap before the first poll. The real defect is that a collapsed track stack truncates the list while `readable` stays true: a 26-track project answered 3 rows, Logic numbering them 1, 25 and 26, and the 23 hidden tracks received no `track_ref`. `complete` is now a fact separate from `readable`, `verified_empty` requires it, and the truncated `logic://tracks/{index}` bounds say so instead of implying the project is that small. Reason code `collapsed_track_stack`, shared with #448's refusal |
| #749 | closed | a long chained-`+` string literal inside a `merging(...)` trailing closure timed out the Swift type-checker and blocked a contributor's build entirely. Two sites hoisted into a `let`; `Scripts/check-typechecker-heavy-literals.py` refuses a flat collection literal in the first unlabeled argument when one ordinary quoted-string value has 3+ concatenations. It does not cover labeled or non-first arguments, a later chain after a nested call, parenthesised pieces, or raw strings |
| #754 | closed | `is_clean` required a capture, a visual assertion and a recording of every run, so a change with no surface in Logic's UI could not earn its zeros honestly and #735's harness was rejected for having nothing to photograph. A document now DECLARES `surface="non_ui"` and earns those zeros with a counterexample control instead; an undeclared document is still judged as UI, because zero counters is also what a harness that did nothing looks like. Landed as two pull requests because `lpm-live-gate.sh` reads the rule from `origin/main`, so the rule cannot judge itself. STILL OPEN and tracked here: the gate has no way to accept a proof it did not generate — #683's reporter's 5-of-5 / 0-of-8 is stronger than the harness and remains unusable as evidence |
| #755 | closed | closed 2026-09-05. The server published `LogicProMCP-MIDI-In`, parsed everything sent to it and dropped it — `inboundMessages` had no production consumer, so a client could not tell "delivered and ignored" from "delivered and acted on". Unpublished until something reads it. `live_735_multi_packet_lists_arrive_intact.py` sent real packet lists to that endpoint and goes with it; falsifiable adoption stays at 8 because `live_755_no_inbound_destination_is_published.py` replaces it, asking CoreMIDI from a separate process and pairing the absence with a positive control — the source must still be there, or the zero is also satisfied by a server that never started. #735's traversal fix keeps its coverage in `testMIDIEngineReceivesEveryPacketAcrossMIDIPacketListCopyBoundary`, 64 packets across the value-copy boundary that crashed. |
| #757 | closed | the sort's settle witness took its first read with no delay and accepted the first pair of equal observations, so a sort landing slower than the 75 ms poll had its own PRE-sort order accepted as settled — measured 1 of 5 live drives, under load. Fixed by the fact that made it decidable: `execute` already refuses `beforeOrder == expectedOrder` as unobservable, so wherever State A is reachable the arrangement must move, and an unchanged order means 'not landed' rather than 'settled'. An expired budget now returns the unchanged order instead of `.unavailable`, because 'no observable change' is an answer and calling it unreadable would deny four successful reads |
| #742 | closed | closed 2026-09-05 as stale. It was a handoff naming branches that have since landed — `d6a2d0c9`, `8643d94f`, `ec8eb768`, `e8e40b16`, `27089818`, `14ae9c81`, `468fc906`, each verified present. Its other function, saying what is in flight, is the phases above. |
| #683 | closed | MECHANISM REPRODUCED 2026-09-08 on the shipped binary. stdout on a FIFO whose reader never reads, 39 `tools/list` past a 64KB buffer, then `sample`: **1539 of 1539 samples** in `writeAll` -> `Darwin.write` on `logic-pro-mcp.stdio.write`. The process stays alive and answers nothing. The queue is serial, so replies AND the 25s timeout envelopes queue behind one write with no deadline — which is why 'no response, ever, not even a timeout' is the signature rather than a puzzle. Ticket `docs/tickets/issue-683-bounded-stdout-write/` bounds the wait BEFORE the first byte (where failing is clean) and leaves the mid-frame case loud rather than fixed, because abandoning a part-written frame is #220's corruption by another route. My own first theory — a per-packet notification storm from MCU feedback — was checked and is NOT in the code: notifications publish from the poller's `postPoll`. NOT established: that this is the reporter's hang; a real host drains stdout and this reproduction stopped it deliberately. FIXED: the wait for stdout is bounded BEFORE the first byte of a frame and never inside one, so a stalled reader fails that one write loudly instead of parking the serial queue behind it; a closed peer and a slow reader are distinct outcomes rather than one blocking call. Live at the fix on the release binary: the write queue is not parked in `writeAll` after the deadline passes, the process is up, and a request sent after the stall is answered — read back as a complete JSON object by its id, because a substring match on the id is satisfied by the half-written reply a stalled writer produces. The reporter has not posted since 2026-09-04 and the owner decided on 2026-09-09 to close on the mechanism rather than wait: the defect is reproduced, repaired and proven on the shipped binary here, and what is NOT established is that the reporter's host is the same defect. Two reviews were needed to get here — the first found a deadline any signal could reset, a partial frame reported as success, and four test names that outran their assertions; the second found the deadline measured against the WALL clock, a watchdog that accumulated an uncancellable work item per frame, and a bound that did not bound because leaving a task group waits for its children |
| #724 | closed | |
| #726 | closed | |
| #685 | closed | fixed in this pull request — the nudge loop no longer abandons a write on one failed AX read |
| #766 | closed | `inferTrackType` answered `audio` for every track and now answers `unknown` when it cannot tell. The header aggregate carries identical type tokens on all 7 headers — the Input Monitoring help present on every one names an audio track and a software instrument track in the same sentence — so first-match-wins was constant and no ordering repairs it. Reading deeper was ruled out by measurement rather than left as an option: at depth 8 the three headers share 135 tokens and every unique one is the track's own name or a track-stack arrow. Live after the change: instrument, audio and drummer created back to back all report `unknown` with `requested_track_type` intact. `SemanticOracleTable` moved its per-op pin to `requested_track_type`, which really is per-op, and the four unit fixtures stopped carrying values live Logic cannot emit. Still open: where the type IS readable is the channel strip (an input slot marks audio, a MIDI effect slot marks an instrument), that read needs the Mixer revealed, and one of four strips measured was undetermined by the pair. FIXED, second half: the type is read from the inspector channel strip, which the header could not supply. Measured 2026-09-09 on Logic 12.3 (6674), en, one track per `create_*`: an `Input slot` marks audio, and no output/send/audio-effect slot beside `Assign control` rows marks external MIDI. The roadmap's other half was FALSE as a type reading — a DRUMMER strip is identical to a software instrument's, both carrying `MIDI Effect slot` and differing only in the plug-in loaded, so that case answers a FAMILY rather than a type and the create path publishes `track_type_verification_source: "inspector_channel_strip_instrument_family"`. Live at the fix: audio and external MIDI each name themselves, instrument and drummer both answer `unknown` with the family source. The strip is consulted only where it costs nothing — after `create_*` the new track is already selected — and `get_tracks` keeps the header aggregate, because enumerating would mean moving the selection. The three label sets ship with EMPTY variants: on an unmeasured locale nothing matches and the answer is the `unknown` the operation already returned, where a guessed translation could match a neighbouring control and answer confidently wrong |
| #767 | closed | AppleScript `entire contents` returns an empty list, without raising, for all ten applications it was tried on here — measured 2026-09-04 at 0 for a Logic window where a manual descent finds 464 elements, and 0 for nine other applications. Ten is what was tested, not every installed application. Evidence from 2026-08-23 has a harness clean, so it is a regression and the cause is still open. The `Sources` use is GONE: `region.select_last` verified with `lastRegionInfo` (startBar then trackIndex) while selecting with a screen-coordinate script, so the correct rule only ever reported that the other had missed — it now selects from the same enumeration, and the rectangle filter that matched every track header went with it. Selecting needed more than swapping the rule: `AXSelected` is settable, returns success, and is a TOGGLE (three writes of `true` give on, off, on), `AXPress` did not select from either start tried (2-selected and 0-selected), and the enclosing `AXLayoutArea` reports `AXSelectedChildren` settable=false — so the operation empties the selection with Logic's own Deselect All (addressed by the locale-free `AXIdentifier` `deselectAll:`, unique among the Edit menu's 151 items), then writes once against a pre-state it has read back, and reports State A only when the whole selection is that one region. `#291`'s harnesses are fixed and clean. One of the two documented AX-opacity conclusions has been re-measured and corrected: the Channel EQ editor exposes 26 named `AXSlider`s over 25 distinct names, 24 of them the eight bands' parameters (manual descent finds 70 elements in the same dialog where `entire contents` returns 0), and on the one slider that was written, `AXValue` is not a setter but a one-step nudge toward the target, so a value is reached by a bounded write-then-readback loop (the other 25 were read, not written). The CONTROLS-view conclusion is unsupported rather than corrected — the 29-row table that refutes the opacity framing was measured on Compressor, a different plug-in, so the Channel EQ's own Controls view has still not been looked at with a working instrument. The readable surface is per plug-in, not a host property: ChromaVerb and the Studio Grand instrument expose zero sliders in the same session, so the spike's description was wrong about the plug-in it was named after and right about its neighbours. A guard now refuses the phrase in any string literal, with no allowlist — every one of the eleven remaining occurrences — across six files under `Sources/` and `Scripts/`, outside the guard and its test — is prose about the defect |
| #774 | closed | `region.move_to_playhead` snapshots and re-reads with `selectedRegionInfo`, which returns the FIRST selected region, while Logic's Move to Playhead is documented to act on the selection — so two selected regions would give an envelope that names one. What Logic's menu command actually does to a multi-region selection has NOT been observed here; the singular readback is what was read, in the code. Same root cause #767 fixed for `region.select_last`, and unlike that one this is on a reachable path (`EditDispatcher` maps it). A CODE READING only: every live run had exactly one region selected, which the harness asserts as a precondition, so nothing measured bears on it. Two earlier claims here are WITHDRAWN — that no harness drives the operation (four `live_575_*` harnesses exist and `..._reachable.py` drives it) and that it moves nothing on this host (it moves: 10/10 checks, bar 1 to 9, independent witness agreeing; the failure was an open Mixer, whose Edit menu does not carry `이동 > 재생헤드로` at all). Separately, that harness cannot produce clean evidence right now: its visual band is a fixed 500x300 window at a constant offset and misses the moved region, so `visual_failed` is 1 with every check green. Closed 2026-09-07 by #805. The verifier read one region where the operation acts on the selection. Its live gate failure was cleared by a control run rather than by adjusting the check — same commit and binary, one variable — which is what #803 was opened for |
| #775 | closed | Reported: the tap Formula bumped `version` to 3.15.0 but kept the v3.14.0 `sha256`, so `brew install logic-pro-mcp` fails for everyone on the documented install path. Reproduced: the pinned hash is byte-identical to v3.14.0's universal tarball and v3.15.0 publishes a different one, both from each release's own `SHA256SUMS.txt`. Root cause is a missing check — `release-verify-formula-install-paths.sh` verifies this same file but only its install PATHS, so the one field that must move with `version` is the one nothing compared. Fixed, and `Scripts/ci-verify-formula-sha.sh` now asks the named release what it published on every build: it refuses rather than passes when it cannot ask, refuses a Formula carrying more than one hash rather than checking the first, and passes loudly on a version whose release is not tagged yet so a release-prep bump is not blocked |
| #776 | closed | closed 2026-09-05. `findRepoFile`'s source-relative fallback counted three `deletingLastPathComponent()` calls and landed on `<repo>/Sources`, probing a path that has never existed, so it could not fire at all. It walks up to the directory holding `Package.swift` now — a marker rather than a count, which is what made the bug silent. First test for that function. |
| #777 | closed | closed 2026-09-05, fixed by other work. The issue named a six-part `+` chain inside the dictionary literal passed to `merging` at `AccessibilityChannel+MIDIImport.swift:640`; `0a6f71f1` (landed for #749) hoists it into `let readbackHint` and the file now passes that binding in. **Not re-measured on Swift 6.3.3** — CI pins 6.2, so what is established is that the named cause is gone. |
| #780 | closed | The visual band was `(_CONTENTS[0] + 12, _CONTENTS[1], 500, 300)` — a constant offset and size derived from the canvas position and nothing else, so whether it contained the region it asserted about depended on zoom and scroll, which it never read. Measured 2026-09-05 on one run: the fixed band contains 39 of the region's 64 vertical points at both positions while the derived band contains all 64, so it was watching a fraction of its own subject even where it worked. Replaced by two rectangles — a canvas slice for settling, and the region's own measured frame before and after, unioned and padded — and `ax_region_select.swift` now emits whole frames in window coordinates and states which space it used. 11 of 11 checks with `visual_failed: 0`. NOT reproduced: the ko-KR run the issue reports, where both bands would have to be compared on that project |
| #778 | closed | Reported: on a Japanese Logic, 72 of 115 `AXLocalePolicy` label sets carry English and Korean forms but no Japanese one, so AX-backed reads fail. Same class as #768, which counts the sets whose `variants` list is empty; this one counts the sets that have variants but not that language. Not yet reproduced here. Closed 2026-09-07 by #808. Fourteen Japanese forms read out of the aligned censuses rather than translated; over half of the first eighteen candidates were the WRONG element and only vetted ones landed. The ja-JP unmeasured ceiling moved 122 to 113, and the five variants that sit on `arrange.window` carry no provenance because no ja-JP record covers that surface — recorded in the ratchet with the measurement |
| #773 | closed | Six livekit harnesses still select their window or menu by an English literal (`"Tracks" in titles`, `name ends with "Tracks"`, `name contains "Mixer"`, `menu bar item "Edit"`), which raises or finds nothing on a Logic whose arrange window is `트랙`. Not a regression: `check-livekit-ui-literals.py` already holds every site in its `KNOWN` ratchet, so new ones are refused and these are grandfathered. Split out of #767 because editing a file under `Scripts/livekit/` makes the ship gate require a passing live run of that harness, so six harnesses means six live runs with their own evidence. Closed 2026-09-07 by #802. The window test was a substring match, which is why three harnesses still could not run after the first pass: the selectors read from the locale table instead of English literals, and the ones that were already localized were measured rather than assumed |
| #792 | closed | A live harness can define a check and never call it, and nothing notices. Walking the AST of every `Scripts/livekit/*.py` finds two module-level `def`s in `live_*.py` that are referenced nowhere in their own file — `start_bar` in `live_519_region_op_on_a_localized_logic.py` and `playhead` in `live_534_goto_position.py`, the latter carrying `if False else None` under a docstring saying it reads live. `evidence.py`'s four are its public surface, called cross-file, which is why the test is whether anything imports the module rather than which directory it sits in. The third instance is what prompted the count: `same_region` was added to `live_575_move_to_playhead_reachable.py` to close a review finding, documented as the fix, and called from nowhere — a blind review of that commit approved it. Neither the ship gate nor CI can see this shape. A ratchet is the wrong instrument: a dead helper is a deletion, not a debt. The census and the command that reproduces it are in the issue. Closed by #806. A harness may not define a check it never calls. The guard counts NAME references, so a dictionary key spelling the same word does not keep a dead helper alive, and it checks its own entry-point premise instead of reasoning from it. Deliberately no ratchet: a dead helper is a deletion, not a debt |
| #793 | closed | The campaign proposer silently resurrects a retracted provenance claim. `markerListDeleteMenuItem` means the Marker List window's Delete; it had been backed by the ja-JP arrange-menus census, which walks the application MENU BAR, so the `削除` it saw was the Edit menu's. That was retracted 2026-09-05 with a dated ratchet entry. Running `locale-propose.py --apply` for two unrelated labels re-added the block verbatim and flipped `coverage['ja-JP']` back to `measured`; nothing failed, and the ratchet reported it as `1 item(s) closed` — inviting a tightened ceiling around a withdrawn claim. `path_contains` would prevent the match but lives INSIDE a provenance block, so it can only constrain evidence already written, and a retracted label has no block to hold it. `roles` is the only label-level filter and is too coarse: an `AXMenuItem` named `削除` is one wherever Logic puts it. Needs a label-level declaration of where evidence may come from, read by the proposer as well as the guard. NOT audited: whether other retractions have already been resurrected. Closed 2026-09-07 by #799. A retraction is a fact about the label, not an edit the next run undoes. Eight review rounds, and several later findings were introduced by earlier fixes — narrowing the rows made a false absence pass, and the over-correction made an honest absence unprovable, which the tri-state scope check resolved |
| #794 | closed | Live evidence can attest a binary to a commit it was not built from. `Scripts/livekit/evidence.py:1300` sets `built_from` from the WORKTREE's HEAD, not from the binary, and nothing compares the two — so a document reads `built_from: <head>, worktree_clean: true` while the artifact came from an earlier commit. Hit on 2026-09-06 by launching a harness before `swift build` finished: the run produced a document naming the new head, reported two red checks, and the code under test never ran. The `.evidence.N.json` rotation is the only reason the two runs are comparable afterwards. Cheapest fix is to refuse when any tracked `Sources/` file is newer than the binary's mtime; stronger is to embed the build commit and read it back |
| #795 | closed | 87 of 142 label sets have variants but no Japanese form, and a census gives STRINGS without the mapping from a label to its string — which is why #778 has moved one label at a time. The three censuses of 2026-09-05 walk the same surfaces in the same order, so stripping bracketed titles out of each row's `path` makes the skeletons comparable across locales: 1005 of 1031 rows match via `SequenceMatcher`, `arrange.menus` 617 of 617, `arrange.window` 388 of 422. Excluding the Apple menu (which carries the SYSTEM locale, not Logic's), 751 aligned pairs carry a string on both sides and 676 differ. The 34 unmatched rows all sit in the Library pane, whose tree differed between runs — 15 of them carry real strings, so that pane is a named hole in the method. Raw index alignment is unsafe: one inserted row shifts the tail and every later pair looks plausible while being wrong. Closed 2026-09-07 by #801. A label's translation is read out of two censuses of the same walk, matched positionally. Index alignment, unplaced pairs, disagreeing targets and base rows that merely CONTAIN the canonical are all refused rather than guessed |
| #796 | closed | The campaign project's track-header count moved 4 to 6 to 7 across 2026-09-06, read from `get_regions._debug` on runs bound to successive heads, and the cause is now measured: the headers are the live harnesses' own declared residue. `track.record_sequence` creates a track on every call by contract — `TrackDispatcher.swift` says so in the tool description — and six live harnesses invoke it once each. `live_572` records in every evidence document it writes that it leaves the imported track behind on purpose, judging a destructive delete unwarranted in a scratch document. The two candidates the issue named were both refuted by measurement rather than argued away: a before/after reading around a full 4427-test gate showed the count unchanged, and so did a quit / language-change / reopen / Save cycle — neither calls `record_sequence`. It cost one false red at `e18bcfe2`, where an enumeration check went red because 7 headers no longer fit the viewport where 6 had, and that coupling is fixed. NOT established: that no other route creates a track — the four `create_*` operations do so by definition and no harness was counted against them — and whether a growing fixture is safe for assertions not yet written |
| #797 | closed | A locked screen is invisible to the live harness, and the three AX paths the repository uses disagree about it. Measured 2026-09-07 with the screen locked and Logic running as pid 3574: the direct `AXUIElement` API reports 1 window titled `Logic Pro`, System Events reports 0 windows, `CGWindowList` reports 1 window with an empty name, `evidence.logic_window()` returns None, and `_production_ax_modal_signals()` raises `_ModalReadError('AX sheet descendant search reached depth 32')`. None of those says the screen is locked, so a run fails naming a recursion guard or an empty window list. On 2026-09-06 the System Events reading was taken at face value and Logic was force-restarted on it, which changed nothing. `CGSessionCopyCurrentDictionary()['CGSSessionScreenIsLocked']` is one call and exact; the harness preflight can refuse by name before any AX read. NOT established: whether a lock is what wedged the 34-minute gate that day |
| #798 | closed | The documented way to regenerate `docs/locale/ui-labels.json` reformats all 2858 of its lines without changing a value. `Scripts/locale_labels.py:408` and `:419` write `indent=2`, `locale-propose.py --apply` likewise, and the file in the tree is indented with one space and round-trips through `indent=1` byte for byte. `check-locale-labels-json.py` compares PARSED objects, so nothing in CI can see the drift and it survives indefinitely. The cost is review: a campaign adding two provenance blocks plus the regeneration the file itself asks for produces a 2858-line diff those two blocks are not findable in — measured 2026-09-07 while adding an 8-line field to two labels. Either direction closes it; what keeps it closed is a test that compares BYTES, since comparing parsed objects is what let it drift. Closed 2026-09-07 by #800. One renderer for ui-labels.json and a test that compares its BYTES, so the documented regeneration no longer rewrites 2858 lines without changing a value |
| #803 | closed | The live qualification gate's known-failure table assumes an English Logic, and when the assumption breaks the run cannot say so. `realReleaseBinaryExecutesEveryOperationWithIndependentReadback` required the observed failures to match `knownLiveGateFailures` EXACTLY and reported a BOOLEAN, so three red runs on three different trees all said the same unreadable thing and a branch that changed one unrelated file read as though it had broken qualification. Measured 2026-09-07, same commit and same binary with one variable: `origin/main` in ja-JP FAILS at `QualificationRunnerTests.swift:1598`, `origin/main` in en-US PASSES in 45.729s, and `fix/774` in ja-JP fails at the same line with none of its code implicated. The exactness is right and stays — `knownLiveGateFailureSetRejectsAKnownFailureStartingToPass` exists to catch a pin that quietly stops failing. What changes is that the two directions are separated and each prints its contents, and the message names Logic's own `AppleLanguages` rather than the host locale, which stayed `ko-KR` across every language switch this campaign made. NOT established: which operations move in ja-JP — that reading needs a Japanese run against the new message, and it is what will decide whether the table gains a locale condition or the repository gains issues for operations that only work in English |
| #804 | closed | A wall-clock assertion measured the CI runner, not the bounded refusal it named. `switchThatNeverChangesStructureRefusesWithinDeadlineAndLeavesEntryView` required `Date().timeIntervalSince(started) < 0.75` and failed THREE pull requests that changed no Swift at all, at 0.763, 0.771 and 0.778 seconds. What it was really asserting was that the `confirmationTimeout: 0.025` passed in is honoured rather than the writer's 3 s default, and `waitForView` re-censuses the window on every turn of its wait loop, so that count says the same thing without asking the machine: measured 2026-09-07, 13 censuses with the argument and 119 without it. Load makes such a loop turn FEWER times, so it cannot cross a ceiling. The audit the issue left open is done: three sites read the clock, all three are now counts of work — browser censuses for the two `waitForSegmentVisible` waits, and 3101 probe calls for the 100-folder traversal, which the neighbouring test already proved is 1 + 2n. Two got STRONGER in translation, catching a mutation in 1 ms that the elapsed-time form needed 120 ms to see. `Scripts/check-test-wall-clock-assertions.py` now refuses the spelling outright, with 17 self-test cases and no allowlist: a site that needs a clock changes the rule and says why. NOT closed: a clock hidden behind a helper of its own, or `mach_absolute_time`, would still pass — the rule reads text and says so |
| #815 | closed | **진단은 착지했고 남은 수용 기준은 #373 의 종료 조건으로 이관.** 이 이슈의 진단은 정확했다 — 일곱 스텝이 모두 `vars.ADR001_QUALIFICATION_ENFORCED` 가드를 달고 있어 `blockingStep` 이 거부한다, **opt-in 게이트는 게이트가 아니다** — 그리고 `Issue815OptInGateTests` 와 `productionReadinessContractsAreSatisfiedOnCurrentTree` 가 그 상태를 코드로 핀한다. 남은 2·3·4번은 전부 **서명된 evidence bundle** 에 조건부이고 그 번들은 #373 의 산출물이다. 이 이슈가 스스로 말한 "하나의 빠진 것에 붙은 네 개의 이름" 이 맞다면, 그 하나를 기다리는 **다섯 번째 이름**으로 열어두는 것은 같은 병리의 재생산이다. 실측 2026-09-12: `gh secret list`·`gh variable list` 모두 **비어 있고**, 게이트 스텝은 `test -n "$QUALIFICATION_EVIDENCE_URL"` 로 시작하므로 시크릿 없이 `if:` 를 떼면 **모든 릴리스가 즉시 실패**한다 — 게이트를 켜는 것이 아니라 릴리스를 끄는 것이고, `release.yml:174-184` 의 소유자 결정(2026-07-21)을 전제조건 없이 뒤집는 일이다. 중간 경로(`continue-on-error`, 시크릿 없으면 skip)는 철자만 다른 opt-in 게이트라 기각했고, 검사기를 고치는 것은 grader 를 옮기는 일이라 금지된다. #373 종료 조건: (a) 시크릿 셋 제공 → (b) `if:` 일곱 줄 삭제 → (c) 계약 테스트를 `[R-PUB, R-SEM]` 으로 뒤집기 → (d) **실제 태그 릴리스에서 게이트 스텝이 실행·통과한 run URL**. (d)가 이 이슈 4번 기준을 그대로 옮긴 것이다 |
| #856 | closed | **ANSWERED: no, and the reason is not on our side of the wire.** Measured live at `fa4b40da` (`Scripts/livekit/live_856_the_scripter_plane_cannot_name_its_target.py`): the request carries three fields that look like a target and not one addresses a plug-in. `insert` accepts exactly ONE value — `insert: 1` is refused outright, so the caller cannot select a slot. `track` is accepted, validated against our own track cache and echoed back, and does NOT reach the bus: two different tracks emit the identical CC on the identical channel. `param` is the only field that leaves this product, and what it emits is a controller NUMBER (0 → CC 102, 3 → CC 105 on channel 16), whose destination is an assignment the operator made by hand inside Scripter — which this product neither creates nor can enumerate. So the reply does not OMIT the parameter's name; there was never a name to omit, and no readback plumbing on our side would produce one. The oracle exclusion now says that instead of "there is no State A to verify", which described our routing. The other half of the question — the MCU-C4 plug-in-edit plane ADR-018 ranks first — was built and WITHDRAWN before merge: Logic re-banks on selection and the product has no readback for its bank offset, so a strip index has no stable meaning and cannot carry a verified write. Those measurements, plus a real un-settled-banking defect affecting every strip-relative MCU operation, are in #862. ADR-009 (#292) stays open on #862, not on this |
| #862 | OPEN | The MCU plug-in-edit plane cannot bind a strip index to a channel, so it cannot host a verified write — measured while withdrawing the #856 attempt. Selecting a channel makes Logic RE-BANK to keep it visible, after which `MCUChannel.currentBank` (the server's own bookkeeping, never compared against Logic) is stale and the next call's strip index names a different channel: a request that had just reached `Studio Grand` read `Deluxe Classic` on the identical repeat. Separately and really: `withBanking` sends its bank presses 1 ms apart then sends the strip-relative message immediately, so arrange track 11 (targetBank 1, strip 3) selected CHANNEL 3 — a 250 ms settle fixes the single call, and the same shape is in the restore loop. Not fixed here because nothing public can exercise it: MCU is a fallback behind Accessibility for select/mute/solo/arm, `set_master_volume` is bank-invariant, and the one `[.mcu]`-exclusive strip-relative operation was the one withdrawn. Also measured: the LCD is a DELTA surface whose upper row (parameter names) redraws fully on a mode change while the lower row (values) does not, and the channel-name toast fires only when the selection actually CHANGES |
| #855 | closed | **ROOT CAUSE: the last segment of a plug-in menu path is the CHANNEL CONFIGURATION, and that belongs to the strip, not to the request.** Every configured path ended in a hardcoded `Stereo` / `스테레오`; on a MONO strip the `Compressor` submenu offers exactly `["Mono"]`, so all four paths missed the leaf and the insert refused a plug-in that was sitting right there. Both suspects in the report are cleared by the same replication — run with the product's own 2.5 s messaging cap and its own 250 ms post-press sleep: `findAudioPluginRootMenu`'s conjunct matches **exactly one** menu out of 162 and that menu carries `Dynamics`, and timing is not it either because `AXUIElementPerformAction` on the slot BLOCKS ~1.5 s before returning `-25204`, so the menus are up and the root findable in ~25 ms before the sleep even starts. The fix reads the leaf off the menu the run opened: the preference wins when offered, a single-item menu is taken because there is no choice in it, and several unrequested configurations still refuse rather than choosing a channel layout on the operator's behalf. The failure now distinguishes root-not-found from a missed category from a configuration the strip lacks — one sentence for all three is why this needed a replication rather than a reading — and a success reports `menu_leaf_chosen`, without which a mono insert and a stereo insert are indistinguishable on the wire. Live: `Mono` on track 1 and `Stereo` on track 19, both State A with the plug-in read back by name, both undone and the undo verified |
| #864 | closed | **FIXED: an undo that reports success now has to have undone something.** It routed `[.midiKeyCommands, .cgEvent]` and the MIDI rung sent CC 30 on channel 16 — a controller number that does nothing unless the operator bound it in Controller Assignments, which this product can neither create nor enumerate — and because a send-only channel succeeds at the wire the `.cgEvent` rung that would post a real Cmd+Z was NEVER reached. AX leads the chain now: the Edit-menu entry is pressed and read back, and State A means the entry named before the press is not the entry named after, so the stack demonstrably moved. Matching names is `noop_unobservable`, not a claim; a disabled entry is refused with `write_attempted: false` before anything is pressed. The row is found by its SHORTCUT because the wording is both localized and state-dependent — with an empty stack Logic writes `Can't Undo`, which does not START with `Undo`, so a prefix match lands on `Undo History…`, and containment is worse because three Edit-menu titles carry the word. That is not hypothetical: it is what the first implementation did, and both reads named that decoy. Measured in `docs/observations/2026-09-12-the-undo-entry-is-identified-by-its-shortcut-not-its-wording.json` (undo = cmd-Z mask 0, redo = mask 1, `Undo History…` = mask 6). Live: rename → `Undo Renaming`, undo → State A with `entry_after: Can't Undo`, and the track's original name back from `logic://tracks`, a source independent of the menu that was pressed |
| #870 | closed | `edit.toggle_step_input` answered `element_not_found` on a menu that plainly carries the item, because the path matched `.exact` against the bare `Step Input Keyboard` while Logic offers `Show Step Input Keyboard`. The sharper reason, measured after the fix: the verb is **lazily populated** — `Step Input Keyboard` before Logic has ever opened that menu, `Show Step Input Keyboard` from the first open onward, and it STAYS. The product reads without opening, so it sees whichever spelling a human last left behind, and the operation's success depended on whether anyone had ever pulled down the Window menu. Two further measurements ruled out the tidier stories: the title reads `Show …` in BOTH window states (it never becomes `Hide …`), and clicking it while open still closes it — so the verb describes nothing. Matched by containment now, which is state-independent. Containment is not automatically safe and was tested against the `[directive]` on `AXLocalePolicy.swift` that rules it out for the metronome (`再生` inside a longer label would be taken for Play): across 48 Window-menu items exactly ONE contains any of the three step-input labels. Found because #864 touched `AccessibilityChannel+Editing.swift`, which obliged a harness that had gone red; fixed in #869 |
| #871 | closed | `insert_plugin` has no way to name a channel configuration, so Gain is unreachable on a mono strip. Measured against the merged #855 fix: `Channel EQ` and `Compressor` reach State A choosing `Mono`, while `Gain` returns State C `leaf_not_offered_by_this_strip` with `menu_leaf_offered: ['Mono', 'Mono->Stereo']`. **The refusal is correct** — that strip offers two configurations, neither is the spec's preferred `Stereo`, and picking `Mono->Stereo` would change the channel's OUTPUT CONFIGURATION, which is a far larger act than inserting a plug-in and not one to perform unasked. So this is a missing parameter rather than a broken match: a strip that offers a choice is unreachable because the caller cannot express one. #855's acceptance named `Compressor and Gain` and I verified only Compressor, which is corrected on that issue. Also the live case `live_855_…` says in its own docstring it cannot arrange — the refusal clause is confirmed live now, not only by unit test | **FIXED:** `configuration` (별칭 `channel_configuration`) 으로 호출자가 채널 구성을 지명한다. 스트립이 **실제로 제공할 때만** 존중되고, 제공하지 않는 값을 요청하면 선호값이나 단일 항목으로 폴백하지 않고 fail-closed 한다 — 요청하지 않은 레이아웃을 쥐여주는 것은 거부가 막으려던 바로 그 피해를 반대편에서 저지르는 것이기 때문. 뮤테이션으로 확인: 그 절을 풀면 유닛 2개가 죽고 라이브 체크에서 그 절만 빨개진다. 거부 문구가 이제 해결 방법(파라미터 이름)까지 말한다. 라이브 3케이스: configuration 없음 → C(제공목록 `['Mono','Mono->Stereo']`), 없는 값 → C, `Mono` → A(chosen=Mono, Gain readback, 되돌림 확인). 한 가지 자기 결함을 현장에 주석으로 남김 — 파라미터가 레지스트리 allow-list 를 통과하고 채널도 읽는데 첫 라이브 실행에서 아무 일도 없었다: 디스패처가 라우터 파라미터 맵을 **명시적으로** 만들기 때문에, 거기 이름이 없으면 채널은 영영 못 본다 |
| #876 | closed | **The report's premise was wrong in both directions, and a German Logic now drives.** It says the server works for English and Chinese: it works for **English, Korean and Japanese**, and Chinese appears in exactly two string arrays and in no `LabelSet` variant at all. German was at **zero**. The fix is not a translation, and the first alignment proves it: German spells the application name with a NON-BREAKING space (U+00A0) between the words and puts a space before every ellipsis (`MIDI-Datei …`), neither of which anyone would type. So de-DE joined the campaign the way ja-JP did. Logic's UI language is a `defaults` write on `com.apple.logic10`, so **every language Logic ships can be measured on one machine** — the wall recorded as 'needs a German Logic' was never a wall. **A LIVE RUN, NOT A TABLE**: on a German Logic the menu bar reads `Ablage / Bearbeiten / Spur / Navigieren`, Edit ▸ Move ▸ To Playhead resolves entirely out of policy labels, and `transport.goto_position` drives the playhead to bar 9 through the German dialog route with the control bar's position readout visibly changing. That run found a PRODUCT DEFECT the tables could not: `goto_position` failed `dialog_unidentified_new_window` — the menu leaf fired, the window opened, and nothing could name it. Two layers: the German dialog title was unmeasured (it is `Zu Position`, read by OPENING it twice; a navigation-free census opens nothing, so it occurs there 0 times), and **fixing that changed nothing**, because the decision lived in a literal title list inside the AppleScript — written TWICE, in the position route and the stray-UI sweep — restating what `AXLocalePolicy.goToPositionDialogTitle` already declared. The menu path beside it had been moved onto the policy for #519; this half was left behind and its own comment said so. The handler is rendered from the LabelSet now, and it is deliberately NARROWER than the literal by one string: `Go to Position` had no provenance, `.exactStrict` is case-sensitive so the Swift side never accepted it, and two members differing only by case are refused by `check-probe-product-drift.py`. Landed: `de-DE` in `SUPPORTED_LOCALES`, the census proving the locale off `Bearbeiten`, German ancestry markers (`[Steuerungsleiste`, `[Bibliothek`, `[Spuren enthält`) and the `Spur-Header.` help rule — without which German produced three surfaces against English's six — six de-DE records + evidence, **30 of 145 labels measured in German**, and the harness-side spellings that had to move too (`AX_REGION_LABELS`, and `ARRANGE_WINDOW_TITLES`, which lacked `Spuren` so every German capture recorded 'no Logic window on screen' while Logic was plainly on screen). The STRUCTURAL blocker is fixed rather than worked around: `check-variants-appear-in-a-census.py` decided a variant's locale from its SCRIPT, which holds only while every language has a script of its own — `Bearbeiten` is ASCII and would have been filed under en-US, `Löschen` matches nothing and would have been skipped, so a second Latin-script language was unrepresentable. It derives the locale from the evidence now, which is stricter: 99 absences became 117 because strings that pinned to no script had been quietly excused. Two tests were defused by supporting German — both used it as their example of an UNKNOWN language — and were moved to a language Logic does not ship and to a locale that cannot exist. NOT done, and its size is a set in `RATCHETS.json` rather than a sentence: **114 of 145 labels are still unmeasured in German**, eight surfaces have no German record, and `tracks.record_sequence` does NOT work in German — the SMF import panel is localised too and it refuses at `preflight_blocking_dialog` naming a dialog called `Importieren`. That refusal is recorded by the passing run itself, because a run that quietly picked the operations that pass would be measuring its own selection. Six German variants were READ but cannot be CITED: `locale-propose.py --apply` keys records by the census row's raw `surface` field and every window row carries `arrange.window`, a pipeline defect affecting ko-KR and ja-JP identically and deliberately not fixed while adding a locale |
| #872 | closed | `mixer.insert_plugin`'s rollback is blind: `undoLastLogicAction` (`AccessibilityChannel+Plugins.swift:428`) builds a `Cmd+Z` CGEvent, posts it and returns `true` unconditionally, so `rollback_succeeded: true` means **a key event was posted** rather than the insert was undone. The verified plug-in surface next to it already does this properly — `liveUndoClick` (`+VerifiedPlugins.swift:4831`) hides plug-in windows first so the undo lands on the global Edit menu rather than a focused plug-in window that would swallow it, clicks `Edit ▸ Undo` and READS the entry, refuses with `notOurs` when the top of the stack is not our insert so it never undoes the operator's work, falls back to a blind Cmd+Z only when the item was not found, and its caller pairs it with a tristate readback that never claims a success it cannot see. #864 added a third path that presses Logic's own entry and reads it back. A rollback is what a caller trusts when a verified write fails its readback; one that cannot fail is worse than none, because the envelope says the project was put back when nobody looked | **FIXED:** 롤백을 슬롯 재읽기로 **확인**한다. 이 연산이 이미 가진 readback 이 슬롯이므로, undo 뒤 그 슬롯이 방금 넣은 플러그인을 더는 들고 있지 않을 때만 성공으로 센다 — 세계에 대한 관측이고, 아니라고 말할 수 있다. **보낸 액션과 관측된 결과를 별도 필드로 분리했다**: 둘을 합쳐놓은 것이 옛 값을 읽을 수 없게 만든 원인으로, `true` 가 어느 쪽인지 알 방법이 없었다. 이제 `rollback_action_posted: true` 옆에 `rollback_succeeded: false` 와 발견한 플러그인 이름이 함께 온다. 뮤테이션으로 확인: 무조건 값으로 되돌리면 테스트가 죽는다. **하지 않은 것**: 세 undo 경로는 수렴시키지 않았다 — `liveUndoClick` 는 우리 것이 아닌 엔트리를 거부하고, `defaultUndoOrRedo`(#864)는 Edit 엔트리를 읽어 돌려주며, 이쪽은 슬롯으로 확인한다. 각자 무엇을 하는지는 말하고 있고, 하나로 만드는 것은 이 결함보다 큰 작업 |
| #866 | closed | A unit test's verdict came from the LIVE Logic window. `inspectorStripReading` declares `runtime: Runtime = .production` and the track-creation verifier omitted the argument, while every other AX read on that path used the injected one — so `testAccessibilityChannelCreateInstrumentVerifiesTrackCountIncrease`, which builds a complete fake AX tree down to a header described `Software Instrument Track`, had its answer decided by whatever strip the real inspector was showing. It produced a false **GREEN** in the ship gate rather than a false red: the same test passed the full suite at 4510 tests half an hour before it began failing, with no change to anything it touches, and what differed was which track the live session had selected. Confirmed failing on a clean `origin/main` worktree, so it belonged to no branch in flight. CI never saw it because CI has no Logic — the read finds nothing there, falls through to `observed_header`, and the assertion passes unconditionally, so the two places that run this test disagree about what it tests and neither says so. The one-line fix matters less than `Scripts/check-injected-runtime-reaches-ax-reads.py`: inside a function whose own signature declares a runtime, a call to a defaulted-runtime resolver must pass one — narrow enough to obey, and deliberately silent on the production entry points that have no runtime to pass, since a guard that flagged the CLI is one somebody switches off. Across 84 resolvers it leaves exactly one named exception, argued in the source since 2026-08-29 |
| #854 | closed | **제품 결함이 아니었습니다 — 실측으로 종료.** AX 사실 자체는 유효하고 관측 기록으로 남습니다(아렌지에서 `AXPress` 는 `AXFocused` 요소만 액추에이트하고, 포커스 없는 press 는 `rc=0` 을 돌려주며 아무것도 하지 않는다). 다만 제품의 액추에이터 사다리는 이미 그 위에서 동작하도록 실측 기반으로 만들어져 있었다 — `AccessibilityChannel+Tracks.swift:781` 이 그대로 적어놓고 있다: *SOLO flips on AXPress; MUTE needs exclusive-select + key 'm'; ARM needs exclusive-select + the key chord. **The read-back — not each rung's return value — decides State A.*** MUTE/ARM 은 애초에 AXPress 를 쓰지 않고 선택을 먼저 세우며, AXPress 에 의존하는 것은 SOLO 하나다. 실측 2026-09-12: track 1·5 mute 양방향, track 5·12 solo 양방향 — 전부 검증된 State A. Acceptance 1번은 “세우지 않는 이유를 기록한다” 쪽으로 이미 충족(코드 주석이 그 기록), 3번은 포커스가 아니라 **readback** 으로 판정하므로 rc 를 믿지 않는 더 강한 형태로 충족. 2번(#291 슬롯 센서스 재실행)은 이 이슈가 아니라 ADR-008 소관이므로 그쪽으로 넘긴다 |
| #852 | closed | **네 acceptance 모두 완료. 오래 막혀 있던 원인은 Logic 이 아니라 우리 프로브였다.** 역할 집합 단일화·체크박스 인식·drift guard 확장은 PR #857 에서 끝나 있었고, 남아 있던 4번(Compressor·Gain 의 Controls-view 체크박스 수를 #292 에 게시)이 열려 있었다. 그 측정을 막던 것: `insert_plugin` 은 플러그인 창을 **열어둔 채** 끝나므로, 삽입 후 Open 을 누르는 프로브 실행은 그 창을 **닫았고**(실측 창 3→2), `newWindows` 는 추가만 보므로 `opened_windows: []` → `not_found` 를 보고했다. `not_found` 는 없는 슬롯·틀린 라벨·읽히지 않는 스트립·토글로 닫힌 창이 **전부 내는 같은 한 단어**라, 하루에 블로커를 네 번 틀리게 짚었다(픽스처 → 구조적이라 단정한 `-25200` → 내 한국어 라벨 → "표면 자체의 벽"). 고친 것은 둘이고 첫째가 본질: 창을 닫은 press 는 before/after 창 수와 함께 `toggled_closed` 로 보고하고, 방향이 알려졌으므로 한 번 더 눌러 연다 — 희망 섞인 재시도가 아니라 한 연산의 나머지 절반이다. 고정 대기 `usleep(1_400_000)` 도 폴링으로 바꿨으나(`open_wait_ms` 가 영수증에 남음) 그것만으로는 결함이 안 고쳐졌고 커밋에 그렇게 적었다. 측정 결과: **Compressor 29행 중 AXCheckBox 2** (`Limiter On:`, `Auto Release:`), **Gain 2행 중 0** — 체크박스가 이 저장소가 액추에이트한 유일한 Controls-view 역할이므로, 이 평면에서 검증된 쓰기가 가능한 파라미터의 상한이 각각 2개와 0개라는 뜻이고 #292 에 그렇게 게시했다 |
| #851 | closed | **FIXED: the staleness rule is evaluated once per payload.** `mcu.feedback_stale` and the word in the MCU `channels[].detail` line shipped in one document and came from TWO reads of the cache — `router.healthReport()`, which reached `MCUChannel.healthCheck` and let it derive the word from its own read, and `cache.getMCUConnection()` a few lines later for the wire field — so feedback arriving between them put `…feedback stale` beside `feedback_stale: false`, with neither field wrong at the instant it was taken and the document wrong as a whole. This is the check `live_849` REFUSED to write: comparing the two fields was its first shape, rejected as green-by-luck and red-on-a-race, and filed here instead of written. `MCUChannel.healthCheck` now renders no staleness at all and reports what it can answer without a clock; the dispatcher takes one snapshot, computes staleness once, and the same boolean produces both the wire field and the clause on the MCU row, so agreement is structural rather than lucky. A disconnected port earns NO clause in either direction — it is not stale under the rule and not active either, and a renderer reading only the boolean would print `feedback active` on a dead port, which is the mutation the live harness catches deterministically. The live run cannot show that the race USED to happen and says so in its own docstring; what it adds is that the ASSEMBLED payload is self-consistent across both answers, with the sweep required to contain both because a surface that is permanently stale agrees with itself forever |
| #816 | OPEN | R-PUB asks where a third party could check the qualification evidence. **CORRECTION 2026-09-20: this row said "there is no qualification evidence: the system that produced it was removed on 2026-09-13", and that stopped being true on 2026-09-19.** #926 (`5519079e`) restored the subsystem — `Sources/LogicProMCP/Qualification/` holds `ProductionReadinessContracts.swift`, `PromotionGate.swift`, `QualificationRunner.swift` and the rest, and `--qualify` is back in `MainEntrypoint.swift`. So evidence can be produced again and the debt is live rather than vacated: nothing publishes it anywhere a third party can read it. It stays behind the same wiring #373 names — the gate steps are not in `release.yml`, so no run produces a URL to publish |
| #846 | SHIPPED | The AX plug-in probe restates a label rule the PRODUCT owns and the two spellings diverged: `AXLogicProElements+Mixer.swift:265` lowercases before comparing against `AXLocalePolicy.mixerNamedElement`, and `ax_plugin_menu_probe.swift`'s `trimmed()` does not — so Logic's `"Mixer"` can never match the label set's `"mixer"`, and only a locale without case distinction (`믹서`) passes. That is why `live_306` was written against a Korean fixture. **Filed with a second defect that turned out to be my own misreading**: I reported the probe's structural model as wrong after finding no labelled `AXGroup` ancestor, but I had measured the INSPECTOR — Logic describes its two-strip area `Mixer` too, and the matcher's ancestor clause is what correctly rejects it. Open the real Mixer and the structure is exactly what the matcher expects. With the case fix applied the chain runs end to end (mixer 1 -> strip 1 -> slot 1 -> open_press 0 -> 7 Controls sliders). SHIPPED in #848: all thirteen policy-label comparisons go through one `matchesPolicyLabel` that folds BOTH sides, user identifiers stay exact, and `check-probe-label-matching.py` fails when probe and product can drift — its own first version had three holes a mutation run found, so its self-test now injects six defects. The Korean-fixture regression question was answered by construction rather than by a run: every observed string there is Hangul, where folding is the identity. **The second defect I filed was my own misreading** — I had measured the INSPECTOR, whose two-strip area Logic also describes `Mixer`; the matcher's ancestor clause is what correctly rejects it, and the case fix alone was sufficient. Unblocked #299's measurement, which is what turned #292 from three options into two |
| #843 | SHIPPED | `FileHandle.availableData` raises an Objective-C `NSFileHandleOperationException` when its pipe goes bad, and a Swift `catch` cannot see one: a full-suite run aborted at `QualificationSubprocessSession.startReaders` and took the whole test process with it, so every test after that point went UNRUN rather than failed. An audit found two more of the same shape in `BoundedProcessRunner`, where it is worse — inside a `readabilityHandler` the exception skips `group.leave()`, so a surviving process waits forever. **The first fix was wrong and the live harness caught it**: `read(upToCount:)` throws properly but WAITS for the count, and a handshake frame never fills 64 KiB — 27s green became a 45s `timeout:handshake`, pinned by reverting that one file and rebuilding. The unit tests were 8/8 green on both versions. All four sites now use POSIX `read`, which returns what is available AND reports failure by return value. NOT covered, stated in the file rather than implied by a green test: #843 has no regression test — a mutant left the first attempt passing, because a child that `kill -9`s itself closes its pipes normally and the exception never fires, and there is no seam to make a descriptor go bad while a reader is blocked on it |
| #821 | SHIPPED | The release path could not publish a tag once the release commit was merged. Both halves of the refusal were narrowed rather than waived. The LIVE GATE was over-broad by exactly one case: rules are read with `git show "$RULE_REF:…"` — from the ref, never the worktree — so "a branch edits its own judge" needs HEAD to carry commits the reviewed ref lacks, and `HEAD == origin/main` has none by definition. That case is exempt now and says so in its output; a local ref and a literal `HEAD` are still refused. The STAMP GATE demanded a token for content already published: a commit reachable from any remote-tracking ref passed the gate on the branch that carried it, and re-demanding a stamp keyed on (tree, HEAD, BASE) asks for one no honest route can mint after a merge. It is now skipped for those, the way a deletion is. Neither change lets a RED gate mint a stamp, and `Scripts/test-preflight-stamp.sh` proves it by building an unpublished commit in a scratch repo AND copying the stamper into it — without that copy the refusal case had been passing for the wrong reason, reporting "gate NOT enforced" and exiting 0. Verified by the release completing: `refs/tags/v3.16.0` -> `c1568bd2`, 5 assets. Found on the way: the pre-push hook in this checkout was a three-week-old COPY of the script, not the documented symlink, so fixing the script would have changed nothing |
| #768 | closed | closed 2026-09-05 by ADR-019. The ask was that an empty `variants` list be able to say whether it is a decision or a gap. All 18 such labels now declare `unmeasured` in every locale in `docs/locale/ui-labels.json`, `check-locale-labels-json.py` refuses a missing or unbacked declaration, and `RATCHETS.json` counts them so the number can only fall. They are named as gaps, not resolved. |
| #769 | closed | closed 2026-09-05. `operations_driven` counted `tools/call` only, so a harness driving the product through `resources/read` scored zero and could never satisfy `is_clean` — the clause's own docstring says it rules out a run that never touched the product. Resource reads count now, named by URI. Measured live at `a88f49e0`: `live_291_output_slot_is_read` came back 9/9 with `operations_driven: 7`, **six of them `resources/read` the old rule recorded none of** (record `2026-09-05-a-resource-only-read-was-absent-from-its-own-receipt`). The zeros #769 cites are earlier revisions of those harnesses and were not re-measured here — this revision makes one `refresh_cache` call, so the old rule would have scored it 1 and passed while its receipt named none of the reads that did the work. |
| #883 | closed | **FIXED: two LabelSets held Apple's row where Logic draws something else, and each stopped track creation in one language.** The German drummer leaf in `newSessionPlayerTrackMenuItem` is Apple's row, with U+00A0 after `Session` and before the ellipsis; the Track menu renders both as U+0020 (the 2026-09-12 German census had already recorded that spelling, and a German Logic read on 2026-09-26 agrees). The menu match is exact, so the drummer create found no leaf and the router fell back to a key command that created no track. The second was found by this issue's own live harness: a Spanish New Track sheet titles its button `Crear`, Apple's plain `Create` row, while `createButton` cites `Create#und`, whose Spanish is the noun `creación`. So in Spanish `project.new` returned State C and left the mandatory sheet up, and the next call was refused as blocked by a dialog. Neither row fits every language: plain `Create` is `Erstellen` in German, where the sheet reads `Erzeugen`. Neither row was replaced; each is what `derivedFrom` cites, and the rendered spelling sits beside it. MEASURED: `live_883_each_track_type_in_every_locale` passed 141 of 141 checks in all ten languages at 7bfd097b, and its evidence document is clean. In each language, `project.new` left no sheet, and instrument, drummer, external MIDI and audio each returned State A, left no sheet, and inserted exactly one track, the one the response names. In German the track-header rail's pixels changed around each of the four creates. The same harness against a binary built with `AXLocalePolicy.swift` as it was on 8c09e586 turned red the three checks that name that change: Spanish `project.new` left the sheet up, and the German drummer returned State B through the key-command fallback and inserted nothing. `Issue883TrackCreationAsRenderedTests` presses each of the four German leaves and confirms the Spanish and German sheets through their own Create. Removing either new member turns exactly one of its six cases red. NOT proven: the TYPE of the created track (#766). The four operations are told apart by the leaf each presses, not by the track Logic makes. A New Track sheet the operation gave up on is now dismissed through its own Cancel when that Cancel works: when `project.new` or a `track.create_*` cannot identify the sheet's Create, or pressed it and the sheet stayed up, it now presses that sheet's Cancel (resolved by the existing `cancelButton` LabelSet, as the Save As and Go To Position cleanups already do; no Escape) and re-reads. The non-success envelope carries `new_track_sheet_cleanup`, whose `result` is `observed_closed` only when the pressed sheet's own identity is gone and a fresh complete read finds no sheet, and `not_observed_closed` otherwise. It dismisses only a sheet the operation owns: `project.new`'s mandatory sheet, or one that appeared after a track create's menu press when the preflight read had seen none; a Cancel that reads disabled is not pressed. `Issue883OwnedNewTrackSheetCleanupTests` covers both operations with a fake sheet that leaves only when Cancel is pressed, a Cancel that changes nothing, and a sheet that was already up; each of five production mutations turns at least one of them red. LIVE for `project.new` only: the same harness against a binary built from 7bfd097b with `createButton` emptied got `new_track_sheet_cleanup.result` `observed_closed`, through `cancel_button`, in all ten languages, and System Events counted no sheet and no dialog afterwards. The track-create cleanup is LIVE in Korean only: on the open fixture, `track.create_drummer` through the same mutant raised the New Track sheet, found no Create, pressed Cancel and got `observed_closed`; System Events counted no sheet and no dialog, and the track count stayed 19. Cancel's label is the one the ten-language `project.new` runs resolved. DECIDED 2026-09-26 as #883's scope: when Cancel reads disabled, cannot be identified, or is pressed and the sheet stays, the sheet stays up and the response says so (`not_observed_closed`, and for a track create `dialog_present` with `waiting_for_user`); no Escape or other key is sent. The operation reports a sheet it could not close; it does not promise that none is left. zh_TW raises its New Track sheet after the project window: an earlier build read that window with no track and no sheet, reported the project open, and left the sheet that arrived next. `project.new` now waits up to twelve more polls for the sheet while the window shows no track, a wait that a build raising no sheet also pays; two cases in `Issue883OwnedNewTrackSheetCleanupTests` cover the late sheet and the sheet that never comes. Last-track deletion shares the Create reader and still leaves such a sheet up |
| #892 | OPEN | Locale support is built by MEASURING each language separately: `AXLocalePolicy` holds one `LabelSet` per label with a variant per language, every variant is a string somebody read off a running Logic, and six of the ten locales Logic ships are not covered at all. The cost is linear in locales and the evidence is unreproducible. Logic ships the whole mapping — `QuickHelp.plist` carries 9,835 entries under ten locale names (seven distinct files; en, it, pt and zh_TW are the same bytes), and the bundle carries 605,160 `.strings` entries across 2,537 files with zero key-set drift between the nine non-English locales. The machinery to read and cite it landed with #893 in #894; what remains here is the migration itself, and two measured facts bound it. A string identifies a SET of keys, not one: 3,766 of 9,835 ko keys share their composition with another. And the reverse direction is mostly unavailable — nib `qhid` binds only 587 keys (6.0%) to a screen file, concentrated in Preferences and Song Settings and zero for the main window, so `key -> control` is not a canonical fact for the surfaces this repository drives |
| #895 | closed | closed 2026-09-16 by #903. `nibstrings` reads the English Apple compiles into `Base.lproj/<table>.nib`, keyed the way its own translations are keyed: 6,270 pairs from 162 nibs, and ZERO of those 162 tables ship an `en.lproj` overlay, so the false absence was total rather than partial. Joined against each of the nine translated locales, 6,188 of the overlay's 6,216 keys agree and not one key is nib-only. The larger half was a defect the source exposed: `translated.en.u32` -- which answers "does Apple translate this", the answer that EXEMPTS a literal comparison from needing a LabelSet -- grouped by SOURCE, so English in `nibstrings` and its translations in `strings` could never meet and all 6,270 labels read as untranslated on no evidence; 66,589 -> 68,417 once joined. `nowhere` fell 102 -> 98. What it does NOT cover is #902 |
| #902 | closed | **Closed by the pull request carrying this row; the row is ahead of GitHub until it merges.** `nibstrings` (#895) read only the 162 `Base.lproj` nibs. The other 1,007 hold zero `NSLocalizableString` objects, so every label on those screens was absent from every corpus: 60 old-style nibs per locale, including `en.lproj`, and 407 in no `.lproj`, 312 of them MAPlugInGUI plug-in screens. A new source, `niblabels`, reads them. It reads a string only at a (class, key) AppKit draws as text (`NIB_LABEL_SITES` in `Scripts/logic_canon.py`), and reads an `NSClassSwapper` as its original AppKit class. It drops Interface Builder's own default titles at their own site, a translated row whose English twin was dropped, blank strings, numeric text-field readout examples, and Apple's DO NOT LOCALIZE marker. The unit is the nib without its `.lproj` and the key is the object's path from the archive root, so a translation meets its `en.lproj` twin. The object index cannot do this, because it moves in 20 of the 60 old-style nibs. A nib in no `.lproj` is locale `-`. Measured with `Scripts/logic_canon.py build` and `census` on Logic 12.3 (6674): 1,007 files, 9,259 rows, 35 corpora (was 24). The build ran twice and wrote a byte-identical `MANIFEST.json`. Dropped: 402,921 strings at 300 non-label sites (NSNibOutletConnector.NSLabel 17,313, NSTableColumn.NSIdentifier 1,266, NSTabViewItem.NSIdentifier 330, NSTokenAttachmentCell.NSContents 102), 42,305 runtime attributes other than `accessibilityLabel`, 810 IB defaults, 2,376 translations of a dropped English string, 183 blank strings or numeric text-field readouts, 43 marked DO NOT LOCALIZE. Join: 636 addresses carry a translated row, 630 carry all ten locales, 3 English addresses miss one locale, 3 translated rows have no English twin. 467 of 633 English addresses are translated, and `translated.en.u32` grows from 68,417 to 68,548. The measured reason: nine AXButton titles a German Logic showed on the Compressor editor (`-12 dB`, `Meter`, seven circuit names such as `Vintage<U+2028>Opto`, record 2026-09-25-977-de-DE-plugin-editor-is-not-a-blocking-modal) were in none of the 24 earlier corpora and are in `niblabels/-`. It closes 0 of the 85 `nowhere` policy literals. Three `canon_absent` records now list the eleven new corpora, and `Scripts/locale_labels.py --write` moved two labels' zh-CN coverage to `derived` through a `Loop` segment label in MAContentTaggingUI's STORMMainWindow.nib. NOT proven: that any other plug-in screen is English on every locale (only the Compressor, only de-DE); that STORMMainWindow is reachable; the plug-in text set through `text` and `labelString` runtime attributes, which is not read, so `THRESHOLD` stays absent; and designer placeholders such as `Header Text` and `Text here`, which no class separates from a label and which are in the corpus. |
| #904 | OPEN | A `LabelSet` carries one string per language somebody read off a running Logic, so ten locales cost ten machines and six of the ten Logic ships have never been read at all. A `.strings` row is ONE control's text in every locale at once, so the ten languages are a LOOKUP. Measured over all 159 LabelSets: intersecting the rows each member matches in its own locale resolves 67; Apple's own key namespaces — `#mti` 48/48 menu titles, `#acc` 60/60 accessibility names, `StrTransportBtns|||` 44/44, `StrToolbItemName|||` 62/62, every one complete in all ten — resolve 45 more, for 112 with a row that can be chosen mechanically. Of those, 78 can be APPLIED with no judgement because the row already holds every string the label carries; the other 34 carry something it does not, most of them lowercase containment fragments but some genuinely compound (`pluginOpenOrListControl` matches `open` AND `list`). 30 are ambiguous between rows that disagree and 17 have no row, fourteen of those being fragments that were never whole labels. So 1,590 live readings become 15 recorded decisions. The census is wider than the LabelSets: `MeasuredLocale` has 2 locales, `topLevelMenuTitlesByLocale` had 3 and `supported_locales` 4, and 60 hardcoded CJK literals sit outside `AXLocalePolicy.swift` in 11 files — 48 matching sites of which only 2 handle all ten languages. Detection, `derivedFrom` and its guard land in #905 |
| #908 | closed | **Decided 2026-09-18: Logic 11 is NOT supported.** The decision was already in the code and nobody had connected it to the question -- `LogicProSupport.minimumSupportedLogicVersion` is `12.0.1` and the doctor's `logic.version_support` check FAILS below it, so a Logic 11 install has been refused at setup all along while three comments and this row called the question open. What was genuinely missing is that the floor had never been watched REFUSING anything: no case fed it a Logic 11 version, so the constant had a comparison beside it and no evidence the comparison decided anything. `Issue908LogicVersionFloorTests` now drives five Logic 11 versions through the check a user actually runs and pins the floor at a Logic 12, so lowering it cannot pass quietly. The three fallbacks removed in #907 stay removed; restoring the Drummer leaf would have needed a reading on a running Logic 11 AND a deliberate growth of `POLICY-LITERALS`'s `nowhere` list, which may only shrink. |
| #909 | closed | **Closed 2026-09-20; landed by #913 (`37c2c40e`), which closed nothing by keyword.** The four hand-written regexes are gone. `AXLocalePolicy.regionBarsSentence` (`:1547` at HEAD, `:1289` when `37c2c40e` landed it -- the file has moved since) is a LabelSet of all ten sentences whose `derivedFrom` is the row this issue named, `Localizable.strings` key `Region starts at %@ and ends at %@`; `check-labelsets-are-derived.py` proves all ten values are Apple's per locale with no running Logic. `regionBarsPatterns()` (`:1564` at HEAD, `:1306` at landing) builds each pattern by escaping the literal chunks and putting a digit capture where each `%@` was, so a locale Apple adds needs no new regex, and a template without exactly two placeholders yields nothing rather than a half-pattern. `AccessibilityChannel+Regions.swift:419` consumes it. Both measured properties survive and are asserted: the pattern anchors on the sentence's own literal text and never on a unit word, so the MIXED unit (`1 마디` beside `2 小節`) and the German INFLECTION (`1 Takt` beside `2 Takte`) both fall inside the lazy gap. Eleven cases in `Issue778RegionBarsLocaleTests`, including the ten-locale round-trip, the count-equals-labels case so a language cannot quietly drop out, the invented German plural `Regionen beginnen …` which must NOT match, and the `Chord group starts at %@ and ends at %@` near-twin |
| #910 | closed | **Closed 2026-09-20; landed by #915 (`6aa4add2`).** Filed as deliberately-not-built — "a mechanism for one label is how a mechanism gets built wrong" — and then built, with the failure shown first. `composed_from` in `docs/canon/LABELSETS-WITHOUT-A-ROW.json` names the template row and the noun row, and unlike `derivedFrom` must also carry `why_this_noun`, `template_values` and `noun_values`; `check-new-labelsets-name-a-row.py:115` reads the declared `composed_from`; `:118-121` refuses one with a missing field, "A composition nobody can re-derive is the same claim as no row at all". Each declared factor value is verified locale by locale against that row's committed digest (`:132-143`), so declared text is not believed. Every member must be the composed product or an accounted-for extra with `why_extra` written down. The mutation test is the one this issue specified: `test_a_factor_value_the_row_does_not_hold_is_refused` swaps German `Bibliothek` for `Mediathek` — a neighbouring row's value, the exact "WRONG noun for that locale" case the old checks could not see — and asserts `is not what that row holds`. TWO LIMITS KEPT: it proves the ten strings ARE the product of two rows Apple ships, NOT that Logic assembles the item from them (the live evidence is one Korean reading plus the German census; eight locales have neither); and `composed_from` is optional in the schema, so removing it leaves the guard green with one fewer thing checked, which a case pins against |
| #919 | closed | **Closed 2026-09-19.** `Scripts/logic_bounce_ui.py` is installed by `Scripts/install.sh`, so it is product, and it drove Logic's Bounce dialog and save panel through six hand-typed English-and-Korean tables that no locale guard scanned. The vocabulary is generated now: `Scripts/locale_labels.py --write` writes `Scripts/logic_ui_labels.py` from AXLocalePolicy, the installer ships it beside the helpers that import it, and `release-verify-formula-install-paths.sh` already walks the import closure so the Formula cannot omit it. Three LabelSets were added from Apple's rows (`Normalize`; `Realtime` and `Offline` from the Bounce dialog's OWN nib, anchored at `ko` because the `strings` side has no `en`) plus `okButton`, which exists so a bounce confirm is not routed through `saveConfirmationButton` and widened to accept `Save`. `SAVE_PANEL_NAME_LABELS` is GONE rather than derived: AppKit owns that string, it is absent from all 24 corpora by construction, and it sat in an AND with three other signals so a two-language term capped the whole predicate -- what identifies the panel is the Bounce button. `check-shipped-python-has-no-ui-literals.py` refuses a CJK literal in any helper the installer ships, deriving that file set from the installer rather than listing it; pointed at the real tree it immediately found four literals this change had missed, in an AppleScript menu drive |
| #921 | closed | **Closed 2026-09-21; landed by #941 (`eadb5f4a`).** The discriminator this row said was missing is that a CLOSED menu's `enabled` is a validation cache rather than a state — macOS refreshes `AXEnabled` only while that item's own menu is open, so a read taken behind the entry guard's required `CLOSED` state returns whatever the last validation wrote, which is why two readers of the same leaf disagreed without either being wrong and why an external open-and-Escape cleared it for every later call. The disabled branch now forces one bounded revalidation pass before it will call a leaf disabled, and a pass that could not open the menu answers `MENU_VALIDATION_UNREADABLE` instead of reusing `MENU_DISABLED`. The readings behind that account are in #941 and were not landed here as an observation record, so this row cites that pull request rather than restating them. The rest of the row below is what landed first and is kept because it is a different defect with a different fix. FIXED EARLIER: the refusal payload printed `menu_state` as the hardcoded string `could_not_be_closed` for every classification reaching the unsafe-UI path, including `.menuNotFound`, `.menuStateUnreadable` and `.menuDisabled` -- each of which the script returns ONLY after `dismissOpenMenu` answered exactly `CLOSED`. The payload contradicted an observation the same run had made, in the field a person triages from, and it sent a careful outside reporter to the wrong handler: they argued the entry cleanup had refused, while their own `dialog_route_outcome: menu_disabled` rules that out. `menu_state` is derived from the classification now -- `closed` where the script proved it, `could_not_be_closed` only where a cleanup really did not close, `unobserved` otherwise. `safe_to_retry`, `fallback_unsafe` and `write_attempted` are untouched on every path, so no safety changed; only the diagnostic stopped lying. WHAT KEPT IT OPEN UNTIL #941, and how that was answered: Logic answered `enabled=false` for `Navigate > Go To > Position…` to the server process while the reporter's external System Events read of the same leaf said `enabled=true`, and the earlier fix explained neither the disagreement nor why an external menu open-and-Escape cleared it for every later call. The cache account above explains both with one mechanism, so the refusal stopped asserting a state it had only inferred. Nothing in this paragraph came from a running Logic; the evidence that closed the issue is the one cited in the closing note at the top of this row. |
| #923 | closed | **Closed 2026-09-19, by the change that filed it.** `parseScanMode` (`AccessibilityChannel.swift:60`) answers `.disk` for an absent mode, so the DEFAULT `scan_library` writes `Resources/library-inventory-disk.json` (`AccessibilityChannel+Library.swift:300`) -- and `Sources/` contained no code that reads that name. `logic://library-inventory` stayed empty however many times you scanned. The split itself is deliberate and stays: an AX scan reads the Library panel and is richer than a directory walk, so a disk scan must not clobber it. Only HALF of it was built -- the writer learned two filenames and the reader knew one. The reader now carries both under each directory, canonical first, so the AX snapshot still wins where both exist and the payload's own `source` field says which was served. Found while checking #882's claim that `scan_library` never writes the file, on which it closed `tracks.list_library` as an unfixable product defect -- that diagnosis is wrong and is a second thing to undo. Nothing here was run against a live Logic: all three lines are read from the tree. |
| #937 | closed | **Closed 2026-09-20 by the change that filed it, and by a decision this repository had already made.** `docs/canon/LOGIC-FACING.json` lists path PREFIXES whose contents are claims about Logic, and `docs/canon/` is one. Four files under it hold job names, guard names, test counts and the numbers a document may state. A change touching only those could not opt out (the prefix forbids it) and had no row of Apple's data to cite, so the only way through was to paste a citation that resolves and quote its value in the diff -- manufacturing evidence. Two changes hit it the day it was filed. LANDED: an `exceptions` list beside the prefixes, and rule 15, which reads each excepted file and refuses it if it carries a `logic-canon://` reference or quotes a value the pinned corpus holds, or if that lookup cannot be performed at all; `--text` proves the list itself rather than trusting a tree check that runs separately. The residual was that rule 15 cannot see a claim asserted in PROSE -- `The bounce dialog has three buttons.` passes. That residual is CLOSED BY AN EXISTING DECISION rather than by more code: writing such a sentence into one of these files is a committer acting in bad faith, and the threat model in `docs/canon/README.md` puts that out of scope by decision, because write access already forges `MANIFEST.json`, the index and the absence sets. The alternative -- these files carrying no free prose at all, every field a defined identifier or a number -- would mean rewriting what CI-GATE, CI-SKIPS and PROSE-NUMBERS store and what their readers expect, to close a hole that decision already covers. A case pins what rule 15 does and does not catch, so nobody reads the floor as a proof. |
| #896 | closed | Nine standalone drives now use `Scripts/test_*.py` and are discovered by `run-repo-guards.py`, including the measured collision resolution `Scripts/test_logic_variants_manifest.py`. The runner moved to `ci.yml`'s macos-15 `test` job because `Scripts/test_logic_key_event.py` compiles AppKit-dependent Swift; the Ubuntu `guards` job remains shell-only and platform-independent. `Scripts/logic_bounce_main_test.py`, `Scripts/logic_bounce_support_test.py`, and `Scripts/logic_bounce_ui_test.py` remain `*_test.py` libraries because they have no `unittest.main()`; `Scripts/run-helper-suites.py` collects them with their committed floors. **Closed 2026-09-21; landed by #949 (`d067e919`).** |
| #897 | closed | `AXStringResolver.resolve` reads `StringsIndex`; the absence sets are built from `extract_strings`. For ko that is 10,395 (unit, key) pairs against 60,048 — **83% of the corpus is invisible to the resolver**, so it answers None for strings Apple ships while `is_absent` correctly refuses the absence claim, and the author has no automated path in either direction. Measured on two real pull requests: `설치` (#891) and `키 레이블로 학습` (#882) both resolve to None and are both citable, at `Install.strings/164.title` and `KeyCommands.strings/300557.title`. Bounded rather than overstated: the policy classification, the absence proofs and the 113-of-114 AXHelp result all use `is_absent` over the full corpus and are unaffected — all 101 literals classified `nowhere` were re-tested and 0 are present. Closed by this row's own pull request, which adds `locate` rather than widening the resolver: the narrow table is right for reversing a live AXHelp reading, and issuing a citation is a different job |
| #898 | closed | **Closed 2026-09-20; the move landed in #918 (`6e921496`) and nobody connected it to this number.** `KNOWN_BARE` is no longer a module constant: `check-guards-have-self-tests.py` reads `.github/ci/GUARDS-WITHOUT-A-TEST.json` (under `docs/canon/` until #951 moved the CI-only lists to their own owner), and `check-every-ci-job-is-required.py` ratchets that file `shrink` with `ratchet.key_members`, the plain-list extractor — so the `_ratchet_members` shape trap this issue warned about (a dict read as the POLICY-LITERALS map, returning the empty set and making the ratchet vacuous) does not apply. The hole is closed at the mechanism rather than at the instance: rule 7 gained a `migrated_from` path (now `Scripts/ratchet.py::_before`) which reads the named predecessor AT THE MERGE BASE and requires every member of the new list to appear there as a quoted string, so a waiver list cannot arrive pre-populated with a permission nobody granted. Measured on `ac67c770`: `32 of 37 guards are driven by a test (5 known bare, awaiting one)`, and the five are the five this issue quoted |
| #893 | closed | Every failure this repository has had about Logic's interface has one shape: a string somebody typed, standing where a string Logic ships should have been. Measured across `AXLocalePolicy`'s 379 distinct literals, 113 are a QuickHelp `Title`, 226 more are somewhere in the bundle's 605,190 `.strings` entries, and **40 are in no file of the app bundle** — roughly half deliberate lowercase `.contains` fragments that were never whole labels, the rest labels Logic composes at runtime or labels somebody typed, and nothing could tell them apart. So a fact about Logic is now either CITED to Apple's bytes, with a reference that resolves and a quoted value whose digest matches, or declared uncitable with a proof that the corpus does not contain it. The constraint that shaped it is already written down: `LOGIC-BUILD.json` records that CI has no Logic, which is why the build there is declared rather than detected — so build time needs Logic and commits an index under `docs/canon/`, and check time needs nothing. Proving ABSENCE needs the whole corpus — bounded, and the bound is written down: the four bundle sources, not strings compiled into the binary or living in the dyld shared cache. `docs/canon/absence/` holds the 32-bit digest prefixes of every value in them; a collision makes an absent string look PRESENT, refusing the claim, and can never make a present string look absent. The parser that motivated it resolves 113 of 114 distinct live AXHelp readings to canonical data and declares the 114th absent. Landing in #894; what it does NOT check is whether a citation is the RIGHT one, which `used_for` leaves to a person |
| #884 | OPEN | An outside request for verified automation-point editing — `automation.get_points` / `add_point` / `set_point`, positions in bar/beat/tick, values in dB including silence — so that "fade the last five seconds to silence" stops being a real-time Write/Touch/Latch workflow. The reporter's account of what they saw is in the issue and is theirs, not a reading taken here; this row does not restate it as fact. ADR-class either way: it asks for a new public surface rather than a fix. `2026-09-21-the-parameter-popup-help-reports-automation-data` measured a creation route and a readback. Driving `리전 경계에 2개의 오토메이션 포인트 생성` registers Logic's undo operation named `리전 경계에 2개의 트랙 오토메이션 포인트 생성`, and the arrange window then reads back that something exists: across five state-tagged depth-14 censuses of 719 rows compared over sixteen attributes plus path and depth, exactly one row differs between applied and undone, in one field — the AXHelp of the `오토메이션 파라미터` popup on the acting track, which switches between a parameter-selector description and one that says it turns that parameter's automation data on and off. It moved on all four transitions of two undo/redo cycles; the same popup on the other track never moved. That is presence, not content: no count, no position, no value, and it cannot tell two points from twenty. `오토메이션 값` read 1509949440 in every state, so that field did not distinguish them — which is not evidence it is not a readback, since its own help says it reports the value at the playhead. The route to a real `get_points` is still unmeasured. |
| #885 | closed | **Closed 2026-09-20; landed by `28bfbbc1`.** This issue set its own closing condition — open the chooser on a non-English Logic, read the three strings off the live AX tree, route all three through LabelSets with those readings in the rationale — and that is what happened, on a Korean Logic on 2026-09-15: `Choose a Project` → `프로젝트 선택` (AXWindow title, AXStandardWindow not AXDialog), `Choose` → `선택` (AXButton title), `Empty Project` → `비어 있는 프로젝트` (AXStaticText value). All three bare comparisons are gone: `AccessibilityChannel+Project.swift:250` is `projectChooserWindowTitle.matches(…)`, `:230` filters the button with `projectChooserCommitButton.matches(…)`, and `:257-258` is `projectChooserEmptyProjectLabel.matches(title)` or `…matches(value)`. CORRECTION 2026-09-20: this row previously claimed ja-JP and de-DE carry no variant for these three LabelSets as a stated residual -- false. `AXLocalePolicy.swift:374-395` shows all three (`projectChooserWindowTitle`, `projectChooserCommitButton`, `projectChooserEmptyProjectLabel`) already carry ja and de variants, added 2026-09-16. The rationale strings there still open with a stale first sentence saying ja-JP/de-DE are unmeasured (#885), superseded by a second, appended sentence -- reading only the first is what produced this row's error. What remains true: the LabelSet ratchet and #904/#892 track locale coverage generally, not a residual specific to this issue |
| #886 | closed | Session handoff for the work that became #882 and #883. Kept open as the pointer to what that session established and what it left open; the answers live in the issue's own comments |
| #887 | closed | Six repo guards, their self-test drives, and `docs/observations/RATCHETS.json` were deleted as collateral by `1fc80e79`, and the same commit removed the whole `roadmap` CI job — which orphaned three checks that are still in the tree and invoked by nothing: `adr-index-matches-github.py`, `test-adr-index-guard.py`, `ci-verify-formula-sha.sh`. `run-repo-guards.py` discovers what is on disk, so 20 guards becoming 14 reported `all 31 passed`. Being restored by this row's own pull request; `check-shipped-variant-claims.py` waits on #882, which is what brings `ReleaseQualificationAttestation` back |
| #942 | OPEN | **In progress. The pull request carrying this row handles one of the issue's three shapes and does not close it.** Found by an independent review of #941 and confirmed to pre-date it: the same twelve returns are unchanged on `main`. The generated goto-position script has twelve post-LEAF return sites whose text says a menu this run actuated was not observed closed — and all twelve are NORMAL script results, so `dialogFailureReconciler`, which runs only on a dialog execution failure or timeout, is never invoked for any of them. #941 added `requiresPostActuationMenuReconciliation` and routed exactly one classification through the parent-owned Escape loop; it deliberately did not widen to these twelve, because widening a shared trigger while fixing a blocker is how the next blocker gets made. It matters because an open Logic menu wedges AppleEvent dispatch — a later call answers -1712 while AX reads look healthy — so a run ending on one of these can poison every later operation in the session while the receipt says only that the operation failed. The twelve sites are now declared once as `PostLeafCleanupSite`, which renders each one's script and its two refusal texts. The parser separates the menu refusal, which the script returns only after it observed the dialog CLOSED, from the dialog refusal. The menu refusal now goes through the parent's menu-only reconciliation (`DialogFailureReconciliation.menuOnly`), and for the four sites that refuse in State C, `menu_state` reports what that pass observed. The other eight end in the State B fallback-suppression receipt, which has no `menu_state`. The refusal itself is unchanged. Reachability, read from the script: every site's trigger is a runtime observation (a thrown click or keystroke, the 3 s dialog wait, a ledger write, a focus read, the post-Return dialog state), so none is dead code. Reaching the menu refusal needs the same two things at every site: the dialog cleanup answers CLOSED, and then the menu reads OPEN or UNREADABLE and Escape cannot close it. How often that happens on a live Logic is unmeasured. Still open here: the twelve dialog refusals and the two dialog-appearance refusals, `DIALOG_UNIDENTIFIED_NEW_WINDOW` and `DIALOG_APPEARANCE_UNREADABLE`, still end without observing the menu. The issue's own comment counts them, twenty-six sites in all. #998 was filed for them and closed as a duplicate. **Decision needed:** may the menu-only Escape loop run while the dialog side is unresolved? A dialog this run cannot identify may still be up, and an Escape that reaches a modal dialog cancels it. Also not changed: the State C receipt's `menu_actuation_attempted` and `dialog_cleanup` fields (#999). |
| #943 | closed | **Closed 2026-09-21; landed by #948 (`fa7b7840`).** The refusal receipt for `transport.goto_position` printed `menu_could_not_be_closed_write_attempted_true` beside `write_attempted: false` — two fields of one payload contradicting each other about whether the run had touched the project, which is the single fact a caller needs before deciding to retry. The reason field was a static string chosen from the classification while `write_attempted` was derived from what the script actually reported, so the two could not be read as one statement of what happened |
| #944 | closed | **Closed 2026-09-22 by #962, written by the requester rather than by this repository.** An outside request, from someone building a native AU3 plug-in that bridges Mackie Control and wanting to depend on this project's AX readback rather than reimplement it: `Package.swift` declared only the `LogicProMCP` executable product, so the only way in was running the server as a separate process and speaking MCP to it. The change is the one product declaration the issue asked for, `.library(name: "LogicProMCPKit", targets: ["LogicProMCP"])`, with a name distinct from the executable because SwiftPM accepts a duplicate name, reports `ignoring duplicate product` and silently drops the library. The row called this ADR-class — a public API surface this repository would then owe source stability on — and what settles it is that the surface was measured rather than promised, and measured twice — the first reading of it, written here, was wrong. What a consumer can reach is `PluginInspector`'s ten entry points and the twelve data types they use, AND the Library inventory data model: `LibraryNodeKind` (LibraryAccessor.swift:18), `LibraryNode` (:26), `TreeProbe` (:92) and `LibraryRoot` (:124) are `public` at file scope, and the README lists all four. What Swift caps at internal is the other direction — `Miss`, `ScanOrchestration`, `PathResolution`, `PathRuntime` and `ScanResult` are `public` but nested inside `enum LibraryAccessor` (:213), which is not, so the scanner itself stays unreachable while its data model does not. `grep -rn "^public " Sources/LogicProMCP` returns 17 declarations at file scope, and the README's list is those 17 — the obligation is that list, not the ten the first reading named. Two things the pull request found rather than assumed: the README's `from: "3.16.0"` form resolves v3.16.0, which predates the product, and a consumer package written that way fails with `product 'LogicProMCPKit' ... not found in package 'logic-pro-mcp'` (measured), so the documented form is `branch: "main"` until a release carries it; and adding the library product changes what `swift build -c release` resolves on CI's Xcode 16.4 toolchain from 9 pins to 25 — `async-http-client`, `swift-certificates`, the NIO extras and 13 more, all transitive through `swift-sdk` — so `Package.resolved` is updated in the same pull request. Why that toolchain adds them is not established here; what is measured is that the local Swift 6.2.4 toolchain neither adds them nor prunes them away once present, so the committed 25-pin file is stable under both. The small read-only facade the requester offered is not part of this and has no issue of its own |
| #945 | closed | **Closed 2026-09-21; landed by #948 (`fa7b7840`).** `classifyGotoPositionDialogResult` fell through to a safe-to-retry outcome for any result string it did not recognise, so a typo in one of the script's sentinels — a string the script emits and the classifier does not match — released the fallback path instead of refusing. An unrecognised result is not evidence that nothing was written; it is evidence that this run cannot say. A parity test now drives every literal the script actually emits through the classifier and fails if any of them lands on `unexpectedResult` |
| #947 | closed | **Closed 2026-09-25; landed by #996 (`c58f8170`).** `swift test` on `main` (`8c6681b7`) aborts the whole test process: `readAvailable` reads `handle.fileDescriptor` on every iteration of its loop, and `FileHandle.fileDescriptor` raises an Objective-C `NSFileHandleOperationException` when the handle is closed — which a Swift `do`/`catch` cannot see, so the process terminates on signal 6. The run then produces no result line at all: not a failure count, not a pass count, and every test that had not yet reported is lost. This is the same class as #843 (`FileHandle.availableData` raising on a closed pipe), one call further along and in the qualification subprocess reader. It is the reason a full-suite status is currently unquotable, which makes it a prerequisite for any claim resting on one. The readers now read a descriptor number captured once, and a close-on-exec stop pipe ends them after the grace, so shutdown no longer closes a handle a reader is still reading. They run on their own threads: on a saturated global pool a queued reader never started, and shutdown took 6.0 s against a 3.0 s grace. The serial full suite passed 4,703 tests with exit 0. |
| #950 | closed | **Closed 2026-09-21; landed by #952 (`06e5d37c`).** The barrier an outside contributor meets before a single line is reviewed, and the rebuild traffic the repository makes for itself. Three parts: the pull request BODY check is a required context of its own (`pr-policy`, added to ruleset 19060230 on 2026-09-21 and re-read from the API) so editing a description no longer restarts a macOS test job; a `classify` job decides whether an event needs code validation at all, and the code jobs change their DISPLAY NAME as well as their condition, because a job skipped under its canonical name publishes `skipped` and GitHub counts a skipped required check as satisfied; and CONTRIBUTING.md plus the pull request template stop demanding the whole repository's checks from somebody proposing a documentation fix. No coverage floor, regression test, fork restriction or action pin is touched |
| #951 | closed | **Closed by the pull request carrying this row; the row is ahead of GitHub until it merges.** What was wrong: Canon issue textareas used `render: text`, so a declaration typed there arrived inside a fence the checker deliberately ignores; the issue workflow posted one generic accusation per failing edit, never resolved it, and read a checker crash as the author's fault; and `check-canon-citations.py` ratcheted four CI-only lists that say nothing about Logic. What changed: #959 landed the implementation on 2026-09-21 (`0e84ae6f`) -- the three issue forms carry a declaration as visible prose and evidence is optional at intake, `check-canon-citations.py --format json` gives one structured diagnosis to both callers, `canon_issue_bot.py` keeps ONE owned advisory note per issue, and the four CI lists moved to `.github/ci/` under `check-every-ci-job-is-required.py` behind the neutral `Scripts/ratchet.py`. The issue stayed open for I02 alone. This pull request records that observation and repairs one gap review found: a note left by the pre-#959 workflow carried no marker, so `owned()` never matched it, a repaired body left its warning standing (#944), and a failing edit added a managed note beside it. `canon_issue_bot.legacy()` now matches that note only from the bot's own account and only in its exact head and tail; `active_note()` ranks it after every managed note, adopts the oldest, and marks the rest superseded. What was measured, against the deployed workflow on main `8c09e586`: the disposable maintainer-owned issue #1006 was driven through five real `issues` events, each run waited out before the next edit. Runs 36204823416 `action=create`, 36204853508 `action=edit` (missing -> hidden declaration), 36204886154 `action=none (the note already says exactly this)`, 36204917537 `category=satisfied action=edit (the note moves to resolved)`, 36204946407 `action=edit (the note moves to actionable)` (recurrence). All five concluded `success`. The issue ended with exactly one comment, id 5841495806 by `github-actions[bot]`, and GraphQL `userContentEdits` lists 4 revisions (00:26:06, 00:26:37, 00:27:37, 00:28:05 UTC, the day #1006 was opened) -- the quiet run wrote nothing. Across the 100 most recent runs of the workflow, 41 logged a decision: 25 create, 11 none on an unchanged note, 5 satisfied with nothing to resolve. #1006 was then closed as not planned. NOT proven: the tooling-error transitions (no note posted on error; an existing note kept or marked unknown) and the later-page, spoofed-marker and stale-body paths were not driven by a real event; they rest on `Scripts/test_canon_issue_bot.py`. The legacy adoption is not deployed until this merges, so no real event has driven it; it rests on 8 new cases in `Scripts/test_canon_issue_bot.py` (71 tests OK) and on replaying `legacy()` over the 99 `github-actions[bot]` issue comments since 2026-09-14: 73 match the old format, 26 are managed notes, 0 are neither. The events were created with the maintainer's own token, so an outside contributor's permissions were not exercised. |
| #955 | OPEN | Backlog, filed 2026-09-21 during the owner-authorized SSOT reconciliation as a follow-up to ADR-009/#292 and ADR-013/#301 rather than a new ADR number, with #308 owning scheduling. It holds the obligations that #725 did NOT discharge when it closed #301: a named Channel EQ parameter writer is not the full plug-in snapshot apply, the compensation and verified inverse operations, or the automatic-EQ recommendation integration the original ADRs mixed in. Filed so that narrowing the current work does not silently drop them. Nothing is implemented or live-qualified by the issue existing |
| #956 | closed | **Closed by the pull request carrying this row; the row is ahead of GitHub until it merges.** #956 asked for the merged files to be read before track D was marked done. PR #1009 merged as 55f767f0, and on 2026-09-26 its entry files were read on `main`: this file's opening routes scheduling to #308, `docs/adr/README.md` routes to #308 and keeps the old order as history, and `docs/API.md` describes `saga_cancel`; the pinned-issue list read back as #308 alone. Documentation only. What changed: this file's opening now routes scheduling to #308 and owning issue bodies instead of claiming the order; the #308 row, the phases preface, and the ADR-001, ADR-008, ADR-010 and #448 rows now carry the 2026-09-21/22 reconciliation (restored ADR-001, selected #291 R0–R3, #293 and #448 as transfers); `docs/adr/README.md` routes to #308, marks the old binding order history, corrects ADR-001 to `In Implementation`, lists ADR-020–026 once with #965–#971, and supersedes the stale C4-EQ and cache-coordinator deferrals; `docs/API.md` describes `saga_cancel` from current source. Native metadata (track M), applied in three passes and read back 2026-09-26: the open milestones are exactly 9 `Stock plug-in & EQ completion`, 11 `Current release reliability`, 15 `Session repair — routing and organization` (#291, #965–#971) and 16 `Maintenance & locale migration` (#892, #904, #951, #956), all with no due date; milestones 8, 10, 12, 13 and 14 are closed with their history; the nine backlog issues have no milestone; #308 is open, unassigned, has no milestone and is pinned. #308 §7 records each pass. NOT proven: that every long row elsewhere in this file agrees with its issue body — only the rows named here were reread; the individual ADR documents (`ADR-014`, `ADR-019`) keep their own status lines; no Logic run was made. |
| #957 | OPEN | Backlog, filed 2026-09-21 as the explicit owner of the client-facing projection phases ADR-003/#286 proposed and did not deliver: richer per-command `oneOf` input schemas, progressive client rollout, and the generated API/test/qualification projections. #286 shipped registry authority, runtime validation and handler consistency; a working command catalog is not those projections. The current `{command, params}` client contract is unchanged and the registry is not to be rebuilt for this. #308 decides activation. NOT STARTED |
| #958 | OPEN | Backlog, filed 2026-09-21 as the explicit owner of the analysis modes ADR-012/#300 proposed and did not deliver: `compare_spectra` over matched before/after artifacts, reference matching, custom target curves, further source profiles and recommendation modes. #300's read-only spectral core is implemented; the current heuristics are not all of those modes. #308 decides activation. NOT STARTED |
| #960 | closed | **Closed 2026-09-22; landed by #963 (`ffcf0bf8`).** Filed 2026-09-21 out of #959's own merge. Editing a pull request's description after CI is green makes `build`, `compile` and `test` read as EXPECTED again and the merge is refused, because the newest CI check suite is the metadata-only run whose gated jobs changed their names under #952 -- absence in the newest suite governs, not success in an older one. Measured three ways on #959 at head `e42db738`: `gh pr checks` and GraphQL `isRequired` both reported every required context SUCCESS while `PUT /pulls/959/merge` answered `405 "3 of 4 required status checks are expected"`; re-running the code run did NOT clear it; closing and reopening, which is a CODE event, did, on the first attempt. The head never moved and the branch was never behind. The cost of a typo in a description is a 35-minute macOS re-validation. The fix takes `edited` out of `ci.yml`'s trigger, which leaves the default set and no metadata-only run to make a newer suite, and moves the obligation that subscription carried rather than dropping it: a retarget still arrives only as `edited`, so `pr-policy.yml` -- required since 2026-09-21, already subscribed to `edited`, about fifteen seconds long -- now runs `classify-pull-request-event.py --refuse-unvalidated-edit` as its first step and goes RED for any `edited` that is not a title or body change. It cannot revalidate the code and does not try to; a red required context holds the merge until a push or a reopen puts the code gates over the pull request as it now is. The refusal reuses `classify()` rather than keying on `changes.base`, so an editable field nobody has taught this code about is refused too instead of inheriting the exemption by being unknown. `classify` and the display-name switching stay in `ci.yml` and can no longer fire: they are the apparatus that makes re-subscribing `edited` survivable, and `Scripts/test_pull_request_event_classifier.py` now pins the trigger list and the single-domain concurrency group so a half-restoration fails rather than silently bringing back the cancellation #950 paid to fix. Eleven mutants -- six in the script, two in each workflow, one in the group -- were each killed by a named case |
| #965 | OPEN | Epic filed 2026-09-22 as ADR-020: a whole-session inventory of tracks, strips and layout for session repair, extending the existing audit readers. Its body marks it selected with O1 ready. Its body calls itself a specification, not implementation or live evidence, and says #308 schedules it; the ADR document is not in `docs/adr/`. |
| #966 | OPEN | Epic filed 2026-09-22 as ADR-021: intent-aware repair planning as an extension of #28, telling observed wiring apart from musical intent. Its body marks the pure P1 work ready. Its body calls itself a specification, not implementation or live evidence, and says #308 schedules it; the ADR document is not in `docs/adr/`. |
| #967 | OPEN | Epic filed 2026-09-22 as ADR-022: an owned bus/aux lifecycle that creates or reuses the intended return without disturbing existing routing, not another routing engine. Its body lets B1 allocation/reuse tests start and holds live creation until inventory, endpoint and provider evidence exist. Its body calls itself a specification, not implementation or live evidence, and says #308 schedules it; the ADR document is not in `docs/adr/`. |
| #968 | OPEN | Epic filed 2026-09-22 as ADR-023: exact naming adapters for the existing cleanup workflow, with explicit naming plans and preservation checks. Its body marks N1 adapter and compatibility work ready. Its body calls itself a specification, not implementation or live evidence, and says #308 schedules it; the ADR document is not in `docs/adr/`. |
| #969 | OPEN | Epic filed 2026-09-22 as ADR-024: layout adapters that keep the shipped sort and add the missing exact ordering, grouping and view layout. Its body marks L1 integration and a bounded L2 investigation ready and says arbitrary movement is not qualified. Its body calls itself a specification, not implementation or live evidence, and says #308 schedules it; the ADR document is not in `docs/adr/`. |
| #970 | OPEN | Epic filed 2026-09-22 as ADR-025: an exact-target color provider and scalar styling adapter. Its body blocks public verified writes on C1, reading the target's actual color, and lets a bounded provider investigation proceed. Its body calls itself a specification, not implementation or live evidence, and says #308 schedules it; the ADR document is not in `docs/adr/`. |
| #971 | OPEN | Epic filed 2026-09-22 as ADR-026: session-repair integration over the existing audit and Saga, with bounded execution and an independent final verification. Its body lets pure adapter and coordination tests start and holds full execution for the component capabilities it names. Its body calls itself a specification, not implementation or live evidence, and says #308 schedules it; the ADR document is not in `docs/adr/`. |
| #972 | closed | **Closed 2026-09-25; landed by #976 (`181de680`).** An outside feature request filed 2026-09-22 and implemented by its author in #976: `AXPluginInstanceIdentity.census(pluginName:identifierPrefix:)`, a public read-only census that lets a host application find its own plug-in instances — Mixer strips whose insert slot names the plug-in, and the `kAXIdentifier` each open editor window advertises. It reads through `getMixerArea` and `pluginEditorWindows`, the two locators #977 made match in all ten languages; submitted before #977, the census found neither the strip nor the open editor on a Spanish or French Logic, and no open editor on an Italian or Chinese one. The author's live record is `2026-09-23-972-en-US-plugin-instance-identity`; a maintainer census of the rebased change on 2026-09-25 found the strip and the open editor in all ten locales, with the as-submitted build as the same-run control. That census is a table in #976's conversation, not a committed record. |
| #973 | closed | **Closed 2026-09-24; landed by #974 (`1bd0f2d6`).** An outside report filed 2026-09-23: `mixer.set_volume` and `set_pan` stopped at the nearest ~10-raw detent, though an `AXValue` write moves the header fader one raw unit. #974, from the reporter's fork, adds a fine phase after the detent loop that writes `AXValue` at most one detent (10 writes) and reads back each step, so a request lands on the nearest whole raw position. Its behavioural record is `docs/observations/2026-09-23-973-en-US-header-mixer-fine-axvalue.json`. |
| #977 | closed | **Closed 2026-09-25; landed by #978 (`0d6183eb`).** Filed 2026-09-25: three shared locators matched Logic's interface in fewer than ten languages. An open plug-in editor read as a blocking modal on a Spanish, French, Italian or Chinese Logic, so `project.save` and `set_param_verified` refused; the docked Mixer was not found on a French or Spanish Logic, so `get_inventory` returned State B; and an insert slot's bypass toggle was recognised by label in only some languages. The three LabelSets now derive from Apple's rows (MAToolKit `bypass`, MAGUI `bypass`, Logic.framework `Mixer#acc`), and the Mixer is matched with `containsNormalized` instead of a lowercased exact lookup against members that keep Apple's capitals. Measured live 2026-09-25 in all ten locales on Logic 12.3 (6674): the `2026-09-25-977-*` records, three per locale, with the pre-change build as the same-run control; it failed in es-ES, fr-FR, it-IT, zh-CN and zh-TW and passed in the other five. |
| #979 | closed | **Closed 2026-09-25; landed by #990 (`bd237ee8`).** Filed 2026-09-25 out of #977: `mixerNamedElement` cited `Mixer#acc`, which Italian ships lowercase while an Italian Mixer reads `Mixer`. Italian could not say which row feeds the description, so the question went to the other main-window areas, read on a running Logic in all ten locales by `Scripts/livekit/live_979_area_description_rows.py`. Every area with a `StrTabBtnLabel|||<area>` row (Tracks, Control Bar, Library, Mixer) carries that row's value exactly; the `#acc` rows fail by spelling in Portuguese (`Barra de Controles`, not `Barra de Controle`) and the plain rows in German (`Bibliothek`, not `Mediathek`). `mixerNamedElement` and `controlBarGroupLabel` now derive from `StrTabBtnLabel`, and `mixerInspectorContext`, whose area has no such row, from `StrViewBtns|||Inspector`. No match changes, because all three ignore case; the Library browser that `libraryPanelLabel` matches sits deeper than this harness reads and is not covered. |
| #980 | closed | **Closed 2026-09-25; landed by #989 (`3e326d90`).** Filed 2026-09-25 out of #977: the rationales of `pluginBypassControl` and `pluginEditorBypassControl` said the toggle was read live in three locales, while #977's twenty records read it in all ten. Both rationales now point at those records, and `docs/locale/ui-labels.json` is regenerated from the policy. Wording only; no member changes. |
| #981 | closed | **Closed 2026-09-25; landed by #991 (`316f523d`).** Filed 2026-09-25 out of #977. `_apple_ships` in `Scripts/locale_labels.py` compares a label's strings with Apple's rows byte for byte, so a member that equals Apple's row only after case folding counts as not shipped; `mixerNamedElement` read `unmeasured` in de-DE for that reason, and #977 closed it with a live record rather than widen the shared function. Coverage is understated wherever Apple's casing differs from a normalised member. The pull request pins `docs/canon/ledger/casefold.tsv`: the ledger strings `build` found in the corpus up to case, compared as strings and stored as strings, and both `_apple_ships` and the ratchet's `_derivable_variants` now ask only it; only case is forgiven, and no credit comes from a digest. 217 coverage cells moved from `unmeasured` to `derived`, and `RATCHETS.json` lost those 217 and 21 undocumented variants. Five cells moved the other way: the base credited `Logic Pro` in de, es, fr and ko and `Audio Units` in es through `normalize`'s no-break-space fold, and they are raised in `RATCHETS.json` until a live reading shows what the running Logic displays. |
| #982 | closed | **Closed by the pull request carrying this row; the row is ahead of GitHub until it merges.** Filed 2026-09-25 out of the #976 review (R-972-01). `stripEnumeration(in:)` and `audioPluginInsertSlots(in:)` read children with `getChildren`, which answers a failed read with `[]`, so a Mixer or strip that could not be read is indistinguishable from one with no strips or no inserts, and `mixerChannelStripsIfCompletelyRead` passes that on as a complete read. `insert_plugin` fails closed but reports `track index out of range`; the other callers are not audited. #976 closed the gap in the census only, through `children:` overloads fed by `childrenResult`. The two enumerations now answer nil for a failed read and `[]` only for -25205 or -25212. `get_mixer_state`, `get_channel_strip` and `get_inventory` report the strips unknown; `insert_plugin`, `insert_verified` and `set_param_verified` refuse for that reason before any write. `pluginSlots(in:)` answers nil too, so the two Mixer readers no longer mark an unread strip's chain `plugins_source: "ax"` and say why in `plugins_read_error`. The Mixer lookup keeps the failure as well: Logic 12.2 and 12.3 give the Mixer no identifier, so it is found by reading its strips, and a failed read at or inside a Mixer-named container outside the Inspector now answers `childrenUnread` instead of "not locatable" when no readable Mixer is found. Every caller above reports or refuses it the same way, and `get_inventory` does not run its reveal over it. Not changed: 76 other `getChildren` calls in 17 files still flatten a failed read, and `mixerChannelStrips` still discards the #290 unreadable-child count for its 10 callers. |
| #983 | closed | **Closed 2026-09-25; landed by #988 (`f706d754`).** Filed 2026-09-25 out of the #964 review (R-964-02): `docs/API.md` opened with a release summary naming v3.15.0 two releases later, and no test read it. The summary is dropped — `CHANGELOG.md` is the release history — and the surface counts that remain are pinned by `testReadmeAndAPIDocsMatchPublicSurfaceAndRouting` as the README's are. The same page's "No MIDI read-back command is shipped in v3.12.0" loses its version anchor; it is still true at 3.17.0. |
| #984 | closed | **Closed by the pull request carrying this row; the row is ahead of GitHub until it merges.** Filed 2026-09-25 out of the v3.17.0 cut: a mutating operation passes only on a recorded write cycle, which `QualificationTransport` files `.verifiedWriteCycle`, and `PromotionGate.operationIsLiveCredited` credited `.semanticReadback` only, so no Phase-B pass could credit its operation or close R-SEM (v3.17.0 preflight: credited=21, none mutating, 14 mutating write-cycle passes). **Decision (#373): yes** -- a verified write cycle credits, on the condition that its restore was read back and matched the original, not merely that a record is present. **Change:** `QualificationCase` carries `restore` (`record_sha256`, `verified`), filled from `QualificationOperationResult.restore`, where `verified` means the record's shape verifies: none of its readings says it did not happen, the readback shows the recipe's value moved on the pre-state's own track (for play, stop and pause, whose recipe records the pre-state before staging the opposite state, that the readback stands where the operation puts the transport), the restore readback matches the pre-state on that same track, and both writes answered as writes; that one check (`verifiedCycleShape`) is also what the trusted-artifact validator applies; `operationIsLiveCredited` credits `.verifiedWriteCycle` only when `restore.verified` is true, beside the unchanged `.semanticReadback` rule; the release gate and the R-SEM producer both call that one function; the verifier binds the case's `restore.record_sha256` to the evidence's `mutation_restore_record_sha256`; the live release-gate test now also asserts every write-cycle pass is credited. **Measured:** `Issue984VerifiedWriteCycleCreditTests` 13/13; reverting the gate hunk turns the two credit tests red, and five further mutants (credit a cycle without a restore, credit on presence, make reads need a restore, make every restore verified, drop the binding conjunct) each turn a named test red; after three reviews, removing the track-identity comparison, the moved-value requirement or the transport exception each turns named tests red; the touched suites pass, 218 tests with 2 skipped; repo guards all pass. No committed artifact derives from the credit rule (the repo-tree evaluator passes no live credit), so nothing was regenerated. **NOT proven:** credit from a real Logic session -- the release-gate test that exercises the credit on real results needs a release build and a running Logic and was skipped, so no operation is shown credited from a Logic run; the transport recipe's staging guard is not driven by a unit test (it takes a real subprocess session), and the disabled Phase-C track recipes' comparisons were not run. |
| #985 | closed | **Closed by the pull request carrying this row; the row is ahead of GitHub until it merges.** Filed 2026-09-25 out of the v3.17.0 cut. The two `QualificationRunnerTests` that need a running Logic are enabled only when `.build/release/LogicProMCP` exists, and `Scripts/release-stable.sh` ran `swift test` before `swift build -c release`, so at a release they drove whatever binary the tree built last, or were skipped. A stale count (#884) and an unsatisfiable assertion (#984) shipped unseen that way. Decision (Isaac, 2026-09-26): a release needs a live, permissioned Logic. `Scripts/release-qualify.sh` is now the one gate: it builds release, stops unless a `Logic Pro` process is running and the new binary's `--check-permissions` exits 0, then runs the whole suite. `Scripts/release-stable.sh` and `Scripts/release.sh` both call it before they tag, and write the commit they qualified into the annotated tag as a `Live-qualified:` line. `.github/workflows/release.yml` runs `Scripts/release-tag-is-qualified.sh` before it builds or publishes, so a tag pushed by hand without that line stops there; the named commit may differ from the tagged one only in the `sha256` line of `Formula/logic-pro-mcp.rb`, the one line `Scripts/release.sh` rewrites after qualification. `Scripts/test_release_builds_before_live_qualification.py` runs the gate with stubbed `swift`, `pgrep` and release binary, and the tag check against real temporary repositories; it fails when the gate continues without Logic or without a permission, runs part of the suite, or tests before it builds, when either script loses the gate or the tag line, when the tag check lets another file or another Formula line change, and when the workflow loses the check. NOT proven: a release cut end to end with this order, the workflow step on a GitHub runner (it runs only on a tag push), a `Live-qualified` line typed by hand (anyone who can push a tag can write one; evidence that cannot be forged is #816), and what the suite itself does without a permission (the gate stops before it). Six release-binary tests failed against a freshly built binary on 2026-09-26 (PR #1005), so the next release stops at the suite until they pass. |
| #992 | closed | **Closed by the pull request carrying this row; the row is ahead of GitHub until it merges.** Wrong: eight sites in four canon guards read `not canon.is_absent(...)` as proof that Logic ships a string. The 32-bit absence set proves only absence, so a prefix collision refused a legitimate change at some sites and credited a string Logic does not ship at others (`check-policy-literals-against-canon.py:408`, `:567`, `:609`). `differs_only_by_decoration` read the same set the same way, so a collision hid a near miss. `translated_en_values` and `folded_entries` were written and never compared. Changed: `canon.presence` answers `ships`, `absent` or `unconfirmed`, and `ships` comes only from `docs/canon/ledger/presence.tsv`, a string comparison pinned by the new `Scripts/logic_canon.py confirm` and re-derived by `build`. Each site now handles `unconfirmed` in its own direction: the ones that refuse on presence still refuse and name `confirm`, and the ones that credit presence do not credit it. `verify_presence_ledger` checks the ledger's declared counts and its rows against the sets, and `verify_derived_counts` compares the two manifest counts. `check-canon-citations.py` runs both. Measured: `confirm` over the 1243 strings the policy-literal guard asks wrote 1887 rows, and 0 of them is a 32-bit collision. A full `build` re-derived every canon file byte for byte (`diff -r` empty). `Scripts/test_canon_presence.py` has 27 cases, and 16 of 16 mutations that restore an old reading turn it red (re-run after the review's repair). NOT proven: no real collision exists in this tree, so every collision case is a fixture. A new string that matches a prefix refuses until someone runs `confirm` on a machine with Logic, even when Logic ships it. The ledger and its count can still be forged together, as with the other pinned files. |
| #993 | OPEN | Filed 2026-09-25 out of #981 (PR #991). Logic 12.3's corpus ships `Logic Pro` with U+00A0 in de, es, fr and ko and `Audio Units` with U+00A0 in es, while `stemExportProgressWindowTitle` and `pluginMenuAudioUnits` are written with U+0020, and `caseInsensitiveCompare` does not equate the two. Needs a live reading of the window title and menu item in those locales; #991 moved the five cells back to `unmeasured`. |
| #994 | closed | **Closed by the pull request carrying this row; the row is ahead of GitHub until it merges.** Filed 2026-09-25 out of the #947 full-suite runs. `Issue529MenuValidationTests.concurrentDialogRequestCannotShareTheObservedModal` awaited `gate.waitUntilEntered()` with no bound, and the gate opened only if the first call acquired the Go To Position `flock`, which every same-user process and two suites share. Two of three parallel full runs hung there with no result line. `gotoPositionViaBarSlider` now takes `dialogExecutionLockPath`, defaulting to the shared file, and the two tests that reach the lock pass files of their own. The first call's return also wakes the gate, so a refused first call fails the test instead of hanging it, and the test asserts the first call reached its script. |
| #995 | closed | **Closed by the pull request carrying this row; the row is ahead of GitHub until it merges.** Filed 2026-09-25 out of the #947 full-suite runs. `SerializedStdioTransportStallTests` closed both pipe ends when the test returned, while the transport's queued write block still held the descriptor number; a block that starts late writes its frame into whatever file reused the number, and the Go To Position lock file on this Mac held exactly that 18-byte frame. Reproduced 3/3 with a temporary test that kept the transport's serial queue busy: closing on the old schedule put `{"jsonrpc":"2.0"}\n` into the file that took the number, and leaking put it nowhere. Each pipe end in the two transport test files now closes only after its last user is done: the write end after `send` returned, the read end after the reader thread returned. An end whose user may still come back is leaked. Five sites: the stalled-reader, watchdog and ordinary-traffic tests, and the two reader-thread tests in `SerializedStdioTransportTests`. The same class was in the POLLNVAL case's own assertion: it closed a pipe's write end and then polled that number, which a test running in parallel could take first. It now closes the process's top descriptor number (`getdtablesize() - 1`), which lowest-free allocation hands out only when every lower one is in use. MEASURED under load, 30 alternating runs each: that case failed 6 of 30 on 97e3fae3 and 0 of 30 after. |
| #1003 | closed | **Closed by the pull request carrying this row; the row is ahead of GitHub until it merges.** Filed 2026-09-26 while re-running the #995 repair's tests under load. Two stall tests replaced the one process-wide report sink, `SerializedStdioTransport.reportMidFrameStall`, and restored it on return; when they overlapped, one restored stderr while the other was still waiting for its report, and the watchdog test timed out. The sink is now an `init` parameter of each transport (default: the stderr reporter), and the static sink and its lock are gone. MEASURED under load, 30 alternating runs each: the watchdog test failed 4 of 30 on 5e47505b and 0 of 30 after. Mutation: with the transport ignoring the injected sink, the watchdog test failed at its report wait. |
| #998 | closed | Filed 2026-09-26 out of #942 and closed the same day as a duplicate of it. #942's 2026-09-20 comment already counts these fourteen returns: the twelve dialog refusals, plus `DIALOG_UNIDENTIFIED_NEW_WINDOW` and `DIALOG_APPEARANCE_UNREADABLE`. The #942 row carries the decision #998 asked for. |
| #999 | closed | **Closed by the pull request carrying this row; the row is ahead of GitHub until it merges.** Filed 2026-09-26 out of #942. The State C goto-position refusal reported `menu_actuation_attempted: false` for `.dialogActuationIssued` and `.dialogSubmissionNotIssued`, and for `.dialogUnidentifiedNewWindow` and `.dialogAppearanceUnreadable` too; every one of them is returned only after the leaf menu item was clicked, and each now reports true. Both refusal receipts reported `dialog_cleanup: "unobserved"` for a post-leaf menu refusal, which the script returns only after it observed the dialog CLOSED, because the computed `cleanupObservedClosed` folded the dialog and menu cleanups. Only those two receipts read it; it is now `dialogCleanupObservedClosed` and answers for the dialog half alone, while the `executionFailed` payload of the same name is unchanged. The State B fallback-suppression receipt now carries `menu_state` for the menu half, as the State C one did, and a result that observed both halves closed reports `menu_state: "closed"`. Review found two more: the eight State B post-leaf receipts carried no `menu_actuation_attempted`, and five pre-leaf returns (preexisting dialog, three unreadable preexistence reads, the menu-pick ledger failure) reported false although the script sets its flag before the forced menu-bar click. The answer now comes from one exhaustive switch over the classification: true or false as the script recorded it, and `menu_actuation_indeterminate` when a dead child left `LEAF_ARMED`, `NOT_ISSUED` or an unknown stage, or when a result lacks the flag suffix. Every pre-leaf return that can follow the menu-bar click appends ` menu_actuation_attempted=` and the flag. `Issue999GotoRefusalReceiptTests` pins both cleanup halves for every post-leaf site, the flag for all five pre-leaf returns with true, false, absent and unreadable values, and a reconciled execution failure. Measured at 7c3c9faa: `swift test` over the #999, #942, #529, #105 and #572 suites passed 96 tests; with the three-case arm of the switch returning false, the #999 suite recorded 25 issues, and with the menu-validation-unreadable arm returning false, the two #529 witnesses recorded 6; `run-repo-guards.py` all 91 passed. Not proven: the script results and reconciler answers are canned, and no run against a running Logic reached any of these refusals. Not changed: the `dialog_route_outcome` labels, which still name the combined cleanup. |
| #1004 | OPEN | Filed 2026-09-26 out of #883. `controlSurfaceInstallMenuItem` holds Apple's German row, with U+00A0 before the ellipsis, and is matched exactly as the title of an item in the Control Surfaces Setup window's `New` menu. #883 showed one German menu drawing U+0020 where its row has U+00A0; it does not show that every menu does. The only live reading of this item is ko-KR, so the German spelling is unmeasured. Needs a German read of that item's `AXTitle` scalars. Not started. |
| #1016 | closed | **Closed by the pull request carrying this row; the row is ahead of GitHub until it merges.** Filed 2026-09-26 out of #993's live run. After a refusal that follows the slot press, `insert_plugin` can return with Logic's plug-in menu still open at window layer 101 or above, and an open menu blocks AppleEvents, so the next `quit` sent to Logic fails. Seen after three refusals in zh_TW on 2026-09-26 (`leaf_not_offered_by_this_strip` twice from the candidate, `root_menu_not_found` once from the control); after the first, the Korean restore's quit failed with "Logic did not quit". An Escape sent through System Events closed the menus each time. `dismissOpenMenu()` posts one Escape and does not read whether a menu is still open. FIXED: the refusal runs the measured popup cleanup the verified write already uses (it counts Logic's windows at the pop-up menu level, cancels them and counts again) and reports `plugin_popup_menu_state` — `dismissed`, `no_popup_observed`, `window_count_unavailable`, or `could_not_be_dismissed` with both counts — with a `recovery_hint` whenever the state is not clean; the blind Escape is gone. LIVE on the Korean fixture (record 2026-09-26-ko-KR-insert-refusal-closes-its-plugin-menu, `Scripts/livekit/probe_1016_refusal_leaves_no_menu.py`): three `leaf_not_offered_by_this_strip` refusals per binary, alternating on one strip; the parent left a layer-101 Logic window open after all three and said nothing, the change reported `dismissed` after all three and the same window reading found none. The #993 ten-locale sweep at the same code read `dismissed` and no open menu after each of the change's eight `leaf_not_offered_by_this_strip` refusals and two zh_TW `root_menu_not_found` refusals. Not measured: the layer-102 window the report also saw (the product counts layer 101 only, and none appeared in these runs), and `window_count_unavailable` / `could_not_be_dismissed` against a running Logic (unit tests with a fixture window list only). |

### Three reopen reasons, checked rather than inferred

The instruction that produced this file asked for these to be established, not guessed:

- **#293** — not absence. `Sources/LogicProMCP/MIDIReadback/` has ten modules including the
  provider. The recorded problem is that the fixture gave every Event-List cell a child, so the
  collector was written against a shape Logic does not produce.
- **#301** — genuine absence of the *operations*: no band read or write is registered in
  `OperationRegistry`. What was inferred from that was wrong. The note read the absence as
  consistent with "a measured wall", and consistency is not evidence — an unbuilt feature and a
  blocked one look identical from the registry. Measured 2026-08-30 there is no wall: Channel EQ
  exposes 26 sliders, including all 24 named band parameters, and every slider is settable. The
  registry was empty because nobody had written the operations, which is the ordinary reason a
  registry is empty.
- **#300** — neither, and it is closed now. Four modules existed *and* two operations were
  registered; what was open was the promotion decision. Answering it took measuring what the flag
  was holding back, which turned up four artifacts nobody had recorded: three bands reporting the
  floor as a reading, an accumulator band drawing a cut at 20 Hz on material with nothing wrong
  there, a loudness gate published under the name `confidence`, and that same accumulator still
  reaching `frequency_peaks` after the first three were fixed. Promotion was the decision; the
  measurement was the work.

## Order — the phases

Numbering expresses dependency order, not a requirement to finish every preceding phase.

**Dated history, not the current order.** These phases were written on 2026-09-07, when #308 had
been closed and this file carried the order. #308 was reopened on 2026-09-21 and schedules the ADR
programme now (#308 §§3–6); where a phase below disagrees with #308 or with an owning issue's body,
the issue is right. Divergences known at 2026-09-26: Phase 5 lists #293, which closed on 2026-09-21
as a transfer to #302, not as completed MIDI reading (#308 §6). Phase 11 lists #291 and #448: #291
is now selected R0–R3 as the session-repair routing owner with R4 sidechain mutation later, and #448
closed as a non-completion transfer to #965/#966/#968/#969/#970/#971 (#308 §3). Phases 13 and 14
are owned by #284 (parent), #373 (implementation) and #816 (publication) (#308 §6). The phase text
below is kept as written.

### Phase 1 — Repair the code-only foundations

Deliver accurate harness interaction accounting, working manifest discovery, and removal of an unused MIDI destination.

- **Issues it closes:** #755, #769, #776.
- **PRECONDITION:** None.
- **DONE WHEN:** Server startup no longer publishes the unconsumed inbound destination; a resource-only harness records product interaction while an idle harness still fails that requirement; source-relative manifest discovery resolves the repository-root manifest with other lookup routes unavailable. These checks require no running Logic.
- **KIND:** `code`.

### Phase 2 — Make live evidence usable across locales and viewports

Deliver harnesses that find the intended UI and observe the region they actually moved.

- **Issues it closes:** #773, #778, #780.
- **PRECONDITION:** Phase 1, so resource-only runs can produce valid interaction evidence.
- **DONE WHEN:** Every affected harness runs successfully with its English-only selectors removed; Japanese region enumeration works using measured `trackContentExplicit` and `trackContentGeneric` forms; the move harness captures the region’s measured before/after frames and detects movement across changed zoom or scroll conditions. Japanese label work remains incomplete until measured on Japanese Logic.
- **KIND:** `mixed`.

### Phase 3 — Resolve MIDI ownership and feedback failures

Deliver confirmed feedback detection by the surviving owner and a server that remains responsive under the reported MCU conditions.

- **Issues it closes:** #683, #736.
- **PRECONDITION:** None for diagnosis; repeat endpoint measurements after Phase 1 changes publication.
- **DONE WHEN:** The surviving owner reports actual MCU feedback with `mcu.connected: true`, while a second instance declines conflicting ownership. The reported hang is resolved and the affected host confirms continued protocol responses. Closure remains blocked on a sample from the wedged reporter process or an equivalent reproducer; Desktop success alone does not resolve the Creator Studio report. SUPERSEDED 2026-09-09 by an owner decision, recorded rather than applied silently: the reporter has posted nothing since 2026-09-04, and #683 and #736 close on the MECHANISM — each defect reproduced, repaired and proven on the shipped binary — instead of waiting for a host that may never answer. What the closure therefore does not establish is that the reporter's host carried these defects and no other; that is a limit on the claim, not a step that was skipped. The condition above is kept verbatim because a done-condition rewritten to match what was shipped stops being able to disagree with it.
- **KIND:** `mixed`.

### Phase 4 — Verify the complete region move

Deliver move-to-playhead results that account for the whole affected selection.

- **Issues it closes:** #774.
- **PRECONDITION:** Phase 2, including the repaired visual witness.
- **DONE WHEN:** A live multi-region measurement establishes what Logic’s command moves and how it treats relative positions. Verification then checks the complete affected selection and detects a partial or wrong-region result. Completion is blocked on that initial measurement; single-region success does not establish multi-region semantics.
- **KIND:** `mixed`.

### Phase 5 — Reach independent MIDI readback in release builds

Deliver a production path from resolved region identity to the existing Event List collector.

- **Issues it closes:** #293.
- **PRECONDITION:** None; the triage already establishes that the collector can read live notes.
- **DONE WHEN:** A release build constructs `RegistryResolvedIdentityProof` from resolved production identity, registers and reaches the provider through the product, and returns complete note observations for the intended region. Stale identity, wrong-region reads, and incomplete observations cannot pass; duplicate notes and timing normalization survive comparison.
- **KIND:** `mixed`.

### Phase 6 — Establish independent expected-note ingestion

Deliver R2 positive verification backed by an independently established expected sequence.

- **Issues it closes:** #302.
- **PRECONDITION:** Phase 5 provides the qualified observed-note source.
- **DONE WHEN:** A release build reaches the live-ingestion boundary and positively verifies matching notes while rejecting deliberate mismatches. Expected notes come from precommitted independent intent or a distinct observation source; copying observed notes or attaching a provenance label cannot manufacture independence. R1 fixtures and debug seams do not satisfy this phase.
- **KIND:** `mixed`.

### Phase 7 — Connect verified note transforms

Deliver Piano Roll transforms whose verification uses the production independent-note source.

- **Issues it closes:** #303.
- **PRECONDITION:** Phase 6.
- **DONE WHEN:** A transform reached through the release product takes its input notes from independent readback, produces the intended live note changes, and detects wrong-target or incorrect-note outcomes. Its own write plan cannot substitute for the independent source, and missing evidence cannot produce a positive verdict.
- **KIND:** `mixed`.

### Phase 8 — Set and qualify the stock-plugin parameter scope

Deliver a shared capability registry whose advertised operations are demonstrably reachable.

- **Issues it closes:** #292.
- **PRECONDITION:** None.
- **DONE WHEN:** The issue explicitly resolves its acceptance scope against the recorded Controls-view slider and popup failures. Every retained capability demonstrates target identity, effective actuation, and independent readback on a live instance. Any reduced scope is recorded as an evidence-backed decision; experimental entries alone do not count as expansion.
- **KIND:** `mixed`.

### Phase 9 — Complete the Compressor scope

Deliver qualified Compressor controls and an explicit outcome for the remaining auto-setting promise.

- **Issues it closes:** #299.
- **PRECONDITION:** Phase 8 establishes the shared registry’s supported contract.
- **DONE WHEN:** Additional retained parameters are qualified through that registry, and retained auto-setting behavior lands the intended settings with readback. Anonymous sliders cannot gain identity through positional pairing. Parameters or auto-setting that remain unsupported receive an explicit scope decision backed by the relevant measurement; the landed threshold control alone does not close this phase.
- **KIND:** `mixed`.

### Phase 10 — Qualify actual third-party host parameters

Deliver a measured third-party capability boundary and production access to its supported controls.

- **Issues it closes:** #306.
- **PRECONDITION:** Phase 8 before integrating verified writes; initial third-party observations may start immediately.
- **DONE WHEN:** Actual third-party instances inside Logic are measured for parameter identity, effective writes, and readback, and supported capabilities are reachable through the product. Completion remains blocked on those measurements: stock Compressor observations cannot qualify third-party plugins. Unreachable scope is closed only through an explicit measurement-backed decision.
- **KIND:** `mixed`.

### Phase 11 — Complete observable mixer and track organization

Deliver honest routing coverage, observed track types, and resolved color and anchor-movement behavior.

- **Issues it closes:** #291, #766, #448.
- **PRECONDITION:** None for observation; Phase 2 before relying on affected localized harnesses for closure.
- **DONE WHEN:** Send edges have observed destination identities, or their exclusion is explicitly settled without publishing an incomplete graph as complete. Channel-strip type readback identifies supported cases and preserves `unknown` for ambiguous strips. Color changes and anchor movement each have an observable verification contract and a successful live demonstration, or an evidence-backed scope decision. Existing slot reads and global sorting do not prove these remaining behaviors.
- **KIND:** `mixed`.

### Phase 12 — Measure and complete region-editor capabilities

Deliver a resolved scope for remaining Smart Tempo and Flex Pitch control.

- **Issues it closes:** #304, #305.
- **PRECONDITION:** None; neither editor observation depends on plugin-parameter qualification.
- **DONE WHEN:** Smart Tempo analysis controls are measured with an audio region selected, and retained analysis modes and sub-bar tempo behavior are verified. Flex Pitch is measured in the Audio Track Editor on a Flex-analyzed region; retained editing has production snapshot access, stable note identity, and independent effect verification. Flex Pitch completion remains blocked on that editor measurement. Unsupported scope requires an explicit decision based on that surface.
- **KIND:** `mixed`.

### Phase 13 — Qualify successful mutations and restoration

Deliver qualification that can prove a mutation worked and that its effects were restored.

- **Issues it closes:** #373.
- **PRECONDITION:** Phase 1 for evidence accounting. Each operation’s live recipe requires its relevant capability phase; MIDI verification recipes require Phase 6.
- **DONE WHEN:** A qualification transport exists at all — **the one this line used to call "existing" was removed on 2026-09-13 along with the rest of ADR-001's subject** — and it performs real calls, checks independent post-state, restores or compensates, and verifies restoration. Negative controls expose stale readback and failed restoration. Disposable-project cases demonstrate final restoration. Coverage is reconciled against the current operation registry rather than inherited operation counts; refusal probes alone cannot qualify successful mutations.
- **KIND:** `mixed`.

### Phase 14 — Enforce same-artifact release qualification

Deliver release enforcement backed by evidence for the artifact actually distributed.

- **Issues it closes:** #284.
- **PRECONDITION:** None for release-workflow work. Phase 13 and evidence for the candidate’s claimed capabilities are required before enabling enforcement.
- **DONE WHEN:** Every remaining release contract is satisfied; the qualified artifact and distributed artifact have identical SHA-256 without rebuilding between qualification and publication. Current registered operations have required evidence or individually justified waivers — **under a policy this phase must now write, not "the existing policy", which was removed on 2026-09-13**. Missing, mismatched, or failing evidence prevents promotion. Completing Phase 13 alone does not satisfy the other release contracts.
- **KIND:** `mixed`.

## What can run at the same time

- Phase 1, Phase 5, Phase 8, Phase 11 observations, Phase 12, and Phase 3 diagnosis can start together. Phase 14’s repository work can also begin immediately.
- After Phase 1, Phase 2 and Phase 13’s qualification-mode work can run alongside the domain phases. Qualification recipes follow capabilities as they become available.
- The hard completion chains are **Phase 2 → Phase 4**, **Phase 5 → Phase 6 → Phase 7**, **Phase 8 → Phases 9 and 10**, and **Phase 13 → Phase 14**.
- Phases 9 and 10 may proceed together once Phase 8’s shared contract is settled. Phase 11’s three issue tracks and Phase 12’s two editor investigations can proceed independently.
- Phase 1’s endpoint changes and Phase 3’s ownership measurements must not overlap on one host. Measurements must name which publication behavior they observed.
- Concurrent live drives must not share a Logic instance: selections, active editors, Mixer visibility, and project mutations change each other’s preconditions. In particular, Phase 4 must not run alongside Phase 11’s Mixer work, and Phase 13’s restore recipes must not overlap another live mutation. Separate hosts permit parallel runs.
- Sequencing assumption: AX-only work does not require Phase 3. Any recipe that relies on MCU feedback must wait for its feedback confirmation.

## Where this plan is most likely to be wrong

1. **Readable controls may lack usable identity or actuation.** This threatens plugin expansion, routing sends, color verification, and Flex Pitch. Cheapest measurement: inspect one actual target inside Logic, attempt one reversible change, and read its effect independently. Use the specific editor and plugin; a neighboring surface cannot establish a wall.

2. **MIDI independence may remain unreachable in the shipped product.** Existing collectors and fixtures can hide a missing production connection. Cheapest measurement: drive one known region through a release build, obtain a positive match from the production boundary, then change an expected note and require a mismatch before building transforms.

3. **Host conditions may invalidate otherwise convincing evidence.** The MCU hang remains reporter-specific, while locale and active-window state have already changed outcomes. Cheapest measurements: obtain the wedged-process sample and surviving-owner feedback observation; run one affected harness in the required locale and window state before expanding the campaign.

## What this file does not claim

It does not claim any of the above is verified live. Issue state here is a transcription of GitHub
at the measured date, and the "waiting on" column is transcribed from each issue's own recorded
findings. Where this file and an issue disagree, **the issue is right and this file is the one that
is wrong** — the same relationship its predecessor declared toward a file nobody could read.
