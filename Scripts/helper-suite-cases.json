{
  "why": "A floor per suite, so a suite that quietly stops contributing cases fails instead of lowering a total nobody reads. Counted by the module that DEFINES each case, because an aggregator re-exporting a class would otherwise hide the loss. Growing is free; lowering one belongs in the diff, with the reason.",
  "measured": "2026-09-20",
  "suites": {
    "logic_bounce_main_test": 4,
    "logic_bounce_support_test": 13,
    "logic_bounce_ui_test": 15
  }
}
